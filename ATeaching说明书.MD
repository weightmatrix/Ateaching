# ATeaching说明书

本文档面向日常使用者，尽量不用技术术语，只说明“这个软件能做什么”和“应该怎么操作”。

ATeaching 是一套为一对一或小班授课准备的授课管理软件。它把教案、学生、随堂笔记、课后反馈、课时统计、排课、普通 Markdown 文档、图片、公式和画图集中在一个软件里。核心目标是：上课时少做重复工作，下课后自动留下清楚的资料，长期积累自己的教案库。

NodeMarkdown新管线中，点击搜索结果或目录会明确移动到目标位置。随后单击正文进入编辑不会再次擅自移动视野。只有键盘或回车使真实焦点超出当前可见区域时，编辑器才把焦点拉回屏幕；拖拽选择仍可在屏幕边缘正常滚动。

NodeMarkdown新管线在大文档中连续输入普通文字时，只更新正在编辑的Node。没有换行、跨行删除或层级变化时，不会重新检查、重建或重画整篇文档；中文候选确认也遵守同一规则。完整检查保留在文档载入和真正改变Node结构的操作中。

首页学生容器的右键菜单提供20个差异明显的图标和经典12色。图标用中文名称说明，颜色菜单直接显示色点，选择后立即应用到该学生。

上课时在搜索清单点击某条结果，会滚动到对应Node；在目录中点击L1、L2或L3也会滚动到对应Node。重复点击同一条仍会重新定位。这些操作只改变视野，不会擅自进入编辑状态或改变文字。

上课中删除一个已插入的非空H3包时，软件会把该学生完成清单中对应项退回未完成，可立即再次从“随堂”清单插入。一次删除包含多个H3时会分别处理；如果同一母本包仍在笔记其他位置，不会误退回。

点击“随堂”时，按ESC失焦后插入到文末；正在编辑某行时，插入到该行所属H1、H2节点或完整H3包的后面。打开随堂清单和完成插入都不会自动改变当前视野。

编辑教案时，可以在两个同时打开的教案窗口之间右键剪切和粘贴节点包。H3包的UUID不会因移动改变，已经使用它的随堂笔记会自动改指向新章教案。教案也有目录按钮，可通过拖动改变节点包顺序。上课目录拖到底部时会加快向下滚；L1展开只显示到L3。

NodeMarkdown新管线在空行第一次输入英文或中文时，焦点会停在刚输入文字的后面，不会再被空行时期记住的位置拉回行首。左侧编号和文字使用同一条真实基线；空行、拼音待选、中文定字以及含公式的行都遵守同一规则，第一字出现时编号不会因为行框变化而跳动。

NodeMarkdown新管线的临时问题诊断现在会同时写入Console和一个Markdown文件。31号诊断只在空行第一次输入文字时启动，真实文件位置是`/Users/Witten/Library/Containers/Han.ATeaching/Data/Library/Application Support/ATeaching/诊断/ATeaching诊断记录.MD`。复现后不再需要手工复制整段Console，开发者直接读取该文件即可。文件创建后会反读校验，每条诊断写完都关闭文件以保持随时可读；拼音重复事件会合并，定字后仍持续观察1秒，以便留下延迟跳回文首的真实写入者。本轮诊断不会改动文字、焦点或页面位置。

NodeMarkdown新管线会根据当前字体和公式图片的真实高度，让同行文字、公式与左侧编号共用同一条纵向中线。较高的分式和根式仍会自动撑高整行，避免碰到上下Node；它们不会再把同行文字压到下方。公式位于自动换行后的下一视觉行时，也不会带动首行编号或其他行文字。用于测量此问题的`【测试·29】`已经删除。

NodeMarkdown新管线输入`\\fr`并自动展开为`\\frac{}{}`时，只修改当前Node。后面的Node继续保持原来的层级、编号、字体、颜色和背景条，不会因为快捷展开多出的字符再次错位。用于定位此问题的临时Console诊断已经删除。

公式渲染所需的数学字体现在由App自身明确携带。即使Xcode的增量构建没有生成SwiftMath包装资源，普通Markdown和NodeMarkdown中的公式也会从App内的真实字体资源加载，不会因为找不到`SwiftMath_SwiftMath`资源包而退出。

NodeMarkdown新管线现在会在第一个字符真正写入前确认正在编辑的是哪一个Node。修改来自教案的H3包内任意一行后，“可保存”状态会立即出现；切换行、按ESC或Command+S时还会再按Node身份与外层文档复核一次，避免文字已经变化却没有保存、没有进入脏包更新。

NodeMarkdown新管线的Node会话和脏包诊断已经结束，`【诊断·26】`不再输出，避免干扰当前问题的Console。

NodeMarkdown新管线中输入拼音时，屏幕上尚未确认的字母会使用当前这一行设置的字体、字号和颜色，不会突然变成更大的默认字并撑高页面。正文3和正文4也会分别使用自己的颜色设置，新建或继续输入时不会沿用上一层的颜色。

NodeMarkdown新管线使用中文输入法时，拼音和候选文字会留在当前Node中预览。候选过程不会把下面Node的编号、颜色或背景条挪到上一行；选定中文后再正式保存。带背景条的Node如果因为文字较长而自动换成两行或更多行，背景条会覆盖该Node的全部实际高度，不再只有第一行。

NodeMarkdown新管线现在严格使用文档中真实保存的信息。每一行都按照自己的编号、层级、文字、来源和实际位置显示与编辑，不会因为附近一行的样式、当前视野或默认正文样式而猜测这一行是什么。点击、输入、保存和滚动也只使用系统实际提供的焦点与可见范围，不会为了补救错误而擅自移动到相邻行或文章末尾。

如果一个Node缺少编号、编号重复、层级超出范围，或者来源信息只保存了一半，新管线会拒绝这一次错误更新，并继续保留上一份完整内容，同时在Console留下说明。它不会暗中补一个编号、改成某个默认层级或清空来源。只有用户明确回车新建一行时，软件才会为新行建立新的编号。

NodeMarkdown新管线支持百度输入法等中文输入法。拼音和候选词尚未确认时，它们只属于当前输入过程，不会提前改变文档中的Node、层级、公式、图片或后面的内容；选定中文后才作为一次正式修改保存。刚打开文档时，开头几行的背景条和高亮会随正文一起显示，不需要先滚动页面。

NodeMarkdown新管线的背景条、高亮和编号会按照屏幕中真正可见的内容一起绘制。刚打开文档、回到文章顶部或轻微滚动时，不会把顶部内容误认为文末内容。旧的`【诊断-19】`已经停止输出。

NodeMarkdown旧管线在长文档中连续输入时，只处理正在编辑的当前一行。拼音候选和按空格确定中文时，不会重新整理整篇文档，也不会主动改变当前视野。按`Command+S`保存时只保存当前编辑结果，焦点和滚动位置保持不动。

NodeMarkdown可以导出彩色PDF或黑白PDF。黑白PDF使用白色背景和黑色文字，保留原有字体、字号、粗体、下划线、间距和背景条层次。文档设置中十二个层级直接按列表展示，正文一至正文五可以分别设置；十二级正文起点采用相同的逐级缩进距离。修改样式后，已打开文档会整体换成新样式。

新建机构时，软件会根据已有机构颜色推荐一个容易区分的新颜色。机构还可以选择一个图标，排课和课表中的课程会用透明大图标辅助识别。Debug的功能按钮中可以使用“颜色整理”重新分配当前机构颜色。

在排课课程上点右键，可以直接“打开信息”查看学生信息表，或“打开课反”查看该课程日期的课反；当天课反不存在时会按这个学生的统一设置建立。课程开始时间可用负数和正数快捷键前后调整5至60分钟。

单列表格导出图片前会先接收正在输入的最后一个字，因此不需要再次导出才能看到最后一行。学生改名完成后，授课页面上的学生卡片会立即显示新姓名。

在普通 Markdown 编辑器中自动保存时，背后已打开的档案列表会静默核对文件，不会因进度条反复出现而上下闪烁。如果文件名和类型没有改变，档案列表不会重建。

公式支持希腊字母的大小写、常见变体和字母类数学符号。直接输入`⍵`、`µ`、`Ω`、`ℏ`、`ℓ`、`ℕ`、`ℤ`、`ℚ`、`ℝ`、`ℂ`等字符也可正常渲染；公式内中文仍然支持。

NodeMarkdown中的教案图片由章教案统一保存。学生随堂笔记引用母本图片，但保存随堂笔记不会删除母本或其他学生正在使用的图片。删除新包里尚未入库的图片时，软件会先放入该文档自己的恢复区，撤销后可以放回；保存尚未确认完成时不会提前清空恢复区。为了保护历史资料，软件宁可暂时保留没有引用的图片，也不会在保存时自动猜测并删除图片。

NodeMarkdown旧管线支持中文输入法。拼音尚在候选时，系统只更新当前输入和可见区的编号、背景条等显示，不会改动后面Node的层级；选定中文后才正式保存到当前行。候选窗口存在时，搜索、外部刷新和后台状态更新都会等待定字。
候选拼音使当前行变长时，后续Node的编号和背景条会立即按新位置重画，不会短暂套用正文符号或留在原行。

---

# 一、主界面

打开软件后，主界面上方会看到几个主要入口：

- **授课**：进入某个学生的授课页面，开课、写随堂笔记、下课、同步教案。
- **教案**：管理自己的中心教案库。
- **教辅**：管理教辅材料。
- **学生**：管理学生名单和学生资料。
- **统计**：管理机构、成课、排课、周表和课时收入。
- **同步 / 设置**：管理同步、备份、显示样式、导出设置等。

Mac 版本标题栏会显示当前页面名称和软件版本号。iOS / iPadOS 版本界面会更简洁，不额外显示版本号。

---

# 二、学生管理

## 1. 新建学生

进入 **授课** 或 **学生** 页面后，可以新建学生。新建学生时，软件会为这个学生建立专属文件夹，并保存学生相关设置。

学生初始化信息通常包括：

- 基本资料
- 学习情况
- 授课相关设置
- 统计相关设置
- 同步文件夹设置

如果已经开启课时统计功能，新建学生时可以填写：

- **机构名称**
- **课时价格**

这里的课时价格指的是 **2 小时的价格**。例如一节标准课是 2 小时，价格填 600，则 1 小时会按 300 计算。

## 2. 修改学生资料

进入学生页面后，可以修改学生资料。学生改名会一次性更新：

- 首页和学生设置中显示的姓名。
- 学生档案文件夹，以及软件自动生成的随堂笔记、学生信息、上课信息和完成情况文件名。
- 学生信息和所有上课信息中指定的学生姓名项。
- 成课、排课和周表中这个学生的显示名。
- 学生个人同步文件夹，以及其中软件自动生成的问题、试卷和随堂笔记导出文件。

软件根据学生的唯一编号更新课时记录，不会因为两个人曾经同名而改错。改名前会检查重名和文件冲突，不会覆盖已有文件；中途失败时会恢复已经改动的内容。正在上课、编辑随堂笔记或执行后台同步任务时，需要先结束当前操作再改名。

## 3. 备份 / 删除学生

学生页面中的“备份”实际含义是：把学生从当前学生列表中移走，并把学生文件夹放起来。

备份后：

- 学生不再出现在当前学生列表里。
- 学生的设置从总设置中删除。
- 学生原有文件不会直接销毁，而是被移动到备份位置。

如果只是临时不用某个学生，建议使用备份，而不是手动删除文件。

---

# 三、教案管理

## 1. 教案是什么

教案是你长期积累的中心资料库。你可以把知识点、例题、公式、图片、课堂说明、练习步骤等内容写进教案。

软件支持把教案按章节、知识点逐层组织。上课时可以把某个知识点插入到学生的随堂笔记中，学生讲过什么内容也会自动留下记录。

## 2. 教案文件

教案可以包含：

- 普通文字
- 标题层级
- 图片
- 公式
- 画图
- 加粗、斜体、下划线、高亮
- 单列表格
- 普通 Markdown 文本

图片会被保存到对应教案的图片文件夹中，软件会自动生成正确链接。不要随意移动图片文件夹，否则文档里的图片可能找不到。

## 3. 教案与随堂笔记的关系

教案是“母本”，随堂笔记是上课时给某个学生使用的版本。

上课时，你可以从教案中插入某个知识点包。插入后：

- 这个知识点会进入该学生的随堂笔记。
- 如果课堂上修改了这个知识点，软件会记录为“有改动的包”。
- 下课或退出时，可以把这些改动同步回中心教案。

这样可以做到：课堂上发现更好的讲法，就直接改；改完后中心教案也一起进步。

如果随堂笔记中的 H3 知识点已经被修改，软件会优先保留随堂笔记里的改动。后续执行更新或下课同步时，会把随堂笔记中的脏包写回中心教案，而不是用中心教案旧内容覆盖课堂改动。

## 4. 洗教案

Debug设置的“功能按钮”中提供“洗教案”。它用于集中整理历史教案中的图片残留、公式旧链接和重复UUID，并重新连接随堂笔记。它不读写完成清单，完成清单由每次开课前的流程重新整理。

使用前请结束正在进行的上课或笔记会话。点击“洗教案”并再次确认后，软件会：

1. 清理确认已经没有真实图片的旧图片链接，并用`🌼{图片}🌼`留下位置标记。
2. 清理公式后面遗留的旧公式图片关联文字。
3. 保证正式教案中的节点UUID不重复。
4. 正式教案的H3更换UUID时，同时更新所有使用该H3的随堂笔记；随堂H3的UUID和来源节点会一起改成母本的新UUID。
5. 修复能够明确找到母本的随堂H3，并用当前正式教案重新生成该H3包。
6. 来源文件和来源节点都为空的H3始终视为尚未入库的新包，不根据同名标题猜测母本。新包的H3或包内其他节点与母本、其他新包发生UUID冲突时，只更换新包一侧的UUID。
7. 随堂笔记内重复的普通节点UUID可以直接更换，不影响正文、层级和母本连接。
8. 把无法确定的已有来源H3留在原处，不自动猜测，并生成报告供人工检查。

“上课收集”在这个过程中只用于查找，不会被修改。清洗完成后，UUID暂存表和人工处理报告位于`系统/暂存/洗教案/`。报告会另外列出随堂H3重连、新包换UUID和普通节点换UUID的具体文件、行号及新旧值。

---

# 四、授课流程

## 1. 进入学生授课页

在 **授课** 页面选择学生，进入该学生的授课页。进入页面本身不会自动创建一节课，也不会自动创建课后反馈表。只有点击 **开课** 后，才正式开始记录本节课。

## 2. 开课

点击 **开课** 后，软件会准备本节课所需资料：

- 打开随堂笔记
- 建立本节课记录
- 如果开启课时统计，并且学生设置里填了机构和课时价格，则自动加入一条课时记录
- 准备课后反馈内容

点击开课不会再增加成课记录。请先在排课中安排课程；进入排课页面时，当前时刻之前的排课会转为成课，成课只以这条路径产生。

## 3. 上课中的按钮

上课时，随堂笔记左上角会有常用按钮：

- **随堂**：插入或处理随堂内容。
- **更新**：把当前改动保存并整理。
- **下课**：结束本节课，进行导出、同步和收尾。
- **退出**：退出当前授课状态。

笔记状态下也会保持类似位置，方便形成固定操作习惯。

在这组按钮的右端还有上课批注按钮群：

- **批注（笔）**：开启或关闭批注状态，开启后可以手写红色批注。
- **橡皮**：按笔画擦除批注。
- **清除**：清空全部批注。

## 4. 上课批注

上课时可以在随堂笔记页面上直接写批注，方便圈画讲解：

- 点击 **批注（笔）** 开启批注状态，按钮高亮；此时鼠标点击不再进入正文编辑，但滚动页面不受影响。
- 按住鼠标拖动即可写出红色批注笔画，批注内容会跟随页面一起滚动。
- 批注只是上课时的临时内容，画在页面上层，不写入正文，也不会被保存或导出。
- 点击 **橡皮** 切换为橡皮擦除，激活时按钮高亮；点击或拖过某条笔画，会整条擦除该笔画。
- 点击 **清除** 一次性清空全部批注。
- 再点一次当前高亮的按钮（批注或橡皮）会关闭批注状态并清空全部批注；关闭后按钮恢复灰色。
- 在橡皮激活时点击批注（笔）会切换回手写；在笔激活时点击橡皮会切换为擦除，批注内容保留。

## 5. 随堂插入知识点

上课时可以从教案中选择知识点插入到随堂笔记。

插入规则大致是：

- 如果光标在某个大标题、小标题或完整知识点包附近，新内容会插入到它后面。
- 如果当前没有光标位置，新内容会插入到随堂笔记最后。
- 多个知识点按你点选的顺序插入。
- 已经插入过的 H3 知识点会在该学生该教案的完成清单中自动标为完成，之后不会再出现在随堂可插入清单里，也不能再次插入。
- 如果随堂笔记里已经有同一个来源的 H3 包，软件也会把完成清单对应项标为完成，避免重复插入。

插入知识点后，课后反馈的“上课内容”会自动增加：

```text
详细内容：
1.第一个知识点；2.第二个知识点；3.第三个知识点；
```

第一次添加时，软件还会在下面留出空白行，方便补充说明。

## 6. 下课

点击 **下课** 后，软件会做一组收尾动作：

- 保存随堂笔记
- 清理空行
- 整理有改动的知识点包
- 把需要同步的内容写回中心教案
- 导出 PDF
- 导出 HTML
- 更新课时统计
- 清理临时图片

导出的随堂笔记会进入学生同步文件夹下的：

```text
1-教案PDF
```

文件名通常是：

```text
随堂笔记_<学生名>.PDF
随堂笔记_<学生名>.HTML
```

如果同名文件已经存在，会直接覆盖，保证学生同步到的是最新版本。

---

# 五、NodeMarkdown 编辑器

NodeMarkdown 是 ATeaching 中最重要的编辑器。它适合写教案、随堂笔记和知识点结构化内容。

NodeMarkdown 的层级符号不是可编辑文字。无论选择新管线还是旧管线，页面左侧只显示设置中的十二级编号标志，正文从标志右侧开始。`### `、`- ` 等层级符不会放进文本，因此鼠标、方向键、回车和退格都不会进入一块看不见的禁入区。

TextKit2新管线的画面、鼠标点击、光标和键盘输入使用同一份正文存储。页面上显示出来的文字就是TextView实际编辑的文字，不再存在排版层看得到全文、输入层却只有空文本的双存储状态。

TextKit2新管线进入某行源码状态或恢复公式图片显示时，只更新这一行，不重新装载整篇文档。使用拼音输入时，未确认的拼音完全交给系统输入法管理；确认成中文后，编辑器才更新Node内容和显示样式。

点击一行进入源码状态时，只会改变刚离开的编辑行和当前编辑行。上下其他内容的背景条、高亮、编号、公式和图片会保持原样，不需要滚动页面才能恢复。公式渲染后的占位宽度按照公式实际显示宽度计算，较长的LaTeX源码不会在公式后面留下大片空白。

TextKit2新管线载入文档后，每一行始终按照自己的Node层级读取字体、字号、颜色、背景条和高亮。界面按钮、保存状态或焦点状态发生变化时，不会把整篇文字重新改成正文一级。公式显示完成后只保留公式结果，后面不会再接着显示公式源码。

TextKit2新管线在正在输入时以当前编辑区内容为准。后台保存或界面状态更新如果稍晚送回旧内容，编辑器会等待正确的新内容得到确认，不会用旧内容覆盖正在输入的文字，也不会把光标拉回文首。回车、合并行、改变层级和撤销后，光标按内容块身份和块内位置恢复；前面的行变长、变短或重新显示公式图片，都不会改变光标所属的内容块。

TextKit2新管线会在内存中保留当前源码的快速读取结果。普通输入、删除和拼音确认只更新受影响的文字与当前内容块；切换编辑行或当前搜索结果也只更新有关的几行。公式和图片仍从同一份正文显示，这项加速不会产生第二份可以单独修改的文档，也不会改变CSV文件格式。

- 点击某行后，焦点位于实际点击的正文位置。
- 按 Esc 可以退出当前编辑。
- 在行内按回车，会从光标处拆成两行；后半部分进入同层级新行，光标停在后半内容之前。
- 回车只会建立这一条新行。下面原有各行的层级、字体、颜色、来源和内容不会跟着上一行改变；当前屏幕会按各行原来的设置立即重新显示。
- 在非 H3 保护行的行首按退格，本行会接到上一行末尾，光标停在两段内容的交界处。
- 当前行、上一行或下一行包含公式和图片时，回车与行首退格仍遵守相同规则，不会跳过附件所在行，也不会跳到文尾。
- Tab 和 Shift+Tab 只改层级，正文内容和光标位置不变。
- 新旧管线都遵守同一套焦点规则；回车或合并行以后，光标不会因为后台保存、重新排版或公式图片刷新跳到文首或文末。
- 文档最后一行即使是空行，也是一条正式内容行；可以正常停留、回车、退格和继承层级。
- 上下方向键可以正常进入文末空行；在空行输入时使用该行继承的层级字体和段落样式。

文件保存时仍使用现有 NodeMarkdown CSV 格式，旧文件可以继续直接打开。

## 1. 基本概念

你可以把它理解为：每一行都是一个可管理的内容块。

它支持：

- 标题层级
- 正文层级
- 自动编号
- 图片
- 公式
- 画图
- 加粗
- 斜体
- 下划线
- 高亮
- PDF / HTML / MD 导出

平时看到的是排版后的阅读效果。点击某一行后，这一行会进入可编辑状态，可以直接改原文。

## 2. 点击编辑

单击某一行：

- 这一行进入编辑状态。
- 光标进入你点击的位置。
- 其他行继续保持排版后的显示效果。

编辑完后，离开这一行，它会重新排版。

## 3. 层级调整

在一行中按：

- **Tab**：进入下一层。
- **Shift + Tab**：回到上一层。

标题和正文都支持层级变化。正文前面的符号、缩进、字体、颜色会根据设置自动变化。

## 4. 自动编号

如果某一行是编号行，例如：

```text
1. 内容
2. 内容
```

在中间回车时，软件会自动插入新的编号，并把后面的连续编号顺延。

删除某一条编号时，后面的连续编号也会自动前移。

类似下面这种也支持：

```text
Step-1. 内容
Step-2. 内容
```

只要结尾是“数字.”，软件会尽量识别为编号。

## 5. 图片

可以在编辑器中插入图片。图片会保存到对应文档的图片文件夹中，文档里会写入图片链接。

图片宽度可以在文档中设置，例如：

```markdown
![image](Pic/img-xxxx.jpg){width=600}
```

导出 PDF 时，图片会跟随 PDF 缩放比例一起缩放。

## 6. 公式

公式使用类似 LaTeX 的写法。点击公式所在行可以编辑公式，离开后会自动渲染。

常用符号、中文、希腊字母、比较符号、全角等号、省略号等都会尽量支持。公式中的中文按普通字重显示，不会强行加粗。例如：

```text
$\sqrt{a^2+b^2}$
```

如果公式显示不正常，通常可以先检查左右的 `$` 是否成对。

## 7. 加粗、斜体、下划线、高亮

支持常见写法：

```markdown
**加粗**
_斜体_
<u>下划线</u>
<mark>高亮</mark>
```

排版后，这些符号本身不会显示出来，只显示效果。

下划线使用蓝色。高亮会用圆角背景显示。

## 8. 右键操作

在某个内容块上右键，可以看到与内容包相关的操作。

常见操作包括：

- **删除包**：删除当前内容块以及它下面属于它的所有内容。
- **母本删除**：只对教案知识点有效，会同时删除中心教案中的对应知识点和随堂笔记中的对应内容。
- **剪切**：剪切当前内容包。
- **粘贴**：把已经剪切的内容包粘贴到指定位置后面。

这些操作完成后，编辑器会刷新显示。

## 9. 保存

保存按钮可用时会显示为蓝色。保存完成并同步后会变成灰色。

可以使用：

```text
Command + S
```

进行保存。

## 10. 撤回和恢复

支持：

```text
Command + Z
Command + Shift + Z
```

用于撤回和恢复。

删除图片时，图片会先进入临时位置，方便撤回。关闭编辑器或下课后，临时图片会被清理。

---

# 六、普通 Markdown 编辑器

除了 NodeMarkdown，软件也支持普通 Markdown 文件。

## 1. 打开方式

普通 Markdown 文件可以从软件内部打开，也可以在访达中直接用 ATeaching 打开。

用访达打开 `.md` 文件时，软件会只打开 Markdown 编辑窗口，不需要同时打开完整授课主窗口。

多个 Markdown 文件会尽量放在同一个 Markdown 窗口中，以标签页形式打开。标签页也可以拖出来成为单独窗口。

## 2. 编辑与保存

普通 Markdown 支持：

- 源码编辑
- 双区域编辑：左边直接编辑，右边实时预览
- 搜索
- 自动保存
- 导出 PDF
- 导出 HTML
- 导出 MD

搜索时，输入文字后不会每输入一个字就立即搜索。需要点击搜索按钮或按回车后才开始搜索，这样大文件不会因为搜索而卡顿。

双区域编辑中，左侧是 TextKit2 编辑区，右侧是 Web 预览区。这样可以一边保持大文件编辑速度，一边看到接近导出的排版效果。

双区域编辑会自动跟随滚动：左侧或右侧停止滚动后，软件会用屏幕中线附近的同一行内容作为锚点，让另一侧对齐到对应位置。打字停止约 3 秒后，也会重新做一次对齐。

## 3. 导出

普通 Markdown 可以导出：

- PDF
- HTML
- MD

导出时会使用系统导出窗口，方便选择保存位置。

如果文档中有分层数字编号，例如：

```markdown
2. 第一层
  1. 第二层
```

导出 PDF 或 HTML 时会保留你源码中写出的数字和缩进，不会把不同层级强行改成同一串编号。

---

# 七、单列表格

单列表格适合做固定格式的资料，例如：

- 学生信息表
- 课后反馈表
- 标准化记录表
- 只有一列内容的清单

## 1. 模板

每个单列表格都可以来自一个模板。模板变化后，使用这个模板的表格会自动跟随更新。

更新时：

- 模板保留的项目，原表中的内容会继续保留。
- 模板新增的项目，会出现在表格中。
- 模板删除的项目，会从表格中移除。

这样可以在不丢失已有内容的前提下，统一修改表格结构。

## 2. 编辑

打开单列表格后，可以逐项填写内容。

表格支持统一背景样式。浅色模式下可以使用草绿到淡金的渐变，深色模式下可以使用墨绿到普鲁士蓝的渐变。

## 3. 导出图片

单列表格可以导出为图片。导出的图片会保留圆角、背景和整体样式。

---

# 八、画图功能

在 NodeMarkdown 中可以通过右键进入画图。

画图功能适合物理课堂中快速画：

- 直线
- 方形
- 圆形
- 三角形
- 坐标系
- 墙壁
- 受力示意
- 文字标注

支持：

- 画笔
- 橡皮
- 文字
- 形状
- 选择
- 复制
- 粘贴
- 删除
- 撤回
- 恢复
- 旋转
- 调整大小

部分吸附功能可以辅助把点、线、图形边靠齐。这个功能仍在完善中。

---

# 九、课时统计

统计功能用于管理机构、已经完成的课、未来排课和固定周表。

统计页面通常包括：

- **机构**
- **成课**
- **排课**
- **周表**

## 1. 机构

机构页面用于管理合作机构或课程来源。

每个机构可以设置：

- 名称
- 颜色
- 默认价格
- 备注

机构名称不允许重名。

右键机构，可以进行：

- 更改
- 删除
- 数据
- 复制上月
- 复制当月
- 导出上月
- 导出当月

删除机构时，软件会清空使用这个机构的学生统计设置，但不会修改历史课时记录。

## 2. 成课

成课表示已经上过的课。

成课页面会显示本月课程情况，包括：

- 月总课时
- 月总收入
- 每个机构的课时和收入
- 每节课的日期、时间、机构、学生、时长

成课图形表和排课一样采用横向表格展示，适合查看整个月的课程分布。

## 3. 排课

排课用于安排未来课程。

排课页面是横向月表，从 1 号一直到月末，可以用下方滚动条左右移动。

可以：

- 选择学生
- 添加课程
- 拖动课程
- 修改时间
- 删除课程
- 复制课程
- 粘贴课程
- 导出学生课表
- 导出机构课表
- 导出机构可用时间
- 导出整月可用时间

排课中的“刚需|生活”是一个特殊占位项，用于占用时间，不属于任何机构。

## 4. 周表

周表用于安排固定的一周模板。

它适合固定每周重复的课程安排。周表中的内容可以插入到排课中。插入时如果与现有课程冲突，冲突的课不会插入。

## 5. 冲突规则

如果两节课时间重叠，就算冲突。

例如：

- 8:00-10:00 和 10:00-12:00 不冲突。
- 8:00-10:00 和 9:30-11:30 冲突。

成课只来自排课。排课转入成课时仍执行时间冲突检查，冲突项不会重复写入。

---

# 十、文件、同步与导出

在“授课-教案”中，对某个教案文件夹或内部的章教案文件右键，可以选择“改名”或“删除”。改名填写新名称后还会显示关联影响并要求确认；删除也会提醒已有随堂笔记、完成清单和同步记录中的母本引用将失效，确认后才移入回收站。正在上课或笔记时不能执行这两项操作。

## 0. 档案文件操作

档案页支持像文件管理器一样管理文件和文件夹：

- 右键文件或文件夹，可以改名、删除、移动。
- 右上角可以新建文件、新建文件夹、多选和刷新。
- 多选后，底部会出现删除、移动、组成文件夹。
- 如果从访达中改动了当前档案目录，软件会自动刷新当前列表。

在软件内改名教案文件夹或章教案时，软件会同时更新学生随堂笔记、完成清单、教案模板、图片目录和图片链接。改名时不能正在上课或编辑笔记；请先关闭当前授课或笔记窗口，再执行改名。

删除会优先移入软件回收站，而不是直接永久删除。

## 1. 学生同步文件夹

每个学生可以设置同步文件夹。下课导出的 PDF 和 HTML 会写入学生同步文件夹中的固定子目录。

如果这个同步文件夹被百度网盘、iCloud 或其他同步工具管理，学生端就能拿到最新资料。

## 2. 导出格式

软件支持多种导出：

- NodeMarkdown 导出 PDF
- NodeMarkdown 导出 HTML
- NodeMarkdown 导出 MD
- 普通 Markdown 导出 PDF
- 普通 Markdown 导出 HTML
- 普通 Markdown 导出 MD
- 单列表格导出图片
- 成课 / 排课 / 周表导出图片
- 机构数据复制文本

## 3. 导出样式

导出会尽量保持软件内的显示样式，包括：

- 字体
- 颜色
- 层级
- 图片大小
- 公式
- 背景色
- 下划线
- 高亮

NodeMarkdown 的 PDF 缩放比例由设置中的 PDF 设置控制。

---

# 十一、备份、回收和安全

软件尽量避免直接破坏数据。

常见保护包括：

- 删除学生时先备份。
- 删除机构时不改历史课时记录。
- 删除图片时先进入临时位置，方便撤回。
- 关闭或下课前自动保存。
- 下课时自动清理空行和临时文件。

建议定期备份整个 ATeaching 数据文件夹，尤其是在大量修改教案或学生资料之前。

---

# 十二、推荐日常使用流程

## 1. 课前

1. 打开软件。
2. 检查教案是否准备好。
3. 检查学生设置和同步文件夹。
4. 如果需要，先在排课中安排本节课。

## 2. 上课

1. 进入授课页面。
2. 选择学生。
3. 点击开课。
4. 在随堂笔记中插入教案知识点。
5. 课堂中随时修改、补充、画图、插公式、加图片。

## 3. 下课

1. 点击下课。
2. 等待保存、导出和同步完成。
3. 检查学生同步文件夹中的 PDF / HTML。
4. 如有必要，检查有改动的知识点是否同步回中心教案。

## 4. 月末

1. 进入统计页面。
2. 查看机构、成课、排课。
3. 复制或导出机构数据。
4. 核对课时和收入。

---

# 十三、常见问题

## 1. 图片不显示怎么办？

先检查图片文件是否还在原来的图片文件夹中。不要手动移动 `Pic` 文件夹里的图片。

## 2. 公式显示不正常怎么办？

检查公式两边的 `$` 是否成对。如果公式中有中文，尽量保持完整，不要删掉公式开头或结尾符号。

## 3. 为什么有些按钮是灰色？

灰色通常表示当前状态下不能执行。例如没有选中内容、没有可粘贴内容、没有需要保存的改动等。

## 4. 下课后没有看到 PDF？

检查学生同步文件夹是否设置正确，并查看其中的 `1-教案PDF` 子目录。

## 5. 排课为什么插不进去？

通常是时间冲突。软件会阻止同一时间段出现重叠课程。

## 6. 机构删除后历史课还在吗？

在。删除机构只会删除机构设置，并清空学生当前使用的机构设置，不会删除历史课时记录。

## 7. 普通 Markdown 文件可以直接用软件打开吗？

可以。把 ATeaching 设为 Markdown 文件的打开方式后，可以直接从访达打开 `.md` 文件。

## 8. 为什么项目文件夹突然变大了？/ 项目里出现 build 目录怎么办？

构建产物统一存放在 `/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/ATeaching`（每个 App 在 `/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/` 下建自己名字的文件夹作暂存目录），项目文件夹内不应出现 `build` 目录。如果项目里出现 `build` 或 `Vendor/SwiftMath/build`，是直接用 `xcodebuild` 命令行构建时落错了位置，可以删除这两个目录（不影响源码），命令行构建请改走统一脚本：

```bash
./Scripts/build.sh          # macOS Debug
./Scripts/build.sh ios      # iOS Debug
./Scripts/build.sh clean    # 清理统一构建目录
```

---

# 十四、使用建议

ATeaching 最适合形成一套固定习惯：

1. 教案集中写在中心教案里。
2. 上课只从中心教案插入需要讲的知识点。
3. 课堂中直接改随堂笔记。
4. 下课让软件自动导出和同步。
5. 月末用统计页核对课时和收入。

这样长期使用后，软件会同时留下三类有价值的资料：

- 每个学生讲过什么。
- 每节课实际发生了什么。
- 中心教案如何逐步变好。

---

# 十五、iPhone上的统计和排课

进入“授课”-“统计”后，首页会直接打开“排课”。Mac、iPhone和iPad的默认页面相同。

iPhone屏幕较窄，统计页会自动改成手机布局：

1. 统计页面选择单独占一行，机构、成课、排课和周表不会与操作按钮挤在一起。
2. 排课月份在上方单独显示，导出和选择学生位于下一行。
3. 点击“选学生”会从屏幕底部打开学生列表；选中学生后弹页自动关闭。
4. 排课表在自己的区域内上下、左右移动，不会把统计页面的其他按钮顶出屏幕。
5. 排课记录在下方独立滚动，小屏手机也能进入并操作排课。
6. 课程、机构和机构数据弹页会按手机宽度显示；较宽的数据表可以左右滑动查看。

授课首页顶部按钮在iPhone上也可以左右滑动，因此即使屏幕较窄，仍能进入统计等页面。

---

# 十六、NodeMarkdown新管线的平台说明

在Mac上选择NodeMarkdown的TextKit2新管线后，软件会使用新的原生编辑管线显示和编辑文档。打开文档时，即使文本区域已经取得输入焦点，磁盘中的正文和各行层级也会正常载入，不会再因为焦点状态而显示空白。

iPhone目前在这个选项下使用基础文本编辑方式，因此可以看到和修改正文，但公式、图片、编号标志和各层级样式尚未达到Mac新管线的完整效果。

Mac的新管线按Node工作。点击一行后，只有这一行进入编辑会话；连续输入英文或拼音时，下面未编辑Node的层级、编号、颜色和内容不会重新解释。切换到另一行、按ESC、保存或执行结构操作时，当前行才以自己的UUID提交。

回车、跨行删除、Tab和Shift+Tab属于结构操作，会一次性更新相关Node。普通打字不触发整篇刷新。具有母本来源的H3节点仍受保护，不能被普通层级操作改成其他层级；在这种H3后回车会建立H4空行。

保存前软件会先收好当前正在编辑的Node，再生成完整文档快照。外部文件变化不会在活动草稿尚未提交时覆盖屏幕中的新输入。

软件不会在每打一个字时收集脏包和新包。当用户点击另一行、按ESC、让编辑器失去焦点或保存时，软件会比较进入Node时和离开时的层级、文字与来源信息。如果改动位于已经连接教案的H3包内，整个H3包进入脏包列表。H1和H2会正常保存，但不会被当成脏包。

新建H3后，在用户仍停留在该Node中时不会提前加入新包。离开该Node或保存时，如果它仍是H3且没有SourceID和SourceFile，才会进入新包列表。如果已经改成其他层级，就不会被当成新包。

Command+S和工具栏保存使用同一个入口。保存后可以继续在原Node中打字，后续修改仍会在下一次保存时正常落盘。

---

# 十七、NodeMarkdown旧管线编辑规则

旧管线显示文档时仍按照设置中的12个层级分别使用编号标志、字体、字号、颜色、段间距、背景条、加重和下划线。`### `、`- `等源码层级标志不显示，也不存放在编辑区域的文字中。

点击某行后，软件会暂存该Node的身份、层级、内容和来源信息。普通打字只修改这一行的暂存，不会把其他行重新解释成正文。切换行或按ESC失焦时，软件按UUID把最终结果写回原Node。

在一行中连续输入英文或拼音时，只有当前行文字长度会变化。下面各行的层级、字体、颜色、编号和缩进保持不动；在行尾输入也仍属于当前行，不会误入下一行。

拼音尚未确认和确认成中文是同一次输入过程。候选词变化只更新正在输入的这一行，不会重排或重新渲染下面的内容。

即使输入过程中发生短暂的行数变化，软件也会保留原有各行层级，不会因为页面中看不到`### `等源码标志而把整篇内容改成正文样式。

在普通行按Tab或Shift+Tab会改变该Node的层级，失焦后与文字一起写回。具有母本来源的H3节点自身受保护：Tab在其下方建立H4空行，Shift+Tab在其上方建立H2空行。

修改H3节点或其包内子节点后，软件会更新该H3包的修时，并在上课状态中把它加入脏包记录。

---

# 十八、设置页面

Mac设置首页使用单列上下结构。上方“设置”包括PDF设置和背景设置，下方“功能”包括备份管理、功能按键和Debug。

进入二级设置后，页面继续使用与首页相同的背景、圆角分组和大标题。内容较多时直接滚动整个页面；缩小窗口或在iPhone上使用时，内容会收窄到屏幕宽度，不会固定成只显示一部分的窗口。
# NodeMarkdown新管线的包提示与新行输入

在上课的NodeMarkdown新管线中，普通打字不会每次都重新检查包。用户点击另一行、按ESC、让编辑器失去焦点或保存时，才会检查当前Node。如果改动属于已有来路的H3包，整个包进入脏包列表；如果当前Node是有标题且没有来源的H3，它进入新包列表。

在一行中按回车建立新行后，焦点保持在新行已输入文字的后面。第一次输入不会把焦点拉回文字前面，也不会把新行误当成原来的下一行。
# NodeMarkdown新管线的中文拼音显示

使用中文输入法时，尚未选字的拼音使用当前Node已设置的字体、字号和颜色。例如当前行是橙色小字，拼音也保持相同的橙色和字号，不会突然变成蓝色大字。

候选文字的下划线和输入法提示仍正常保留。这项修复同时避免拼音大字号临时拉高当前行，减少后续Node、编号和背景条的上下跳动。
# APP内投屏

进入“投屏”后，先在标题下填写自己的名字。名字为空时，投屏和接收都不能使用；名字会保存在本机，下次不必重复填写。

点击“投屏”，输入四位数字并确认。软件会回到投屏页继续工作，状态区显示当前四位数字。可分别打开“上课”和“Markdown”，也可以同时打开。状态区下方显示已经连接的成员及每个人的批注颜色。点击四位数字旁的铅笔可以更换数字；更换后原接收端需要重新连接。

点击“接收”，输入主控显示的四位数字并确认。软件会在同一Wi-Fi内自动寻找，不需要填写IP和端口。连接成功后进入专用观看页；Mac上的其他编辑窗口会暂时隐藏，退出接收后恢复。连接断开时保留最后画面，回到投屏页确认已经自动填写的四位数字即可重新连接。

主控打开上课随堂笔记或普通Markdown后，接收端显示渲染好的文档。主控打字、滚动和移动鼠标会在短暂延迟后同步。主控缩小编辑窗口时，接收端停留在最后内容，不会变成黑屏。

接收页提供批注、橡皮、清除和退出。每个接收者有自己的批注颜色；“清除”只清除本人笔迹。iPhone竖屏时标题和退出位于第一行，频道与三个批注按钮位于第二行，按钮不会挤出屏幕。

Mac版第一次投屏时，系统可能询问本地网络权限，应选择允许。确认四位数字后，状态应稳定显示“投屏中，等待接收端”或已连接人数；如果系统拒绝监听，状态区会保留具体失败原因，不会闪回成普通的“未连接”。

# NodeMarkdown新管线回车

在任意Node的行内按回车，光标前内容留在原Node，光标后内容进入同层级的新Node，焦点位于新行行首。新行从第一刻起使用该层级设置的字体、字号、颜色和段落效果，不会先显示成默认正文再跳变。

回车不会主动改变当前视野。编辑器不再为了“保证光标可见”把新行定位到屏幕其他位置；仅当在文档最后一行继续输入、内容自然超过窗口底部时，滚动容器才按实际边界向下移动。

带SourceID和SourceFile的受保护H3按回车时不生成空H3，也不拆开来源包，而是在其后建立一个没有来源信息的空H4。

# NodeMarkdown块式新管线

Mac的NodeMarkdown新管线现在把每个Node当作独立编辑块。屏幕外的Node不创建文本视图，屏幕内的Node分别保存自己的文字、层级、公式、图片、编号和背景条。在某个Node打字时，其他Node不会重新解析或变更样式。

英文和拼音输入期间，系统只处理当前Node。输入导致当前Node换行时，下方Node只会按真实高度向下移动，内容、层级和外观不变。离开当前Node或保存时，编辑结果才按UUID提交到文档。

回车会把一个Node拆成两个；在行首按Delete可与前一Node合并，在行末向前删除可与后一Node合并。多行文字粘贴会建立多个Node，不会把隐藏换行塞进一个Node。这些结构操作可撤销和重做。

Tab和Shift+Tab只改当前Node层级。带母本来源的H3仍受保护；在这种H3后按回车建立无来源H4。图片文件、剪贴板图片、画图结果和公式都使用当前Node的局部插入与渲染，不能跨到相邻Node。

点击搜索结果或目录项会将目标Node放到窗口上方附近。随后点击该Node编辑不会再次强制移动视野。Node高度由实际文字、公式和图片测量，窗口宽度改变后自动重测。
