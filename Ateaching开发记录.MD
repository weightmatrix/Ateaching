# 2-37 NodeMarkdown新管线进入编辑时保持视野

- 版本号更新为`V1.2608112351`。
- 搜索结果和目录点击仍是用户明确发出的定位命令，可以滚动到目标Node；同一定位令牌不会因后续获得文本焦点而再次执行。
- 单击正文进入编辑时记录单击前真实滚动原点。TextKit完成选区与编辑行样式切换后恢复该原点，并在下一主线程轮次复核一次，阻止系统因进入源码编辑态而把视野大幅拉走。
- 鼠标拖拽形成选区时不锁定视野，保留系统原生的边缘自动滚动。键盘移动使焦点真正越出可见区域时同样允许系统追踪焦点。
- 删除进入编辑后重复调用`makeFirstResponder`的操作；鼠标系统事件已经建立第一响应者和真实点击选区，无需再次触发插入点滚动。
- 回车后的滚动由“无条件执行两次”改为检查TextKit真实插入点矩形。插入点已在可见区域内时完全不滚动；确实越界时才滚动，并在下一轮按真实几何决定是否需要补一次。

## 验证

- Mac Debug与iPhone Simulator Debug构建通过；`git diff --check`通过。

# 2-36 NodeMarkdown新管线字符事务提速

- 版本号更新为`V1.2608112330`。
- 根据`【诊断·35】`实测，普通字符输入不再逐键拆分全文并校验全部Node元数据；只有回车、跨行删除等真正改变行数的结构事务才执行完整数据校验。
- `updateNSView`收到本地输入引起的迟到回写时，不再比较、重建或重刷全文布局。外部同步令牌、文档载入和结构变化仍保留完整验证。
- 未打开焦点位置调试显示时，不再把每次光标移动写回SwiftUI状态，切断“每打一字就刷新父页面”的反馈链。
- 连续输入仍在同一Node时，不再调用整篇布局变化探测；只在真实跨Node时切换编辑行。中文普通定字也不再强制重画当前行和下一行，格式符或结构变化时才刷新样式。
- 第二轮实测发现英文普通输入仍产生每键两次局部富文本样式事务。字符事务现彻底禁止行样式刷新；编辑行保持源码输入态，回车、跨行、层级变化和离行时才重新渲染。重复的输入字体、颜色和段落属性设置也合并为写入前一次，只有真实跨Node才重新计算。
- `【诊断·35】`新增局部样式来源计数，可区分当前行刷新、编辑行切换、搜索/编辑状态同步和结构变化；若仍有样式写入，下一份记录可以直接定位调用入口。
- 单Node更新的Debug不变量检查缩小到当前Node及UUID索引；轻校验只核对当前行、相邻接缝、文末和选区。完整全文检查仍用于载入、保存前快照和结构事务。
- 行坐标增量更新去掉了平移后第二次全表连续性扫描，只验证改动接缝和真实文末。现有绝对字符范围仍需在行长度变化时平移后续范围；后续若继续追求严格`O(log N)`，应单独引入前缀长度索引，不能在本轮用猜测坐标替代。

## 验证

- Mac Debug与iPhone Simulator Debug构建通过。

# 2-43 NodeMarkdown新管线回车局部结构事务

## 问题

- 大文档回车会卡顿约一至两秒，新行建立后视野从当前Node跳到错误的超大Y坐标。
- 原流程在结构修改后重建全文行布局、全文替换Node状态、深度校验，并在TextKit2布局尚未稳定时连续两次调用`scrollRangeToVisible`。
- 新行样式尚未安装便写入选区，TextKit2会先按错误字体计算行高，再因样式变化重新布局。

## 改动

- 新增DocumentState单Node拆分事务：原UUID保留在前半段，新UUID只属于后半段，非H3来源不会误继承。
- 回车只替换当前Node文字范围，只重建原行、新行和相邻接缝；后续行只应用已知换行偏移，不重新解析正文或重算样式。
- 新行先安装继承层级对应的字体、颜色、段落与输入属性，再设置选区并建立活动Node会话。
- 删除回车后的同步和异步双重`scrollRangeToVisible`，结构事务期间拦截AppKit自动追踪选区并恢复真实原视野。
- 回车热路径不再执行全文深度校验或`documentState.replace`；局部接缝契约失败时才进入有诊断记录的安全完整布局回退。
- 结构快照延迟到命令返回后的主循环并合并重复发布，避免SwiftUI在AppKit命令处理中重入TextKit。
- 新增Node拆分回归测试，覆盖内容、层级、UUID保留和后续UUID索引。
- 版本更新为`V1.2608122242`。

## 验证

- `git diff --check`通过。
- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- 保留`【诊断·35】`，用于同一份3628 Node大文档复测改造后的逐键路径和耗时。

# 2-35 NodeMarkdown新管线大文档输入诊断

- 版本号更新为`V1.2608112240`。
- 新增`【诊断·35】`连续输入性能诊断。诊断只在Debug启用，高频事件仅在内存计时，停止输入约1.2秒后才向Console和`ATeaching诊断记录.MD`追加一条汇总。
- 每段输入记录文档UTF-16长度、Node数量、编辑位置、直接输入与输入法组合次数，并分别统计整篇字符串投影、Node元数据投影、单行坐标更新、后续Node坐标平移、Node发布、轻校验、局部/全文样式、`updateNSView`、Coordinator同步和实际绘制。
- 代码初查确认普通字符路径仍有随全文增长的工作：`projectedMetadata`逐键校验全文和全部Node；行坐标增量更新复制整张布局表、平移后续Node并再次全表校验；Debug的Node不变量检查逐键扫描全部UUID。此次只建立测量，不提前修改这些数据契约。
- 诊断不读取或记录正文内容，不修改正文、Node、焦点、选区、布局或视野；Release下计时入口返回零且不建立诊断会话。

## 验证

- Mac Debug与iPhone Simulator Debug构建通过；等待大文档开头、中间、末尾的英文及拼音输入样本后制定最终重构顺序。

# 2-34 学生标识与NodeMarkdown新管线定位

- 版本号更新为`V1.2608112225`。
- 首页学生容器右键图标扩充为20个差异明显的SF Symbols，包含人物、天体、自然、学习、艺术和兴趣类；菜单显示中文名称，不再显示系统图标代码。
- 学生颜色扩充为经典12色：红、橙、黄、绿、薄荷、青、青蓝、蓝、靛蓝、紫、粉、棕。右键菜单使用真实色点预览，旧的颜色存储值完全兼容。
- NodeMarkdown新管线新增独立的定位请求序号。搜索结果和目录L1/L2/L3每次点击都会发出一次明确定位，不再依赖“目标行号是否变化”。
- 定位使用TextKit2已建立的真实Node字符范围调用系统滚动，只让目标行进入可见区，不改选区、焦点或编辑状态。
- 新增定位回归检查：同一Node用不同请求序号可重复定位，已处理序号不重复执行，越界行不执行。

## 验证

- Mac Debug与iPhone Simulator Debug构建通过。

# 2-33 包删除、随堂插入与跨教案移动

- 版本号更新为`V1.2608112205`。
- 上课中删除H3包，或删除包含多个H3的上层包时，会按`SourceFile + SourceID`找到学生的教案完成清单，将已经不在随堂笔记中的非空H3退回未完成。同一母本包仍在笔记中时不误回退。
- 随堂插入锚点只使用真实编辑会话的Node UUID。ESC或失焦后固定插到文末；有焦点时，H1/H2后直接插入，H3包内任意行后放到该完整H3包的末尾。
- 点击随堂、完成插入及外部文档回写均保留当前可视Node和它的屏幕内偏移，不为新内容主动滚动。
- 教案编辑器的右键剪切包可在另一个教案窗口粘贴。包内所有UUID保持不变；跨章教案移动后，所有命中这些H3 UUID的随堂笔记统一改为新`SourceFile`和标准`SourceID`。目标写入或笔记重链失败时不丢剪切包，可继续粘贴或回退。
- 教案编辑器开放目录拖动排序。上课目录拖到底部热区时按真实行框快速向下滚动；L1展开最深只显示到L3。
- 新增`TeachingCourseInsertPlacementPolicy`回归检查，覆盖失焦文末、H2节点后、H3完整包后和损坏层级回退文末。
- Debug跟踪使用`【诊断·33】`，同时写入Console与`ATeaching诊断记录.MD`，只记录包事务和插入锚点，不参与焦点、布局或滚动。

## 验证

- Mac Debug与 iPhone Simulator Debug构建均通过，数学字体资源已复核进入两端App包。
- `git diff --check`通过；NodeMarkdown启动回归新增随堂插入结构断言。

# 2-32 NodeMarkdown新管线空行首字与焦点修复

- 版本号更新为`V1.2608112127`。
- 根据`【诊断·31】`确认，输入法定字后TextKit焦点已经正确位于新文字末尾；随后普通SwiftUI回写错误使用空行时期保存的`offset=0`，主动把焦点改回当前Node行首。
- 焦点锚点改为一次性结构事务数据。回车、Tab、撤销和真正的外部正文替换仍可按Node UUID与Node内偏移恢复；正文相同或保留本地正文的普通回写只认可TextKit当前选区，并清除旧锚点。
- 空Node改用`contentRange.length == 0`判断。行范围即使包含换行符也能进入真实空Node布局，不再误判成有内容行。
- 左侧编号不再按照可能随空行、拼音和公式改变的整行矩形中线定位。所有Node统一读取TextKit实测文字基线，再结合当前层级真实字体的`ascender`与`descender`计算视觉中线。
- 增加编号视觉中线回归检查，覆盖实测基线和无可用基线时的系统布局回退。

## 验证

- Mac Debug构建通过，构建缓存位于`/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/ATeaching`。
- `git diff --check`通过。

# 2-31 NodeMarkdown新管线空行首字诊断第二轮

- 版本号更新为`V1.2608112041`。
- 确认诊断文件的真实物理位置是`/Users/Witten/Library/Containers/Han.ATeaching/Data/Library/Application Support/ATeaching/诊断/ATeaching诊断记录.MD`，以后直接读取该文件，不再复制Console。
- 每次建立记录后马上反读校验；每条追加都独立打开并关闭文件，既保证随时可读，又不使用会改变输入时序的强制磁盘同步。
- 取消80条硬上限，改为合并完全相同的输入法事件；正式提交后继续记录下一轮、50毫秒、250毫秒和1秒状态。
- 期望焦点改为按最终上屏文字的真实UTF-16长度更新，不再把第一个拼音字母的位置当成永久期望值。
- 新增官方行表与TextView临时行表的对照、焦点附近源文/显示文本片段、SwiftUI同步分支和本地修改版本。
- 所有会主动写选区的主要入口记录改前值、请求值、改后值及原因，用于直接找到把焦点送回文首的写入者。
- 本轮仍未改动空行、编号、基线或焦点的生产逻辑。

## 验证

- Mac Debug构建通过，构建缓存位于`/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/ATeaching/DerivedData-2-31-diagnostic-v2`。
- `git diff --check`通过。

# 2-31 NodeMarkdown新管线空行首字诊断

- 版本号更新为`V1.2608112020`。
- 本轮只增加诊断，没有修改空行、编号、基线、焦点或保存逻辑。
- 当空Node第一次收到非换行文字时，自动开启`【诊断·31】`事务；普通非空Node输入不会输出这组记录。
- 同时记录Console与Markdown文件，Markdown位置以上方“真实物理位置”为准。同一轮首次记录会替换旧的31号小节，避免长期无限增加。
- 记录首字写入前后的真实选区、预期光标位置、输入法标记范围、TextStorage与源码长度、Node UUID、行号、层级和真实字符范围。
- 覆盖系统输入、输入法临时态和正式提交、正文发布、行表更新、单行及全文样式事务、选区通知、下一轮主队列与120毫秒后状态。
- 从实际绘制阶段记录文字行框、编号绘制框、文字基线及真实字体度量；诊断不主动强制生成布局。
- 当选区从首字后跳到文首0位置时，日志明确标记`跳到文首=true`，可结合紧邻阶段确定是哪条流程触发。
- 每次事务最多记录80个关键事件；旧的通用诊断继续关闭，避免无关Console内容淹没问题。

## 验证

- Mac Debug构建通过。
- 构建缓存位于`/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/ATeaching/DerivedData-2-31-diagnostic`。
- 尚未取得用户现场复现数据，因此本轮没有修复问题。

# 2-30 NodeMarkdown新管线公式、文字与编号纵向对齐

- 版本号更新为`V1.2608111907`。
- `【测试·29】`最终确认：同一个55点幼圆Node，普通状态与源码状态的文字基线距行顶均为`49`点，公式渲染后变为`125`点；文字下沉由公式附件的上下边界参与TextKit2排版造成，与Node层级和字体设置无关。
- 删除以x高度和SwiftMath数学轴间接推算附件位置的旧规则。现在使用当前Node字体真实上升、下降和公式图片真实高度，直接让两者的视觉中心重合，不写任何固定偏移。
- 真实样例中134.882点高公式的新附件范围为`-49.821...85.061`，中心严格等于55点幼圆字体中心；较高、较矮和不同字体公式都使用同一测量公式。
- TextKit2继续按照公式真实上下边界保留完整行高，分式、根式不会与上下Node重叠；普通文字自身不加偏移，因此公式位于自动换行的第二视觉行时也不会抬高其他行文字。
- 左侧编号不再按整个公式行框居中。只有第一视觉行包含公式时，编号才对齐该行公式与文字的共同视觉中心；公式落在后续换行时，首行编号保持原位置。
- 渲染与源码切换时明确清零基线偏移，防止旧显示属性残留。
- 删除全部`【测试·29】`代码和Console输出，增加公式图片与字体视觉中心严格相等的回归检查。
- 公式缓存键加入基础字体名称及真实上升、下降度量，避免相同字号的不同字体错误共用旧公式位置。

## 验证

- Mac Debug构建通过。
- 构建缓存位于`/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/ATeaching/DerivedData-2-30`。

# 2-29 NodeMarkdown新管线公式纵向位置测量

- 版本号更新为`V1.2608111824`。
- 对比确认普通Markdown与NodeMarkdown新管线没有使用同一套公式纵向定位：普通Markdown附件使用原始`y=0`，新管线根据文字x高度、SwiftMath数学上升下降和数学轴计算附件偏移，并另外扩展公式行高度。
- 仅增加`【测试·29】`测量，不改变公式偏移、文字位置、行高或左侧编号位置。
- 公式生成时记录真实字体上升、下降、x高度、SwiftMath数学上升下降、图片尺寸、目标数学轴、图片数学轴、附件偏移及最终占用高度。
- TextKit2布局时记录它真正采用的附件矩形、行框、字形原点、文字基线、段落最小行高和编号矩形；普通Markdown同步记录公式附件实际采用的矩形，可直接与新管线对照。
- 相同几何只输出一次，避免滚动和重复绘制持续刷Console。
- 首轮数据确认公式行文字基线距行顶达到`125`点，而所用55点幼圆字体的上升仅`45.12`点，文字确实被高公式行框压到下部，不是截图错觉。
- `【测试·29】`继续增加所有可见Node的真实文字基线、基线距行顶、行框、编号绘制矩形和编号字体度量。这样可以直接拿下一条无公式Node与公式Node比较，不再只分析公式行自身。
- 编号是编辑器单独绘制的图形，不属于TextKit文字布局，系统没有可读取的编号基线；诊断只报告它的真实绘制矩形和字体度量，不制造推测值。

## 验证

- Mac Debug构建通过。
- 构建缓存位于`/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/ATeaching/DerivedData-2-29`。
- 本次为测量阶段，尚未修改公式纵向位置。

# 2-28 NodeMarkdown新管线快捷输入后续Node错位修复

- 版本号更新为`V1.2608111731`。
- `【诊断·28】`已经确定根因：输入`\\fr`并替换为`\\frac{}{}`后，TextStorage和现有行表已经包含这次长度变化，旧代码又把字符长度差应用到后续行，造成后续Node被重复向后移动。
- 复现数据中，背景条Node的真实起点是`106`，旧更新流程却再次把它推到`113`；样式系统随后按这个错误范围工作，才把上一Node的背景、字体和颜色画到下一Node文字上。
- 删除依赖事件字符长度差移动后续行的状态。当前行改动后，以重建当前行的真实结束位置和下一行的真实开始位置计算唯一偏移；已经移动过就不再移动，尚未移动才统一修正后续行。
- 行表更新后必须连续、无重叠、无空洞并完整覆盖正文，否则拒绝局部结果并进入现有完整重建入口。
- 增加回归检查，同时覆盖“后续行尚未移动”和“输入法或TextKit2已经移动”两种状态，保证同一次改动只计算一次。
- 结论成立后删除全部`【诊断·28】`输出；已经停用的旧诊断仍不输出，Console恢复安静。

## 验证

- Mac Debug构建通过。
- 构建缓存位于`/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/ATeaching/DerivedData-2-28`。
- `git diff --check`通过。

# SwiftMath公式资源包崩溃修复

- 版本号更新为`V1.2608110931`。
- 修复运行公式渲染时`unable to find bundle named SwiftMath_SwiftMath`导致的立即崩溃。
- 原因是SwiftMath依赖SwiftPM自动生成的`Bundle.module`访问器；增量构建一旦没有复制包装资源包，该访问器会直接终止进程，业务代码无法捕获。
- App资源阶段现在明确复制`mathFonts.bundle`，不再只依赖SwiftPM隐式复制。
- SwiftMath增加真实资源定位器，优先读取App内直接资源，也兼容标准`SwiftMath_SwiftMath.bundle`包装资源；公式代码不再调用会直接崩溃的`Bundle.module`。
- 独立DerivedData全新构建确认Mac App和iPhone App均同时包含直接字体资源与SwiftPM包装资源。

## 验证

- Mac Debug全新构建通过。
- iPhone Simulator Debug全新构建通过。
- SwiftMath测试执行550项，跳过2项，失败0项。
- `git diff --check`通过。

# 2-26 NodeMarkdown新管线保存与脏包漏收修复

- 版本号更新为`V1.2608110817`。
- `【诊断·26】`确认问题发生在活动Node会话建立过晚：TextStorage已经写入新文字，外层文档仍是旧文字，但新文字被误当成会话基线，导致`dirty=false`。
- 所有原生文字修改与中文输入法确认在改动正文前，先按实际修改字符范围锁定Node，并从修改前的Node文档建立基线，不再依赖可能延迟到达的选区通知。
- 活动会话的`dirty`只负责即时显示“可保存”状态，不再决定是否提交。切换Node、ESC、失焦和保存时始终将当前Node草稿按UUID交给外层复核。
- 外层仍只在Node真实变化时写文档、标记未保存并收集所属H3包；未变化的Node提交是常量级比较，不触发全文解析或包摘要计算。
- 保留`【诊断·26】`，增加“输入前锁定”记录，用于确认首个字符之前已经建立正确Node基线。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- `git diff --check`通过。

# 2-26 NodeMarkdown新管线脏包全链路诊断

- 版本号更新为`V1.2608110757`。
- 本次不修改脏包判定规则，先建立可以证明问题位于哪一层的诊断链。
- 删除`2-19`的滚动、缩放、背景条、高亮和编号绘制诊断，同时移除其视口通知观察器。其他旧诊断入口保持空操作，Console不再输出旧`diagnostic`内容。
- 新诊断统一以`【诊断·26】`开头，只跟踪脏包相关数据。
- Node会话层记录进入时基线、首次变脏、离行或保存的提交原因、是否向外层提交，以及保存后重建的基线。
- 外层文档层记录Node UUID是否找到、旧新level、文字、Source完整性、所属H3 UUID，以及追踪后的脏包和新包名单。
- 包追踪层为每次事务编号，记录改动前摘要、改动后摘要、会话基线摘要、来源完整性和最终脏包判定。如果基线丢失并被当前包重建，Console会明确显示。
- 诊断中UUID只显示前8位，摘要只显示末10位，文字只显示前24个字和总长度，便于在同一次复现中连接事件而不大量刷屏。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- `git diff --check`通过。

# 2-25 NodeMarkdown新管线Node提交、保存与H3包收集收束

- 版本号更新为`V1.2608110720`。
- 取消 #2-23 建立的“打字时实时收集活动草稿”机制。普通输入只更新当前Node草稿、TextStorage和未保存状态，不更新脏包或新包名单。
- 进入Node时保存UUID、level、content、SourceID和SourceFile的完整基线。切换Node、ESC失焦、文本视图失焦、Command+S、关闭、同步和导出前统一提交活动Node。
- 提交时按Node UUID对比进入时与离开时的层级、内容和来源信息。发生变化且Node属于已连接母本的H3包时，通过包摘要将整个H3包收集为脏包；H1和H2修改仍保存，但不会产生脏包。
- 新包只在Node会话边界收集。新建或调整为H3的Node在Tab、回车等结构事务中不会提前入队；离开或保存时它仍是H3，且SourceID和SourceFile为空，才进入新包名单。改成非H3后会从新包名单移除。
- 修复Command+S被`NSTextView`第一响应者链吞掉的风险。新管线文本视图直接识别Command+S，调用与工具栏保存相同的唯一落盘入口。
- 修复保存后继续在同一Node打字时，第一个字被当成新基线而漏掉的问题。保存提交后在同一Node上立即建立新的干净基线；只有真正失焦或切换Node才结束会话。
- 新增回归检查：已连接H3子Node在提交后必须进入脏包；H1修改不产生包；新H3结构事务中不收集，会话提交后收集，改成非H3后移除。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- `git diff --check`通过。

# 2-24 NodeMarkdown新管线拼音使用当前Node样式

- 版本号更新为`V1.2608110702`。
- 修复拼音组合文字使用输入法自带的蓝色大字号，而不是当前Node字体、字号和颜色的问题。
- 新管线为当前Node保存一份不受输入法改写的标准输入样式，数据来自该Node真实层级对应的文档设置。
- `setMarkedText`调用AppKit之前先给拼音应用标准样式；AppKit和输入法完成本次组合文字写入后，再对TextStorage中真实的marked range复写字体、字号、颜色和段落样式。
- 只覆盖会影响行高和外观的受控属性，保留输入法的下划线、分词和候选标记。拼音的行高不再因默认大字号变化。
- 新增回归检查：即使输入法在首次写入后强制改成蓝色、48号粗体，也必须恢复当前Node的真实字体、颜色和段落样式，且不丢失候选下划线。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- `git diff --check`通过。

# 2-23 NodeMarkdown新管线包收集与新行焦点修复

- 版本号更新为`V1.2608110653`。
- 修复新管线脏包收集失效。当前Node草稿第一次变化时，直接按Node UUID找到所属H3并标记；不解析、不复制整篇文档。草稿恢复原文时撤销标记，离行正式提交后再用真实文档复核。
- 修复新包收集失效。无SourceID和SourceFile的H3在输入第一个字时，活动草稿会按UUID保留在新包名单中，不会被尚未离行的外层文档误删。
- 修复新行第一次打字后焦点跳到文字前面。回车、Tab等结构操作现在先提交旧Node，再把新正文、UUID和层级安装到Node文档，最后才按新Node UUID恢复焦点。
- 新管线结构快照改为当前事件内立即写入外层文档，避免防抖期间新Node UUID尚不存在。普通字符输入仍只更新当前Node，不触发全文结构处理。
- 新增回归检查：已入库H3的子Node变化必须进入脏包；新H3仅在活动草稿中有标题时也必须进入新包并显示真实标题。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- `git diff --check`通过。

# 2-22 NodeMarkdown新管线拼音字号稳定与正文颜色分层

- 版本号更新为`V1.2608102311`。
- 修复中文输入法的临时拼音使用输入法默认大字号，导致当前Node行高增大和页面上下跳动的问题。每次`setMarkedText`现在把临时拼音的字体、字号、颜色和段落样式固定为当前Node的真实设置，同时保留输入法自己的下划线、分词和候选标记。
- 修复正文3（L9）和正文4（L10）颜色向后错一层的问题。换行符仍保留上一Node的段落排版，但字体、颜色和下划线继承属性明确使用下一Node的样式，新输入不再从上一行继承颜色。
- 全文样式和单行离行刷新都重新写入真实的Node边界属性，不修改Node的level、UUID、Content或Source信息。
- 新增回归检查：拼音必须使用当前Node排版且保留输入法标记；L9必须读取正文3，L10必须读取正文4。
- 修正颜色回归检查本身的错误：不再直接判断`NSColor(SwiftUI.Color)`与AppKit预设`NSColor`对象是否相等，改为先验证完整层级样式，再转到同一RGB色彩空间比较实际色值，避免视觉同色却因内部表示不同触发断言。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- `git diff --check`通过。

# 2-21 NodeMarkdown新管线拼音装饰稳定与多行背景条

- 版本号更新为`V1.2608102257`。
- 修复中文输入法拼音候选期间，当前Node后面的编号和背景条各自向上一行移动、定字后才恢复的问题。输入法开始时保存真实Node布局，拼音临时进入TextStorage后只根据本次真实替换范围和实时字符长度差生成临时绘制坐标；当前Node扩大或缩小，后续Node只平移字符地址，level、样式和Node身份不变。
- 输入法临时坐标不写入正式Node、源码快照或外部绑定。中文定字后立即由正式Node事务接管；事务失败仍恢复开始前的有效Node快照。
- 修复有背景条的Node自动换行后，背景条始终只有一行高的问题。背景条不再只读取第一视觉行，而是合并TextKit2为该Node实际排出的全部`NSTextLineFragment`几何，高度随实际换行数量增长。
- 编号仍以第一视觉行的真实中轴线定位，不因多行背景条改变自身位置。
- 新增拼音临时布局回归检查，确认后续Node只改变字符地址，不改变其层级和样式。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- `git diff --check`通过。

# 2-20 NodeMarkdown新管线全面取消猜测并使用真实数据

- 版本号更新为`V1.2608102246`。
- NodeMarkdown新管线建立严格文档入口：正文行数必须和Node数量一致，每个Node必须具有真实且不重复的UUID、1至12级真实层级；SourceID与SourceFile必须成对存在，并且只能属于H3节点。数据不合法时拒绝本次更新并保留最后一份有效文档，不再补默认Node、默认层级或临时UUID。
- 行地址完全由正文真实字符范围生成，并要求从文首到文尾连续覆盖。行查找统一使用有序范围二分查询，不再从头遍历，也不再用上一行、最近行或正文七级猜测当前Node。
- 焦点只通过Node UUID和Node内真实字符位置恢复；Node不存在或位置已失效时不移动焦点。视野锚点只使用TextKit2给出的真实viewport、Node UUID和实际行矩形，不再从编号空白区反查、向后试探若干行或强行滚到活动行。
- 样式、编号、背景条、高亮和附件只应用到通过严格校验的真实Node范围。删除了先给全文套正文七级再覆盖的基础样式做法，防止一处数据错误扩散到整篇文档。
- 普通输入、中文输入法、快捷输入、回车、Tab、删除和图片操作都先根据真实事务计算修改后的正文、Node和选择范围，校验成功后才一次提交。失败时恢复事务开始前的Node快照，不再从已经渲染过公式或图片的显示文本反推源码。
- 稳定性检查由崩溃断言改为明确拒绝并输出诊断。正文长度、Node数量、UUID、层级、来源、行范围或选区只要不一致，本次事务不会污染现有文档。
- 新增回归检查，覆盖重复UUID、行数不一致、非法UUID、非法层级、不完整来源以及非H3节点带来源等错误数据。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- `git diff --check`通过。

# 2-19 NodeMarkdown新管线滚动装饰诊断与快捷输入一致性

- 版本号更新为`V1.2608102212`。
- 不修改背景条、高亮和编号的绘制行为，完整删除上一轮专项诊断，改为只输出`【诊断-19】`。
- 新诊断监听滚动和视口尺寸变化，记录事件前后的`draw`序号、TextKit2 viewport字符与行范围，并比较viewport行、生产候选行、有几何的行以及三种装饰的期望/实画数量。
- 滚动或尺寸变化发生80毫秒后复核是否出现新的`draw`，用于定位“装饰缺失，轻微滚动后恢复”的时序原因。诊断只旁观已有状态，不主动布局或重绘。
- 修复输入第四个`=`触发快捷输入后，`TextKit2 row ranges are not contiguous`断言崩溃。普通按键和随后发生的快捷替换现在合并计算净字符长度变化，当前行重建与后续行平移使用同一最终结果。
- 快捷输入若包含换行，也会明确转入结构修改路径，不把结构替换伪装成普通字符输入。
- 根据`【诊断-19】`确认首屏装饰缺失的根因：页面实际显示第0至8行时，绘制器从左侧编号空白反查成第28至31行，因此没有绘制顶部Node的背景条、高亮和编号；轻微滚动后命中恢复，所以画面随之恢复。
- 背景条、高亮和编号现在共用TextKit2提供的真实viewport字符范围选择Node，并在上下各保留两行缓冲。`dirtyRect`只负责最终几何裁剪，不再负责猜测当前是哪几行。
- TextKit2尚未建立viewport的短暂阶段保留坐标兜底，但命中点移动到正文列内部，不再使用没有文字的编号空白区。该修复不遍历全文、不修改Node、不改变焦点和滚动位置。

## 验证

- Mac Debug构建通过。

# 2-17 NodeMarkdown新管线输入法事务与首屏装饰修复

- 版本号更新为`V1.2608101724`。
- 修复百度输入法等中文输入法处于拼音候选状态时，临时拼音进入正式源码快照，造成`TextKit2 storage/source length mismatch`断言崩溃的问题。
- 新管线为输入法增加完整事务边界：开始拼音后冻结Node数据、层级、附件、样式和外部绑定；候选文字正式提交后，只结算本次替换范围一次。
- 保留严格的一致性断言。输入法临时文字存在时暂缓稳定性校验，正式提交后立即重新校验正文长度、行范围和选择范围，不用放宽断言掩盖错误。
- 中文定字按普通Node字符修改处理，只更新当前Node并平移后续字符地址；不因一次定字重建整篇文档。
- 修复新管线开头几行背景条和行内高亮首帧不显示的问题。TextKit2取得真实视口尺寸并完成可见区布局后，明确重画一次当前可见装饰，不依赖滚动触发补画。
- 新增`【诊断】`输入法事务开始、临时变化忽略和正式提交日志，便于继续核对第三方输入法行为。

## 验证

- Mac Debug构建通过。

# 2-16 NodeMarkdown旧管线视野稳定与Node局部输入升级

- 版本号更新为`V1.2608101250`。
- 修复拼音候选后按空格定字时视野上下跳动。输入法组合期和定字事务期间禁止`NSTextView`因选区变化自动滚动，定字完成后恢复系统默认行为。
- 去掉输入法定字后的全文行坐标重建。活动Node仅记录自身实时范围和一个字符长度差，后续Node的有效位置用稳定索引与长度差二分查询，连续英文输入、拼音候选和中文定字都不遍历后文。
- `Command+S`只提交活动Node草稿并落盘，删除保存后连续三次强行写回`NSClipView`滚动坐标的逻辑。
- 活动行、离开行和文字变化的局部样式刷新不再用“记住坐标后强行恢复”补偿，只在局部事务内禁止选区自动滚动。
- 保留结构边界：回车增行、跨行删除、外部整篇替换仍可重建行索引，普通Node字符修改不得进入该路径。

## 验证

- Mac Debug构建通过。

# TextKit2视图更新期状态回写警告清理

- 版本号更新为`V1.2608100807`。
- 修复`NodeMarkdownEditorView`中焦点位置、活动行、脏包列表、新包列表、选择集合和待更新计数触发的`Modifying state during view update`警告。
- 六处警告来自同一条调用链：AppKit编辑器在SwiftUI执行`updateNSView`期间同步回报焦点或活动行，外层立即修改`@State`；活动行处理又在同一调用栈内刷新四项包状态。
- 新旧TextKit编辑器的活动行回报以及TextKit2焦点回报统一经过编辑器状态队列，在本轮SwiftUI视图更新返回后执行。
- 脏包、新包、选择集合和计数仍作为一个同步事务一起更新，没有把四项状态拆成各自异步任务，避免界面短暂显示不同版本的数据。

## 验证

- Mac Debug构建通过。

# 2-15 TextKit2样式所有权收口与混合渲染修复

- 版本号更新为`V1.2608100654`。
- 根据完整诊断确认，1503个UTF-16字符和76行Node元数据均正确载入，层级样本也正确；错误不在Node层级数据，而在正文样式安装完成后被再次覆盖。
- 根因是每次SwiftUI调用`updateNSView`时，同步入口都会执行`NSTextView.font`和`textColor`赋值。这两个属性是全文编辑命令，会把已经按Node层级安装的字体、字号、颜色及公式透明源码属性重新改成正文一级；附件本身没有同时移除，最终造成层级正文一级化、背景条与高亮状态不稳定、公式附件后继续显示源码。
- 删除文档同步阶段的全文基础样式覆盖。基础字体和颜色只允许在空TextView创建、正文尚未安装时设置一次；正文载入以后，只有逐Node样式事务可以修改TextStorage属性。
- 活动行和焦点状态改为在`updateNSView`结束后异步回报，并丢弃过期回报，消除`Modifying state during view update`导致的未定义重复更新。
- 保留编辑切行只刷新离开行和进入行的边界；相邻Node不重装样式，公式透明源码的0.001pt宽度规则继续保留。

## 验证

- Mac Debug构建通过。

# 2-14 TextKit2编辑切行装饰稳定与公式真实宽度

- 版本号更新为`V1.2608092316`。
- 修复点击某行进入源码编辑状态后，上下未编辑行的背景条和行内高亮消失、必须滚动才恢复的问题。
- 根因是编辑行切换原本把旧行、新行以及它们的上下邻行全部恢复附件源码并重新挂载样式。邻行状态没有改变却被纳入样式事务，TextKit2旧布局片段失效后，装饰绘制没有在同一轮完成。
- 编辑状态转换现在只允许刷新“离开的编辑行”和“进入的编辑行”，相邻Node完全不动。局部样式事务结束后先完成当前可见区布局，再统一重画背景条、高亮和编号，不再依赖滚动触发刷新。
- 修复渲染公式后方存在与公式源码长度接近的巨大空白。经AppKit实测，`NSFont.systemFont(ofSize: 0)`不会产生零号字体，而会返回13pt默认字体；原先透明公式源码因此仍按正常字号占宽。
- 公式附件后的源码改用0.001pt字体，仅用于保持源码字符位置与选择坐标一一对应。100个隐藏字符的测量宽度约0.06pt，实际可见宽度由渲染后的公式附件决定。
- 增加`【诊断】编辑行切换`、`【诊断】行样式事务完成`和`【诊断】公式附件安装`，分别报告实际刷新行、装饰重画区域、公式源码长度及附件测量尺寸。

## 验证

- Mac Debug构建通过。
- AppKit字体测量确认：请求`systemFont(0)`实际得到13pt；0.001pt等宽字体的100字符宽度约0.06pt。

# 2-13 TextKit2非空文档被输入法空标记阻断修复

- 版本号更新为`V1.2608092303`。
- 根据完整`【诊断】`日志确认：SwiftUI已经把53个字符和4行Node元数据送入`updateNSView`，TextView对象链、窗口尺寸和draw均正常，但唯一TextStorage始终为0；同步函数在写入正文前提前退出。
- 根因是AppKit在TextView成为第一响应者期间可能返回“位置有效、长度为0”的marked range。旧代码只检查位置，把这个空标记误判为正在进行拼音组合，因而首次文档同步和全文样式都被永久挡住。
- 在`NodeMarkdownTextKit2TextView`集中定义有效输入法组合态：marked range必须同时位置有效且长度大于0。同步、输入委托、全文样式和局部样式全部使用同一判断，不再各自解释输入法状态。
- 增加首次非空正文强制装载边界：当唯一TextStorage为空而外部正文非空时，正文安装优先于临时输入法状态，避免空编辑器无法自救。
- 同步入口现在记录marked range的真实位置和长度、有效组合态、内外正文长度及每个退出分支；所有输出继续统一以`【诊断】`开头。

## 验证

- Mac Debug构建通过。

# 2-13 TextKit2文本管理器收口与统一诊断

- 版本号更新为`V1.2608092259`。
- 新增独立的`NodeMarkdownTextKit2TextSystemController`，首次进入、教案切换和随堂笔记明确外部替换统一经过一个全文安装入口；视图工厂不再写正文，生命周期不再独立重建正文，Coordinator连接也不再隐式生成空布局。
- 全文安装顺序固定为：进入loading、写入唯一TextStorage、建立Node状态和UUID索引、接收规范化metadata、结束外部写入状态、重建行布局、应用样式、进入ready、执行一致性校验。
- `NodeMarkdownTextKit2TextView`继续由一个位置显式组装TextKit2五对象链。Mac的`NSTextView.init(usingTextLayoutManager:)`是便利初始化器，子类不能直接调用；因此保留显式组装，但每次诊断和Debug断言都会验证NSTextView实际编辑的Storage、ContentStorage、LayoutManager和Container就是业务持有的同一组对象。
- 新增`NodeMarkdownTextKit2Diagnostics`统一诊断器。本模块所有诊断输出都以`【诊断】`开头，可在Console直接筛选。
- 诊断覆盖：binding、源码快照和TextStorage长度；TextKit2对象身份；TextView、可见区、ClipView和Container尺寸；metadata、行布局和viewportRange；首字符字体颜色；首个布局片段frame；正文样本；前512字符透明文字、微小字体和附件数量；外部同步分支；前三次实际draw。
- 保留普通输入的轻量边界：统一全文安装入口只允许首次载入和明确外部替换调用，普通字符、拼音组合、滚动和局部样式刷新不得调用。

## 验证

- Mac Debug构建通过。

# 设置中心单列化与二级页面统一设计

- 版本号更新为`V1.2608092251`。
- Mac设置一级目录去掉双列自适应布局，固定使用宽度受控的单列上下结构，“设置”和“功能”两个分组依次排列。
- 新增`AppSettingsVisualComponents.swift`，统一设置页面骨架、30号页面标题、说明文字、圆角分组、导航行、分割线和状态提示。
- PDF设置、背景设置、备份管理、功能按键和Debug五个直接二级页全部移除裸`Form/List`外观，改用与一级目录相同的背景、圆角容器、字体层级和纵向间距。
- 备份快照改为圆角分组内的导航行；备份进度、取消、快照详情入口和原有任务逻辑保持不变。
- 功能按键的H3查重、洗教案、随堂笔记SSC修改和颜色整理使用统一图标、标题、说明与箭头样式。
- 页面最大内容宽度统一为820点；较窄窗口和iPhone自动缩到可用宽度，页面整体滚动，不再用固定高度卡住二级设置。

## 验证

- 实际启动Mac Debug版，检查一级设置单列布局和PDF二级页面圆角布局。
- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。

# 2-13 TextKit2新管线首帧空白修复

- 版本号更新为`V1.2608092245`。
- 修复上课随堂笔记和教案编辑进入TextKit2新管线后正文完整存在、可以输入，但已有内容完全不可见的问题。
- 根因是`NSViewRepresentable`创建阶段尚未获得SwiftUI分配的窗口尺寸，此时`NSScrollView.contentSize`接近零；首帧代码错误地用这个临时尺寸把`NSTextView`和`NSTextContainer`压缩到约1点宽高。
- 视图工厂不再用未完成布局的ScrollView尺寸覆盖TextView有效初始frame。视口模块统一负责几何尺寸：同步阶段保留720点安全宽度，下一轮主线程取得真实窗口宽度后再更新TextView与TextKit2容器，并且只布局可见区。
- 修复不改变正文、Node元数据、选择、保存或同步数据，只恢复显示链的有效几何尺寸。

## 验证

- Mac Debug构建通过。

# 2-13 NodeMarkdown TextKit2新管线八步整改

- 当次八步整改版本号为`V1.2608092228`，首帧显示修复后总版本更新为`V1.2608092245`。
- 第一步，明确新管线只有一份Node事实源。新增`NodeMarkdownTextKit2DocumentState`，完整保存UUID、level、content、SourceID和SourceFile，并用UUID索引定位Node；载入时修复重复身份，编辑时不再从显示文字反推层级。
- 第二步，建立修订号、可见区高度索引和独立渲染缓存。高度索引使用树状累计结构，可按纵坐标快速定位行；公式、图片和样式缓存以Node身份、修订号、宽度和样式版本区分。
- 第三步，首帧按`empty -> loading -> ready`推进。TextKit2容器先取得有效宽度，再接收正文和元数据，最后只准备系统可见区，避免首次载入空白和整篇强制布局。
- 第四步，新增单一活动Node会话。点击行后暂存该Node的完整身份和草稿；普通输入只改变活动草稿和当前行，不逐字向SwiftUI回送整篇文档；拼音组合期间不做结构提交。
- 第五步，结构操作统一走完整事务。回车、跨行删除、Tab、Shift+Tab和撤销才发布结构快照；有来源的H3根节点保持保护，回车在其后建立H4，而不是制造新的H3。
- 第六步，绘制范围缩到可见行。编号、背景条和附件绘制使用二分定位可见行，不再复制整篇富文本；图片缓存加入文件修改时间，避免路径相同但图片内容更新后仍显示旧图。
- 第七步，保存和同步先提交活动Node，再生成文档快照。保存控制器、单Node提交、脏状态和结构快照接到旧管线已经稳定的语义契约；活动草稿未提交时，迟到的外部状态不得覆盖本地输入。
- 第八步，把生命周期、视图工厂、协调器工厂和移动命令移入`ATeaching`源码目录；增加Node身份、H3保护、高度索引和五万行索引查找回归检查，并补充说明书、白皮书和双平台构建验证。
- 性能边界：普通字符输入不解析全文、不刷新SwiftUI全文、不重建公式图片、不强制整篇布局；只有结构操作、明确外部替换和最终保存需要生成完整快照。
- 平台边界：Mac使用原生TextKit2新管线；iPhone当前仍保留基础文本编辑兜底，本次保证数据模块和工程跨平台编译，但没有把兜底分支冒充为完整移动端TextKit2。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- Debug回归检查覆盖Node身份保留、重复UUID修复、有来源H3保护、高度索引更新和五万Node随机定位。

# NodeMarkdown回车保持后续Node身份与层级

- 版本号更新为`V1.2608092139`。
- 根据Console中的结构编辑日志梳理旧管线回车事务；布局递归警告不是层级数据变化的直接来源，实际风险是结构插入后继续使用旧行号缓存，以及SwiftUI短暂回送结构编辑前的旧行数元数据。
- 新增独立的行元数据投影器。回车只在源Node后生成一个新UUID；插入点之后所有旧Node的UUID、level、content、SourceID、SourceFile按原顺序逐项保留，不再根据邻行重新推断。
- 本地编辑期间拒绝旧行数元数据回灌；明确的外部文档同步仍由同步版本放行，不影响随堂插包等正式结构替换。
- 结构变化后统一作废按行号保存的层级、段落和行内标记缓存，再以真实Node元数据重建当前可见区；不扫描、不重写全文Node。
- 增加回归检查，覆盖不同层级和Source信息的后续Node，验证回车插入后完整后缀逐项不变。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。

# 2-12 洗教案扩大到随堂笔记身份清洗

- 版本号更新为`V1.2608091954`。
- “洗教案”在正式教案H3换UUID后，按旧`SourceFile+SourceID`同步更新所有随堂笔记对应H3的根UUID、SourceID和SourceFile。
- 来源两项均为空的随堂H3固定视为未入库新包，不再按同名标题或摘要猜成母本包。
- 建立跨全部正式母本和全部随堂新包的UUID保留表；新包根节点或子节点发生碰撞时只更换新包一侧。当前数据只读模拟确认“混合场2·能量”的公式子节点`E2A5A59C-FA36-42E7-8C19-F9F4A152F808`会被更换，H3根身份和正文保持不变。
- 随堂笔记内非H3重复UUID直接换新值；不能通过身份验证的随堂文件保持原样，且其临时UUID占用不会污染后续文件。
- 洗教案报告增加随堂H3重连、新包H3换UUID、新包子Node换UUID和普通Node换UUID的统计与逐行明细。
- 完成清单从洗教案边界中完全移除：不枚举、不重建、不写盘，继续由每次开课前的既有流程重新整理。
- 更新说明书和白皮书，明确母本H3身份迁移、新包身份隔离和报告规则。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。
- 只读扫描真实数据，确认“混合场2·能量”包内存在1个母本UUID碰撞，新规则命中该子节点且不改真实文件。

- 投屏服务生命周期修复：`ScreenCastService` 从 `ScreenCastView` 内部 `@StateObject` 提升到 `ContentView` 层级，切换标签不再中断投屏。工具栏显示"投屏中"/"接收中"状态指示。App 退出时自动停止所有投屏连接。

- 批注 canvas 生命周期修复：移除 `if isActive` 条件渲染。canvas 始终存在于视图树中，通过 `allowsHitTesting(isActive)` 控制交互。`isActive=false` 时不再销毁 NSView，批注笔画保留不丢失。

# 908 暂停上课中母本有更新机制

- 版本号更新为`V1.2608081956`。
- 上课期间"母本有更新"检测和提示暂停：`summarizePreviewState` 中差异统一归为脏包不再区分方向；`TeachingCoursePackageSyncEngine` 不再因 source.mtime > notebook.mtime 而跳过回传；两处 UI（编辑器更新页面+课程页）注释掉母本有更新显示。开课时母本→随堂覆盖逻辑保留不变。所有注释标注了恢复方法。

# 907 批注逻辑精细化：再点熄灭笔/橡皮进入编辑模式

- 版本号更新为`V1.2608081301`。
- 批注笔/橡皮按钮交互：激活状态下再点同一工具→关闭批注进入编辑模式（不清空批注）；异工具切换保留批注；清除按键和下课清空批注不变。

# 906 投屏功能初版

- 版本号更新为`V1.2608081144`。
- 新增投屏标签到底部导航栏：授课 → 档案 → **投屏** → 设置。图标 `rectangle.connected.to.line.below`，蓝色高亮。
- 投屏主页面包含两个入口按钮：「APP内投屏」和「网络投屏（待开发）」。
- **APP内投屏**（`ScreenCastService` + `ScreenCastView`）：
  - 投屏端：启动 TCP 监听（端口 55555），等待接收端连接。教室端打开上课时通过 `pushDocument` 推送随堂笔记文档（NodeMarkdownDocument JSON + 学生名）到所有连接的接收端。
  - 接收端：输入投屏端 IP 和端口连接，自动请求文档。接收后以只读 WKWebView 展示随堂笔记 HTML。
  - 接收端只有「批注」按钮，批注颜色非红色，每新学生自动分配新颜色（蓝/绿/橙/紫/青/粉等 10 色调色板）。
  - 接收端不能编辑、不能下课。仅可批注叠加。
- **状态监控**：主页面显示主控 IP、端口、连接状态、接收学生名。
- 新增文件：`ScreenCastView.swift`、`ScreenCastService.swift`、`ScreenCastReceiverView.swift`。修改 `GlassTabBar.swift`、`ContentView.swift`。

# 905 公式垂直定位改用字体真实指标

- 版本号更新为`V1.2608081018`。
- 修复公式偏高问题：`axisHeight` 由固定 `fontSize * 0.25` 改为基于实际字体指标——行内公式用 `baseFont.xHeight * 0.5`（对齐 x 字高中点），独立公式用 `(ascender + descender) / 2`（对齐字体中线）。`baseFont` 从 `applyFormulaAttachments` 传入 `renderedFormula`。

# 904 空行渲染高度修正

- 版本号更新为`V1.2608080956`。
- 修复新建空行（回车后）背景条和编号偏高的问题：空行因 `applyNodeMarkdownStyle` 跳过 `length==0` 的行，只拿到字体自然行高，比 `minimumLineHeight` 短。`firstLineRect` 现对空行补上段落样式的最小行高，背景条和编号从起始就落在正确位置，不再随打字"下降"。

# 903 排课交互重构：无锁编辑、标签重排、点击菜单、长按拖动

- 版本号更新为`V1.2608080934`。
- **移除排课时间锁**：`planningLockDate` 返回 `nil`，已完成课程在排课中可自由编辑；`promotePastPlanning` 不再自动调用，过去排课不再被迁入成课历史。
- **标签重排**：统计子页面顺序改为「排课 → 成课 → 机构 → 周表」，排课置顶。
- **点击行为**：课表格和列表行的单击/点击现在弹出操作菜单（popover），不再直接进入编辑页。菜单包含复制、导出图片、打开信息/课反、前后半小时调整、修改和删除。上下文菜单（右键）共用同一菜单内容。
- **拖动行为**：长按拖动在 Mac 和 iPhone 上均启用（`lessonDragGestureMask` 统一为 `.all`），iPhone 上不再被右键菜单抢占长按手势。

- **显示修正**：`displayRecords` 合并 `lessonRecords + records` 全部展示，已完成课程以暗淡样式呈现（`isRecordLocked` 视觉反馈）但正常可编辑。删除 insert/edit/move/shift/paste/delete 六处操作中的锁定拦截 guard。

# 902 主窗口不再随 Dock 点击自动弹出

- 版本号更新为`V1.2608080919`。
- `applicationShouldHandleReopen` 增加 `hasVisibleWindows` 检查：当前已有可见窗口（如上课窗口、编辑器）时，点击 Dock 不再弹出主窗口并打断工作。无可见窗口时仍正常恢复主窗口。

# 901 公式渲染改用 SwiftMath 真实 metrics

- 版本号更新为`V1.2608080834`。
- **SwiftMath 补丁**：`MTMathImage` 新增 `mathAscent` / `mathDescent` 公开属性，`asImage()` 调用后由显示列表回填。
- **公式 attachment 重建**：不再使用 `MTMathImage.asImage()` 的位图宽高猜测定位。改用 `mathAscent`、`mathDescent` 与字体数学轴高度 (`axisHeight ≈ fontSize * 0.25`) 计算基线偏移：
  - `baselineAscent = axisHeight + ascent`（公式顶部到文本基线的距离）
  - `baselineDescent = max(0, descent - axisHeight)`（公式底部到基线的距离）
  - `imageOriginY = axisHeight - axisFromBottom`（公式图像原点相对于文本基线的偏移）
- **字号**：移除行内公式 ≥14pt、独立公式 ≥18pt 的硬下限。行内公式使用正文字号，独立公式为正文的 1.2 倍。
- **行高**：不再用 `attachment.bounds.height` 粗暴拉行高。直接用 `formulaAscent` / `formulaDescent` 分别与 `font.ascender` / `-font.descender` 取最大值，精确计算所需行高。
- **留空**：隐藏的公式分隔符字符改用 `fontSize: 0` + `kern: 0`，不再产生残留空白。

# 900 批注按钮不再清空批注

- 版本号更新为`V1.2608080754`。
- 批注模式激活时，再次点击笔或橡皮不再清空全部批注并关闭批注状态；仅切换工具或保持当前工具。
- 批注的清除唯一入口保留为工具栏“清除”按键、下课和窗口关闭。

# 899 H3包时间戳与同步方向修复

- 版本号更新为`V1.2608080728`。
- 修复 `NodeMarkdownTextKitEditor.parse` 的回退逻辑：当 `canTrustRowMetadata == false` 时，删除位置匹配和末尾行层级的盲目赋值（策略3/4），仅保留 UUID 精确匹配，其余交还给 `decodeLine` 从文本前缀自行解析层级。
- 修复 `decodeLine` 在无元数据时保留完整行文本（不再 `dropFirst`），确保 `commonPrefixCount`/`commonSuffixCount` 在任意路径下均产出相同格式的 content 元组。
- 修复 `TeachingCourseWorkflowService.refreshNotebookBySources:2072` 的时间戳比较从 `>=` 改为 `>`。旧逻辑在 mtime 相等时让母本无条件覆盖随堂笔记，新逻辑相等时不动随堂笔记。
- 新增 `NodeMarkdownDocument.propagateChildMtimeToH3Roots()`：扫描每个 H3 包，当子节点 mtime 晚于根节点时，将根节点 mtime 同步为子节点中最新的值。此方法在每次解析完成和每次写盘前调用，确保编辑子行时 H3 根节点的时间戳正确反映包内变化。

## 背景

H3 包的同步方向（脏包回传母本 vs 母本覆盖随堂）通过比较根节点的 `mtimeCache` 判断。但改动 H4/H5/正文等子行时，根 H3 自身的 level 和 text 不变 → parse 的 `isChanged` 为 false → `mtimeCache` 不被更新 → 时间戳比较失效。这导致上课时改的子行非但不回传母本，反而被母本覆盖。

## 验证

- macOS + iOS 双平台 Debug 构建通过。
- `refreshNotebookBySources` 的 `>=` 已改为 `>`；`propagateChildMtimeToH3Roots` 在全部4处 `touchChangedH3Packages` 调用点之后执行。

# 898 构建产物统一迁移到 DerivedData

- 版本号更新为`V1.2608062236`。
- 查明"代码突然变大到一百多M"的原因：项目目录内的 `build/`（App 命令行构建产物 35M）与 `Vendor/SwiftMath/build`（本地 Swift 公式包构建产物 177M）被我此前用 `xcodebuild` 命令行验证构建时写进了项目目录。Xcode 图形界面的构建默认进 `~/Library/Developer/Xcode/DerivedData`，命令行不带 `-derivedDataPath` 时默认落在项目树内；SwiftMath 包又会在包目录内生成自己的产物。源码本身只有约 16M。
- 清理并删除项目内两个构建产物目录；实测结论：凡是写入项目树（又位于 iCloud 云盘）的构建中间件都会被 iCloud 当作资料同步放大。
- 统一构建位置：新增 `ATeaching.xcodeproj/project.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings`，把共享派生数据位置指到 `/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/ATeaching`，Xcode 图形界面的构建从此统一落在这里。
- 后续统一约定：每个 App 都在 `/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/` 下建立自己名字的文件夹作为暂存目录，构建产物、中间件、临时文件一律放那里（ATeaching → `Downloads/Temp/SoftWare/XcodeTemp/ATeaching`；排课 → `Downloads/Temp/SoftWare/XcodeTemp/排课`）。曾先指向 `~/Library/Developer/Xcode/DerivedData/ATeaching`，再改为 `Downloads/Temp/ATeaching`，最终定到 Software/XcodeTemp 下。
- 新增统一构建脚本 `Scripts/build.sh`：命令行构建固定加 `-derivedDataPath` 指向同一目录，并提供 `clean` 子命令清理统一构建目录；`./Scripts/build.sh`（macOS Debug）、`./Scripts/build.sh ios`（iOS Debug）。
- 曾尝试在 pbxproj 工程级设置 `SYMROOT` 统一输出，但会触发"legacy build locations"，导致 Swift 包依赖无法解析，已回退；正确做法是派生数据位置而非 SYMROOT。
- `.gitignore` 增加对临时目录的排除；仓库做 `git gc` 后 `.git` 从 54M 瘦身到 9.6M。

## 验证

- `./Scripts/build.sh`（macOS Debug）构建通过；构建后项目根目录与 `Vendor/SwiftMath` 目录内均无 `build` 残留。
- `./Scripts/build.sh ios`（iOS Debug）构建通过；项目目录保持干净。
- 统一暂存目录 `/Users/Witten/Downloads/Temp/SoftWare/XcodeTemp/ATeaching` 下生成全部产物。

# 896 批注与橡皮按钮切换与高亮

- 版本号更新为`V1.2608051658`。
- 批注按钮激活时高亮显示，关闭时恢复灰色；橡皮按钮同样激活高亮、关闭变灰。
- 批注激活且当前为笔时再次点击批注：清空全部批注并关闭批注状态。
- 批注激活且当前为橡皮时点击批注：保持激活并切换回手写笔。
- 批注激活且当前为橡皮时再次点击橡皮：清空全部批注并关闭批注状态。
- 批注激活且当前为笔时点击橡皮：保持激活并切换为橡皮擦除。
- 清除键在没有任何批注时禁用。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。

# 895 上课临时批注图层

- 版本号更新为`V1.2608051658`。
- 上课时随堂笔记左上角按钮组右端新增批注按钮群：批注（笔）、橡皮、清除。
- 批注激活后，编辑器上层出现临时批注图层：鼠标点击被图层拦截，不再进入正文编辑，但滚轮仍可正常滚动页面。
- 批注内容为红色笔画，绘制在上层临时图层，不写入正文、不参与保存。
- 批注笔画按文档坐标保存，页面滚动时随内容同步位移。
- 橡皮按整条笔画擦除：点击或拖过某条笔画即删除整条笔画。
- 清除键一次性清空全部批注；批注状态关闭时同样自动清空。
- 批注图层随批注状态销毁，属于临时内容，不持久化。
- 新增独立文件`ATeaching/TeachingAnnotationOverlay.swift`承载批注控制器、工具栏按钮群与原生覆盖画布；主编辑器文件只做状态挂载，不混入批注逻辑。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。

# 890 新包落盘复核与NodeMarkdown十二层样式直连

- 版本号更新为`V1.2608032245`。
- 查明“新包提示更新错误但实际已经更新”的根因：图片入库后，随堂笔记和母本分别保存相对于各自目录的图片链接；两段链接文字不同但指向同一图片，旧的落盘复核按原始文字比较，因而把成功写入误报为失败。
- 新包写入后的复核改为先把图片链接解析成标准化的真实文件地址，再比较H3包的层级、正文、图片、宽度与替代文字。只调整最终落盘复核，不放宽脏包与新包的真实内容判断。
- NodeMarkdown十二层样式增加角色到层级的双向唯一映射。设置页不再用整数和`level - 6`换算正文层级，直接绑定H1至H6、正文1至正文5、注释对应的完整样式对象。
- 颜色、字体、字号、粗体、下划线、背景条、段前段后和行距由同一个样式角色整体读写，正文3不会再写入或读取正文4。
- 增加回归检查：正文3与正文4使用完全不同的整组样式，验证层级映射和JSON落盘往返不串位；另验证随堂与母本使用不同相对图片链接时，落盘复核仍识别为同一新包。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。

# 879 NodeMarkdown旧管线活动Node事务重构

- 版本号更新为`V1.2607301125`。
- 找到当前Node末尾丢字和下一Node样式、内容被扰动的共同根因：旧管线分别用活动行号、全文字符差、编辑草稿和样式脏行维护同一次输入，行尾和输入法状态下这些事实可能不同步，下一Node会被误认为当前编辑范围的一部分。
- 新增活动Node事务作为编辑期间的唯一事实，同时持有当前Node的UUID、层级、正文、SourceID、SourceFile、稳定字符范围和实时字符范围；不再由多组松散状态互相推断。
- 普通字符输入后直接根据当前Node的真实换行边界更新实时范围。当前Node长度变化只负责平移后续Node坐标，不会把下一Node加入正文读取、样式刷新或提交范围。
- 普通输入只标记当前Node；相邻Node不再作为候选脏行。离行、回车、Tab、跨行删除等结构操作必须先通过统一出口读取并提交当前Node，再重建结构索引。
- 当前Node提交时只按UUID写回一个Node，并携带完整层级和Source信息；提交正文严格取当前Node实时范围，避免最后输入的字符被旧范围截掉。
- 有SourceID和SourceFile的H3继续保持保护状态。此类H3按回车不再拆分标题，也不建立同级H3，而是在完整H3后建立一个全新UUID的空H4并把焦点放到H4行首。
- 空Source的H3仍按普通Node处理，回车继续建立同级节点。
- 增加旧管线回归检查，固定“活动事务只拥有当前Node”和“受保护H3回车生成H4”两项规则。

## 验证

- Mac Debug构建通过。
- iPhone Simulator Debug构建通过。

# 883 行首公式原子换行与坐标修复

- 版本号更新为`V1.2607300939`。
- 找到公式靠近自动换行边界时漂移的根因：旧管线保留真实公式源码并将其透明压缩为公式宽度，但TextKit仍可能从这段透明源码内部断行；绘制层再读取整段跨行包围框，公式便会漂到下一行左侧并与正文重叠。
- 公式与图片的渲染源码范围现在是不可拆分的视觉单元，只允许在整个单元之前或之后换行，不允许从公式源码内部断开。
- 公式绘制位置不再取可能横跨多行的整体包围框，而是严格使用公式首字形所属的真实视觉行片段与首字形坐标；行首、行尾和自动换行统一使用同一套坐标。
- 继续保留旧管线正文中的完整Markdown源码，没有引入附件字符替换，也不改变保存、焦点、撤销和脏包流程。
- TextKit2新管线原本使用原子公式附件，本次不改动其正文和焦点管线。

## 验证

- 使用长中文正文与`$e = 1.6 \times 10^{-19} C$`构造自动换行边界，排版结果确认整个公式范围只占一个视觉行。
- Mac Debug与iPhone Simulator Debug构建通过。

# 882 图片粘贴本行事务与视野稳定

- 版本号更新为`V1.2607262203`。
- 找到图片粘贴三个问题的共同根因：原流程由父页面改写Node后强制回灌整篇文档，焦点恢复、离行渲染登记和滚动视野分别属于不同事务。
- 图片文件继续由父页面按照H3包作用域落盘，但父页面只返回插入后的本行正文，不再改写整篇NodeMarkdown文档，也不再为图片粘贴增加外部全文同步令牌。
- 新旧TextKit管线分别在自己的唯一正文存储中完成本行替换，保留Node UUID、层级、SourceID和SourceFile，图片粘贴成为与普通输入一致的本行编辑。
- 粘贴完成后焦点固定在图片链接所在行的行末；该行同步登记为已编辑行，第一次离开该行便通过统一离行入口恢复渲染，不需要重新点击链接再离开。
- 图片粘贴期间禁止选择区域触发自动滚动，并复用`Command+S`保存事务的原始视野恢复方式，跨过当前和后续两轮布局后再解除锁定。
- 图片粘贴继续进入原有撤销与正文发布链；普通文字粘贴不受影响。

## 验证

- Mac Debug与iPhone Simulator Debug构建通过。

# 881 公式中文下标边界修复

- 版本号更新为`V1.2607262138`。
- 找到`∆x = X_后 - X_前 = L`中第二个`X`变小的根因：普通中文包装先于上下标处理，并把空格、减号、字母和下划线继续并入同一段，导致`后 - X_前`整体成为第一个`X`的下标。
- 公式正规化顺序统一为“Unicode数学符号、上下标、根式、普通中文”；中文上下标先取得自己的确定边界，后续普通中文处理不能再扩大它。
- 普通中文段不再跨越空格、减号和下划线，数学运算符及后续变量保持各自的公式语义；显式`\\text{...}`内容继续原样保留。
- 同时支持外观相近但编码不同的增量符号`∆`与希腊大写`Δ`，两者均正规化为`\\Delta`。

## 验证

- 使用原始公式和中文上下标、根式、普通中文组合进行正规化回归检查。
- Mac Debug与iPhone Simulator Debug构建通过。

# 880 NodeMarkdown保存与更新视野事务解耦

- 版本号更新为`V1.2607262112`。
- 普通保存、包同步、编辑器全文替换和焦点导航不再被视为同一种刷新事务；保存状态变化没有资格触发滚动。
- TextKit2删除普通SwiftUI更新后的活动行定位；只有真正的外部全文替换且无法恢复视觉锚点时，才允许执行一次定位。
- 更新完成后先比较磁盘文档与当前文档的UUID、层级和正文。三者完全相同时，只合并SourceID、SourceFile、Cache和保存状态，不替换NSTextStorage，不重建正文，不刷新样式。
- SourceID和SourceFile从TextKit布局身份中移除；它们继续参与H3保护与同步，但单独变化不会让旧TextKit或TextKit2重排文字。
- 如果新包清洗、图片迁移等确实改变了正文或结构，则允许外部全文替换，但不再按焦点恢复视野。
- 新旧TextKit管线统一记录当前视野顶部第一个有效Node的UUID及其屏内纵向偏移；全文替换后按同一Node恢复，文档上方增删行也不会改变眼前位置。
- 旧管线保存时继续阻止NSTextView因选择区域自动滚动；保存提交只更新活动Node草稿和磁盘快照。
- 增加元数据布局身份回归检查，固定Source字段变化不触发布局刷新的规则。

# 879 随堂插入锚点与保存视野彻底分离

- 版本号更新为`V1.2607261648`。
- 找到失焦后仍按旧位置插入的根因：插入锚点只检查历史活动行，没有验证编辑器当前是否仍处于输入会话。
- 随堂按钮按下时统一冻结两种状态：输入会话有效且Node身份有效时冻结Node UUID；ESC、窗口失焦、弹窗失焦或没有活动Node时固定冻结文末。
- 写入旧的活动行共享存储时同步使用冻结结果；文末锚点不会再把失焦前的行号留给后续插入流程。
- 有焦点插入继续按磁盘最新文档中的UUID定位：H3内部落在完整H3包后，H1/H2节点落在该节点后；锚点Node不存在时安全退回文末。
- 找到TextKit2保存跳动的直接原因：普通SwiftUI状态更新也会调用滚到活动行。现在只有真正的整篇外部替换才允许执行活动行定位，保存状态变化完全不操作视野。
- 旧TextKit管线的保存提交增加视野事务：提交活动Node期间禁止NSTextView因选择区域自动滚动，并跨过本次SwiftUI与AppKit布局后恢复原滚动坐标。
- 增加插入锚点回归检查，固定“编辑状态用Node、非编辑状态用文末”的规则。

# 878 NodeMarkdown旧管线正文层级颜色映射收口

- 版本号更新为`V1.2607251230`。
- 检查确认设置文件中的正文1至正文5均独立落盘；当前正文3与正文4保存的是不同颜色，原有设置数据没有被覆盖。
- 原来的层级读取、设置页写回和旧管线渲染分别维护数字分支，虽然当前静态分支未直接写反，但缺少统一约束，存在正文层级错位后串色的风险。
- 新增12层Node到样式角色的唯一映射；正文第3层固定对应`level 9/body3`，正文第4层固定对应`level 10/body4`。
- NodeMarkdown设置页写回样式改为使用统一映射，不再单独维护一份层级分支。
- NodeMarkdown旧管线的行文字颜色改为通过共享渲染契约取值，文字、编号、背景条与导出继续使用同一层级事实。
- 增加旧管线启动回归检查，使用互不相同的正文颜色验证正文3、正文4不会错位或共用设置。

# 877 ESC失焦后随堂插入固定文末

- 版本号更新为`V1.2607241926`。
- 确认#876剩余问题不在插入位置计算，而在ESC失焦状态传递：界面已经退出源码编辑，但父页面仍可能短暂保存失焦前的活动行。
- 旧TextKit管线此前依赖`textDidEndEditing`异步通知活动行为空；ESC之后立刻点击随堂时，旧H3行可能先被冻结为插入锚点。
- 旧管线ESC现在先提交当前Node草稿并恢复渲染，再明确发布活动行为空，最后交还第一响应者；系统编辑结束通知只做重复收尾。
- TextKit2管线ESC现在同步清除编辑行、父页面活动行和焦点位置，然后才交还第一响应者；不再依赖稍后的失焦回调决定随堂锚点。
- ESC的业务含义固定为“完全失焦”：之后点击随堂必定冻结`documentEnd`，H3包插入到清理尾部空行后的文末。
- 鼠标仍在某行编辑、未按ESC时，#876的Node UUID锚点规则保持不变，继续插在焦点所属H3包或H1/H2节点后面。
- 插入刷新继续保持原滚动视野，不主动滚到文末或新插入的H3包。
- Mac Debug与iPhone Simulator Debug构建通过。

# 876 随堂H3包插入锚点与视野稳定

- 版本号更新为`V1.2607241706`。
- 找到随堂H3包位置不稳定的四个根因：工具栏按钮与编辑器失焦存在时序竞争、冻结UUID后又退化为旧行号、H1/H2被错误按整个子树计算、课程页无焦点插入仍可能读取历史焦点。
- 随堂插入锚点统一为两种确定状态：文末，或者焦点Node的UUID；瞬时行号不再跨越弹窗和磁盘写入事务。
- 按下随堂按钮时立即冻结锚点，早于清单弹窗夺走编辑器焦点；按钮自身不参与键盘焦点，避免旧管线和TextKit2因失焦回调顺序不同得到不同结果。
- 插入前先提交活动行并落盘，随后用冻结的Node UUID在磁盘最新文档中重新定位；活动行换行、清洗或结构变化不会让锚点错到其他Node。
- 有焦点时：焦点属于H3包便插到该完整H3包之后；焦点是H1或H2便紧跟该节点插入；旧数据中孤立的其他层级则紧跟焦点Node，不再静默跳到章末或文末。
- 没有焦点时明确使用文末锚点；课程页尚未打开编辑器的插入也固定放到文末，不再读取上一次遗留焦点。
- 多个选中H3包继续按用户选择顺序连续插入，后续原内容整体后移。
- 取消清单、识别学生失败或读取失败会清除本次冻结锚点，下一次操作不会复用旧位置。
- 插入后的两套TextKit外部刷新继续锁定原可见区域，只恢复原焦点Node身份，不滚动到新包，不改变当前视野。
- Mac Debug与iPhone Simulator Debug构建通过。

# 875 黑白PDF高亮与背景条边框

- 版本号更新为`V1.2607241631`。
- 所有黑白NodeMarkdown PDF统一增加装饰边框，包括普通黑白PDF、黑白PDF分H1和PDF黑白分文件。
- 黑白导出样式增加只在本次导出内存中存在的边框标记，不写入文档设置文件，不改变彩色PDF、HTML和NodeMarkdown编辑器的显示。
- 背景条保留原来的浅灰渐变和椭圆轮廓，在渐变完成后沿同一个轮廓绘制黑色细边框，黑白打印时可以辨认条幅范围。
- 普通文字高亮与公式高亮共用圆角高亮绘制规则；黑白版本在填充之后增加黑色细边框，彩色版本继续保持原效果。
- 多行文字高亮按每个实际排版行片段分别绘制边框，不用一个大框跨过自动换行区域。
- 黑白PDF原生TextKit渲染失败时，直接进入支持相同边框规则的HTML PDF回退，不再经过会丢失行内高亮的旧打印回退器。
- Mac Debug与iPhone Simulator Debug构建通过。

# 874 iPhone表格图片导出恢复

- 版本号更新为`V1.2607241457`。
- 找到iPhone表格导出失效的根因：单列表格编辑器的图片生成和系统分享实现全部限定为macOS，iPhone点击导出时只会进入“当前平台暂不支持”的占位分支。
- 表格图片导出改为双平台共用流程；Mac继续使用`NSImage`，iPhone和iPad使用`UIImage`，两端都从同一个表格快照生成PNG。
- 导出前先结束当前活动输入、提交最后一个编辑字段并完成落盘，再读取最新行数据生成图片，避免最后输入的一行没有进入导出结果。
- iPhone和iPad生成PNG后接入系统分享页，可以保存到文件、图片或发送到其他App。
- iOS系统分享页改为从当前最上层弹窗展示；表格编辑器处于sheet内时也能正常出现，不会被主窗口或当前弹窗挡住。
- iPad分享页补齐系统要求的弹出锚点，避免因缺少`popoverPresentationController`位置而无法展示或崩溃。
- Mac Debug与iPhone Simulator Debug构建通过。

# 873 下课导出同步目录失效授权自动恢复

- 版本号更新为`V1.2607241405`。
- 确认本次失败不是同步目录路径丢失：学生真实目录已经正确落盘，失败原因是#812之前保存的普通书签不含可恢复的macOS沙盒写权限。
- 安全目录访问增加可选的交互恢复模式；普通文件操作保持原行为，不会随意弹出目录选择窗口。
- 下课自动导出PDF、分H1 PDF、分文件PDF和HTML时启用交互恢复：旧书签失效后直接弹出重新授权窗口，并默认定位到当前学生真实同步目录。
- 用户可以选择学生目录或它的上级目录；程序会检查所选目录确实覆盖目标学生目录，选错时明确提示，不会把文件写到错误位置。
- 授权完成后自动保存新的安全书签并立即重试同一次下课导出，不需要用户返回学生设置再操作一遍。
- 若选择的是共享上级目录，恢复访问后会继续为真实学生目录补建独立书签，后续下课不再重复授权。
- Mac Debug与iPhone Simulator Debug构建通过。

# 872 画图颜色、新包入库与NodeMarkdown输入保存稳定性

- 版本号更新为`V1.2607241259`。
- 画图颜色入口改为12个快捷色，面板下方保留支持透明度的自定义颜色；当前颜色继续落盘到`setting-画图.json`。
- 画图或图片真正插入Node时，只清除行首遗留的`🌼{图片}🌼`占位标签，不改动正文中其他内容，生成的Markdown图片链接不再带花朵前缀。
- 修复同一章教案同时存在新包和脏包时的写入覆盖：先以已经插入完整新H3包的磁盘母本为基础，再合并本轮脏包修改，统一迁移图片并最终写盘。
- 新包落盘复核从“只检查UUID存在”加强为“UUID、来源链接和整个H3包内容签名一致”；不完整插入不会再被报告为成功。
- NodeMarkdown旧管线在拼音候选写入前先发布当前行的临时UTF-16坐标，避免AppKit绘制中间帧时把下一行识别成输入行；候选确认后才重建稳定行索引。
- 拼音候选期间协调器和自绘视图共用同一组临时行坐标，下一行编号、层级、背景条和后续内容不再因两套坐标不同步而串行或跳动。
- `Command+S`提交活动Node草稿时锁定当前滚动原点，不执行滚动到焦点等视野调整。
- 关闭或下课清洗掉当前焦点空行时不再强制失焦；旧管线把焦点准确放到上一行末尾，编辑器继续保持第一响应者。
- Mac Debug与iPhone Simulator Debug构建通过。

# 870 iPhone表格编辑器解除桌面尺寸锁定

- 版本号更新为`V1.2607232307`。
- 找到iPhone表格几乎无法显示的根因：排课课反、课程页课反等入口把Mac所需的`760×560`或`900×620`最小窗口尺寸直接施加给了iPhone弹窗，内容大于屏幕后只能被裁切。
- 建立统一的表格弹窗尺寸规则：Mac继续保留原工作尺寸；iPhone和iPad只使用系统实际可用宽高，并以大尺寸sheet展示，不再继承桌面最小宽高。
- 首页学生课反、排课课反、课程页文件与课反、NodeMarkdown课反，以及表格模板的字体和颜色弹窗全部接入同一尺寸规则。
- iPhone紧凑表格编辑器减少页面和列表边距，标题最多显示两行并可按空间缩放，正文编辑区域获得更多有效宽度和高度。
- Mac的表格编辑、自动保存、关闭前保存和导出前保存逻辑均未改变。
- Mac Debug与iPhone Simulator Debug构建通过。

# 869 iPhone紧凑布局、Mac设置外观与课表长按分流

- 版本号更新为`V1.2607232254`。
- iPhone竖屏档案页不再横排九个按钮：前五个页面入口收为当前页面菜单，后面的页面操作收为独立操作菜单，两个菜单均可展开完整功能。
- iPhone横屏、iPad与Mac继续使用原有展开工具栏，不牺牲宽屏操作效率。
- Mac设置首页改为大字号、自适应宽度的圆角分组设计；宽窗口使用双栏，窄窗口自动变成单栏，高度随内容和窗口变化，不再沿用紧密的默认列表外观。
- 成课、排课和周表继续共用同一课块菜单；iPhone课块不再注册长按拖动手势，长按只打开右键功能，Mac仍保留长按拖动排课。
- Mac Debug与iPhone Simulator Debug构建通过。

# 868 排课占位课重命名并排除预计统计

- 版本号更新为`V1.2607232237`。
- 排课和周表的特殊占位项由“刚需|生活”统一改名为“占位|空课”。
- 占位课沿用原有固定学生UUID和机构UUID；旧`排课.json`、`周表.json`读取时自动采用新名称，后续保存时写回新名称，不破坏既有排课数据。
- 排课的月预计课时量和月预计收入都按固定UUID排除占位课，不依赖显示名称，旧数据也不会误计。
- 已进入排课历史显示的占位记录同样规范为新名称，避免同一页面同时出现新旧名称。
- Mac Debug与iPhone Simulator Debug构建通过。

# 867 NodeMarkdown保存与插包视野锁定

- 版本号更新为`V1.2607231302`。
- `Command+S`保存完成后不再重新发布整篇TextKit正文；保存只提交活动Node并落盘，焦点、选择和当前滚动视野保持原位。
- 更新脏包或新包时不再执行下课级别的整篇空行清洗；同步引擎仍会清洗实际回传的每个H3包，下课和关闭继续负责最终全文整理。
- 随堂清单打开前按Node UUID冻结插入锚点，清单弹窗临时拿走焦点不会丢失原插入位置。
- 有焦点时，插入点严格位于焦点所属H3包、H2包或H1包的完整末尾；多包按用户点选顺序连续插入。
- 没有焦点时，先删除文末全部普通空节点，再把内容追加到最后一个有效Node之后；受保护H3或尚未命名的新H3不会被尾部清理误删。
- 旧管线真正失焦时会清空活动行，不再把历史焦点误认为当前焦点。
- TextKit2新管线在外部整篇刷新前按Node UUID和Node内偏移记录焦点，刷新后恢复同一Node；两套管线继续保持原滚动原点，不主动滚到插入内容。
- Mac Debug与iPhone Simulator Debug构建通过。

# 866 下课与同步自动导出PDF分文件

- 版本号更新为`V1.2607231009`。
- 下课收尾自动导出新增“PDF分文件”：每个H1及其所属内容生成一个独立PDF，首个H1之前的实际内容也保留为独立文件。
- 分文件保存到学生同步目录的`1-教案PDF/随堂笔记_<学生名>_分文件/`，文件名沿用H1标题；重名标题继续使用既有的稳定编号规则。
- 分文件与普通PDF、分H1 PDF、HTML共用同一次文档快照和样式，避免同一次下课导出的内容版本不一致。
- 整套分文件先写入同级临时目录，全部成功后才替换正式目录；H1删除或改名后不会残留旧PDF，渲染或写盘失败时保留上一套完整结果。
- 同步页“全部导出”与下课共用这一入口，也会重新生成每个学生的PDF分文件。
- Mac Debug与iPhone Simulator Debug构建通过。

# 865 统计页高度约束与底部导航常驻

- 版本号更新为`V1.2607221622`。
- 修复统计子页内容高度超过窗口后，把主界面底部三个按钮顶出屏幕、无法点击的问题。
- 主窗口明确把底部导航设为固定高度、不可纵向压缩；中间页面只使用剩余空间并裁切越界布局，任何子页面都不能再扩大主窗口内容层。
- 授课主页和统计主页分别建立可用高度边界，统计顶部切换栏保持可见，机构、成课、排课、周表只在中间区域布局。
- 成课的大型横向课表改为固定内容尺寸、可收缩滚动视口；下方文字记录拥有独立的有限滚动区。
- 排课与周表取消不可压缩的固定视口高度，课表内容尺寸不变，窗口较矮或iPhone显示时只缩小滚动视口，内容仍可横向、纵向查看。
- Mac Debug与iPhone Simulator Debug构建通过。

# 864 上课加载与保存进度状态收口

- 版本号更新为`V1.2607221135`。
- 修复上课编辑器左上角加载菊花永久不停：磁盘加载之前误用了TextKit文本解析版本号，加载期间任何样式或草稿刷新都可以废弃加载结果，但`isLoading`来不及收尾。
- 磁盘加载建立独立`diskLoadGeneration`，只有新的磁盘加载才能废弃旧加载；文本解析、样式刷新和输入回调不再影响加载菊花。
- 修复保存图标永久旋转：旧保存任务在排队时发现文档版本已更新，现在会明确回到`dirty`并立即串行保存最新版本，不再停留在`saving`。
- 保存正在进行时不重复创建写入任务；文档已保存且没有新修改时，`Command+S`直接返回，不显示旋转、不写盘、不启动脏包同步。
- Mac Debug与iPhone Simulator Debug构建通过。

# 863 上课Command+S保存视野完全不动

- 版本号更新为`V1.2607221100`。
- 查明跳动不是保存文件本身引起，而是上课状态的`Command+S`落盘后，又在自动脏包同步前调用了整篇空行清洗；删除上方空行后编辑器必须更换整份文本，因而视野会瞬间跳一次。
- `Command+S`现在只提交活动Node的UUID、层级、正文和来源信息，然后落盘当前文档快照；不再删行、更换全文或发送强制外部刷新。
- 脏H3包的空行清洗没有取消：同步引擎仍在每个待回传包提取、新包入库和母本替换前统一执行`cleanPackage`，清洗后的内容才会进入母本。
- 下课、关闭和明确的最终整理仍执行整篇空行清洗，不改变原有收尾规则。
- Mac Debug与iPhone Simulator Debug构建通过。

# 863 NodeMarkdown所有PDF公式行片段坐标统一

- 版本号更新为`V1.2607212320`。
- 查明公式飞到上下其他行的根因：`NSLayoutManager.location(forGlyphAt:)`返回的是公式字形相对于所属行片段的局部坐标，PDF链路之前把它当成了整个文本容器的绝对坐标；公式不在段落第一行时，就会被绘制到错误的上一行或上方段落。
- 建立唯一公式几何换算：先根据公式字形找到真正所属的行片段，再把行片段原点与行内坐标合成文本容器坐标，最后按该行文字中线绘制公式。
- PDF行高测量与最终公式绘制共用同一换算，避免测量与显示分别理解坐标而再次偏移。
- 普通PDF、分H1 PDF、分文件PDF、黑白PDF、黑白分H1 PDF和黑白分文件PDF全部经过这一条公共链路，没有为某个导出按钮另加补偿。
- 用自动换行附件布局做坐标验证：旧值为局部`y=17`，公式实际所在行片段为`y=83`，新值正确还原为容器`y=100`。
- Mac Debug与iPhone Simulator Debug构建通过。

# 862 学生同步目录安全书签根因修复与旧数据迁移

- 版本号更新为`V1.2607212315`。
- 根据下课错误中的完整路径核验物理文件：学生同步目录和`1-教案PDF`均真实存在；失败来自目录权限书签，而不是设置地址丢失或iCloud目录不存在。
- 检查现有书签存储发现28条记录没有一条能按macOS安全书签解析，其中24条只能作为普通路径书签读取、4条已经损坏；这解释了路径完全正确却反复提示`The file couldn't be opened`。
- 修正保存根因：目录选择器返回的原始授权URL专门用于生成并验证安全书签；解析符号链接后的真实物理路径仅用于学生设置和书签索引，不再拿失去授权身份的新URL制作书签。
- 修正读取根因：安全书签解析得到的授权URL原样用于`startAccessingSecurityScopedResource`；规范化URL只用于路径比较，避免解析成功后再次主动剥掉授权。
- 学生新建、学生统一设置、学生独立设置和课程初始化四个目录选择入口全部传递原始选择URL，保存的学生第七项仍是规范化后的真实物理地址。
- 旧版普通书签会先尝试在当前权限仍可用时自动升级为安全书签并重新落盘；无法自动升级时明确要求重新授权一次，重新选择后不再继续生成旧格式书签。
- 书签候选除按旧路径逐级查找外，也会解析已存书签的真实当前位置；目录改名后旧Key仍可授权新路径，并会自动补建新路径书签。
- Mac Debug与iPhone Simulator Debug构建通过。

# 861 NodeMarkdown PDF公式与编辑器共用基线排版

- 版本号更新为`V1.2607212304`。
- 查明PDF公式下沉不是SwiftMath图片内容偏移，而是PDF在进入TextKit排版前先按字体中线改写了一次附件边界，排版完成后绘图阶段又按文字中线居中一次；高公式改变行框后，第二次计算会把公式整体推向行底。
- PDF公式附件现在与NodeMarkdown编辑器使用完全相同的原始基线边界参与TextKit行布局；PDF只在最终绘制时按所属行文字中线定位一次，不再预移动附件。
- 普通文字、上下标、分式及混合公式均走同一基线规则，不再根据公式内容添加人工偏移常数。
- Mac Debug与iPhone Simulator Debug构建通过。

# 860 普通Markdown图片、Node层级保持与背景条椭圆统一

- 版本号更新为`V1.2607212242`。
- 普通Markdown在双区域、TextKit2、源码和即时模式中统一支持从访达图片文件或剪贴板图像执行`Command+V`；图片使用内容哈希命名、压缩后原子写入`系统/Markdown图片`，正文保存相对于当前Markdown文件的标准图片链接。
- 普通Markdown的TextKit2模式插图前先还原光标行内已经渲染的图片源码，保证新链接插在真实Markdown字符位置；剪贴板不是图片时继续使用系统原生文字粘贴。
- NodeMarkdown插图前强制提交当前编辑Node的UUID、level、content、SourceID和SourceFile，再读取父文档并写入图片链接；修复H5回车、Tab变H6后粘图又被旧H5层级覆盖的问题，新旧管线共用该提交边界。
- 背景条建立统一椭圆规则：横向圆角半轴固定为当前层级一个字宽，纵向圆角半轴为实际背景条高度的一半；单行两端接近半圆，多行背景可随内容持续增高。
- NodeMarkdown旧管线、TextKit2新管线、PDF和HTML统一使用同一椭圆规则，避免各输出端继续以单个圆角常数绘制出不同形状。
- Mac Debug与iPhone Simulator Debug构建通过。

# 859 NodeMarkdown剪贴板图片Command+V插入

- 版本号更新为`V1.2607212227`。
- 查明NodeMarkdown新旧管线虽然都处理了`paste(_:)`，但macOS可能在它之前由菜单的键盘等效命令消费`Command+V`，因此右键插图正常而快捷键没有进入图片入口。
- 新旧管线的文本视图统一在键盘等效命令层拦截纯`Command+V`，剪贴板含访达图片文件或图像数据时，直接调用与右键“插入图片”相同的资源落盘与Markdown链接生成流程。
- `keyDown`保留同样的图片判断作为直接键盘事件的兜底；剪贴板不含图片时完全交回系统，普通文字粘贴不受影响。

# 858 NodeMarkdown结构操作与包同步保持视野

- 版本号更新为`V1.2607212204`。
- NodeMarkdown新旧管线删除节点包前先提交当前编辑内容；只有删除成功后才失焦，删除取消或失败不改变焦点。
- 旧管线外部结构替换后取消强制`scrollRangeToVisible`，只恢复操作前的滚动坐标，避免包删除后视野追随替代行跳走。
- 随堂插包、脏包回传和新包入库后，按Node UUID恢复活动节点，不再把编辑行写入搜索定位状态，避免刷新触发搜索式强制滚动。
- 所有脏包回传母本前先清理空行并重新落盘，保证随堂笔记与母本收到同一份已清洗快照。
- 空行清洗改为按UUID保留仍存在的活动Node；只有活动空行本身被删除时才失焦，不再因清洗无条件跳转视野。

# 856 NodeMarkdown按H1批量分文件导出与黑白打印评估

- 版本号更新为`V1.2607211221`。
- NodeMarkdown导出菜单新增“导出PDF分文件”和“导出PDF黑白分文件”，与原有“分H1换页但仍为一个PDF”的功能明确分离。
- 新增独立H1分文件结构：每个H1连同其下内容组成一份PDF；首个H1前若存在正文，也会独立导出，不再要求文档必须以H1开头。
- H1分文件直接复用现有NodeMarkdown PDF渲染管线，图片、公式、层级样式、背景条和PDF缩放规则均保持一致；黑白分文件使用现有黑白样式。
- 批量导出只打开一次系统目录选择器；所有PDF先完整渲染成功，再逐个原子写入所选目录，避免渲染中途留下半套文件。
- 文件名采用H1内容；首段没有H1时采用原文档名。非法文件名字符会替换，空标题使用“未命名H1”，重名标题按`-2`、`-3`递增，避免同批文件相互覆盖。
- 检查黑白打印背景条：主PDF管线把层级色按页面底色预合成为10%到2%的浅渐变，黑白导出时相当于浅灰渐变，正常黑白打印不会遮字；但大面积背景仍会增加网点和碳粉，省墨或低质量打印可能略显灰脏。
- Mac Debug与iPhone Simulator Debug构建通过。

# 855 NodeMarkdown黑白分H1导出与文件命名统一

- 版本号更新为`V1.2607211158`。
- NodeMarkdown导出菜单增加“导出黑白PDF分H1”；使用黑白文档样式，并在每个H1节点自身之前换页。
- 原“导出为PDF分天”改名为“导出PDF分H1”，明确实际分页规则不是按自然日，而是按H1节点分页。
- 四种PDF导出建立独立格式：普通PDF不加后缀、分H1 PDF加`_分H1`、黑白PDF加`_黑白`、黑白分H1 PDF加`_黑白分H1`。
- 系统保存面板和临时分享文件统一使用上述后缀，避免菜单含义与导出文件名不一致。
- 下课和同步页自动生成的原`随堂笔记_<学生名>_分天.PDF`改为`随堂笔记_<学生名>_分H1.PDF`，事务日志同步改称“分H1 PDF”。
- 分段合并失败等错误提示统一从“分天PDF”改为“分H1 PDF”。
- Mac Debug与iPhone Simulator Debug构建通过。

# 854 下课同步目录永久授权逐级恢复

- 版本号更新为`V1.2607210720`。
- 读取班课郭实际学生设置，确认真实同步目录早已逐学生落盘为`.../26061-班课郭`；本次失败不是每次重新抓取地址，也不是PDF写入时目录消失。
- 对照下课事务与PDF日志确认：普通PDF和分天PDF均已在内存完成渲染，但尚未进入第一条“开始写入已渲染PDF”日志就报错，因此失败点是同步目录的security-scoped bookmark解析，而不是PDF内容或文件写入。
- 重构目录授权查找：从学生真实目录的精确书签开始，逐级尝试父目录书签；损坏的精确书签不再阻断仍然有效的“5-学生数据”等父目录授权。
- 使用父目录授权成功进入学生目录后，立即为该学生真实目录重新生成独立永久书签，并以原设置路径和解析后的真实路径双键落盘，后续直接使用学生目录授权。
- 所有书签都失效时仍尝试已落盘真实路径；若沙盒拒绝访问，错误改为中文并明确展示真实目录和每一级书签失败原因，不再只显示`The file couldn't be opened.`。
- 新增`SecurityScope`调试记录，可区分精确书签损坏、父目录恢复成功和确实需要用户重新授权三种情况。
- Mac Debug与iPhone Simulator Debug构建通过。

# 853 首页与排课课反弹出页工具栏修复

- 版本号更新为`V1.2607202025`。
- 查明学生文件入口通过主窗口的`navigationDestination`展示单列表格，编辑器工具栏由主窗口承载；首页和排课入口通过macOS sheet展示，sheet无法稳定接管父窗口工具栏，所以虽然使用同一个编辑器，导出等按钮没有显示。
- 单列表格编辑器增加弹出模式内建操作栏，明确提供关闭、上一个课反、下一个课反、搜索、深浅色、目录、导出和保存，不再依赖sheet系统工具栏。
- 导航模式继续使用原工具栏，弹出模式使用页面内操作栏，两者调用相同的保存、导出、课反切换和搜索实现，避免以后功能分叉。
- 弹出模式的操作栏支持横向紧凑布局，iPhone宽度不足时仍能访问所有按钮。
- Mac Debug与iPhone Simulator Debug构建通过。

# 851 课反统一打开与单列表格保存关口

- 版本号更新为`V1.2607201900`。
- 授课首页学生右键“上课记录”先检查该学生当天是否存在排课；当天有排课且尚无课反时，按学生课反模板建立当天文件，再打开日期最晚的一份上课记录。没有当天排课时不会因为浏览学生而新建课反。
- 统计排课课程右键“打开课反”继续以该课程实际日期定位文件；缺失时建立，有文件时直接打开。
- 首页和排课入口统一使用与学生文件页相同的`SingleListDocumentEditorView`，保留图片导出、课反前后切换、搜索、目录、深浅色和保存能力。
- 单列表格建立统一保存关口：关闭、Command+S、保存按钮、图片导出、前后切换文件前，均先结束活动输入并提交最后一行内容，下一轮主线程再原子保存；保存失败时不关闭、不切换、不导出。
- 自动保存继续保持一秒防抖；窗口消失时仍执行最终兜底保存。
- Mac Debug与iPhone Simulator Debug构建通过。

# 841 上课NodeMarkdown实际PDF导出链路修复

- 版本号更新为`V1.2607190757`。
- 重新从上课按钮和下课自动导出向下追踪，确认实际主链路是`TeachingCourseWorkflowService.exportNotebookExportsIfPossible → NodeMarkdownPDFExporter.renderData → NodeMarkdownTextKitPDFExporter`。第840步重点修改Web回退样式，但上课主链路仍直接使用`0.28 → 0.14`的浓背景条，因此实际导出看不到变化。
- 在上课PDF实际使用的`NodeMarkdownTextKitPDFExporter`内直接重写背景条：不再调用饱和度增强，而是将层级原色按`10% → 2%`与PDF实际页面底色预合成，再绘制水平渐变。这保证浅色、深色和黑白PDF都淡且可读，也避免某些PDF查看器把透明色错误显示成浓色。
- 纯公式行在上课PDF主链路内单独处理：段前高度置零，并删除仅用于源码保护的透明LaTeX尾部和前后空格，只让公式附件参与排版，不再多出一个空行。
- 上课PDF的正文起点在“一个当前字号的编号占位”之后再独立增加`markerGap`，不再把间距吞进字号占位中，大字号编号与正文不再重叠。
- 两个导出失败回退链路同步改为`10% → 2%`淡渐变，避免原生导出偶发失败后又生成浓色PDF。
- 调试日志新增实际链路记录：`PDF.Export / 上课NodeMarkdown PDF渲染链路：TextKit原生导出`，以后可直接确认最终使用的渲染器。
- Mac Debug与 iPhone Simulator Debug构建验证通过，并确认应用包内实际可执行文件。

# 840 NodeMarkdown编辑器与PDF背景条、公式行和编号对齐

- 版本号更新为`V1.2607190644`。
- 删除第837步背景条`40%`饱和度增强；在统一渲染契约中改为仅增加`10%`，旧TextKit管线、新TextKit2管线、原生PDF和Web回退PDF共用同一规则，禁止导出器再次增强。
- PDF背景条取消额外混白，起点和终点使用同一饱和色的不同透明度，恢复与NodeMarkdown编辑器一致的水平渐变、半高圆角和背景条强度。
- 纯公式行导出时只保留公式附件锚点，透明LaTeX源码尾部不再参与TextKit换行；Web/KaTeX路径同时取消块公式默认上下外边距，去掉公式上方多出的空行。
- PDF编号不再只用`2px`外边距推开正文，改为直接使用NodeMarkdown共享契约中的编号到正文固定占位；大字号、小字号和12个层级都不再重叠。
- Mac Debug与 iPhone Simulator Debug构建验证通过，并检查应用包内实际可执行文件。

# 839 统计时间操作、课反连续浏览与首页学生菜单

- 版本号更新为`V1.2607190028`。
- 统计课程添加和修改窗口的开始时间快捷键改为固定两行七列：第一行`+5、+10、+20、+30、+40、+50、+60`，第二行`-5、-10、-20、-30、-40、-50、-60`；取消横向滚动，按钮宽度由窗口平均分配，不再超出框体。
- 统计排课的课程块右键菜单删除裸“复制”项；“复制文本·学生”和“复制文本·机构”等统计输出功能保持不变。
- 单列表格编辑器为`上课信息_*.CSV`增加课反专用前后按钮：只扫描同文件夹课反，按文件名升序排列并循环切换，切换前先保存当前课反。由档案、排课和首页打开的课反统一获得该功能。
- 上课页和NodeMarkdown原本已有课反前后工具栏，这两个入口关闭编辑器内建按钮，避免同一窗口出现两套箭头；上课页的课反列表也统一按文件名升序，打开时定位到最新一项。
- 首页授课学生卡增加右键菜单：学生信息、最近上课记录、更改图标、更改颜色。学生信息复用现有建档与文件定位；上课记录打开最新课反，并可通过前后按钮按名字顺序浏览同学生历史课反。
- Mac Debug与iPhone Simulator Debug构建通过。

# 838 NodeMarkdown旧管线公式显示与行高恢复

- 版本号更新为`V1.2607190011`。
- 查明第837步同时让公式高度扩大整行最小高度、抬高整行基线，并再次按基线移动公式，三层布局补偿互相叠加，造成公式被画出当前行、看似消失，同时把有公式的行撑得异常高。
- 公式恢复为不改变正文结构的覆盖绘制：真实Markdown源码和公式左右各半字留白继续保留，公式只按所属文字行的中线绘制，不再参与段落最小行高计算，也不再给整行写入额外基线偏移。
- 公式布局函数只返回渲染成功或失败，不再向调用方暴露公式高度，从接口层阻止公式尺寸以后再次误接入段落行高。
- 图片继续使用原有独立占高规则；公式修复不改变图片、PDF编号间距、PDF背景条、源码字符保护及滚动静态快照。
- 公式位置不再使用相对行坐标冒充全文坐标；每个公式的绘制矩形直接由所属行片段确定，公式自身高度不会反向改变对齐基准。
- 构建时发现iCloud生成了四个名称以` 2.swift`结尾的旧副本；这些副本原样保留到工程源码目录外的`iCloud重复Swift备份/838/`，不删除、不合并，也不再参与Xcode文件夹同步编译。
- 修正一次构建设置错误：`EXCLUDED_SOURCE_FILE_NAMES`中的带空格通配值被Xcode拆成`*`和`2.swift`，曾误排除全部源码并生成没有主程序的空`.app`。现已彻底删除该设置；验证标准增加为包内必须真实存在`Contents/MacOS/ATeaching`，不能只依据`BUILD SUCCEEDED`。
- Mac Debug与iPhone Simulator Debug构建验证通过；并在Xcode实际使用的DerivedData目录完成Mac干净重建，确认`ATeaching.app/Contents/MacOS/ATeaching`为具有执行权限的arm64 Mach-O文件，应用签名验证通过。

# 837 NodeMarkdown公式、PDF编号与背景条统一

- 版本号更新为`V1.2607182348`。
- 第836步已经同时消除公式`$`与图片链接`!`被附件替换后丢失的根源；本步核对确认旧管线渲染主体不再产生U+FFFC，也不增加新的图片链接恢复补丁。
- 修正公式对齐基准：此前按被公式自身撑高的行框中线居中，分式和上下标会反过来改变自己的对齐位置。现在新旧管线和PDF都按该行字体基线计算文字视觉中线，所有公式统一与文字中线对齐，不再针对`t_0`或任何具体公式设置例外。
- 旧管线中，公式撑高行框时同步把正文移动到扩展行框中央；公式和文字共享同一视觉中线，同时为高公式保留完整上下空间。
- 公式左右分别保留半个当前行字宽；透明源码的排版占位同步增加一个完整字宽，后续文字不会压住公式留白。
- PDF编号到正文的距离改用共享行样式，至少保留当前层级一个字宽；大字号编号不再与正文重叠，新管线和两种PDF同时使用该间距。
- 彩色PDF与黑白PDF的背景条统一使用NodeMarkdown背景条几何、40%饱和度增强、真实透明渐变和半高圆角；不再先把颜色混合成白底实色。
- 图片继续使用原有尺寸和行中线规则，不增加公式专用横向留白。
- Mac Debug和iPhone Simulator Debug使用相互隔离的构建目录验证通过。

# 836 NodeMarkdown旧管线滚动与公式图片渲染重构

- 版本号更新为`V1.2607182335`。
- 查明旧管线把公式和图片渲染成TextKit附件时，会把源码首字符替换成U+FFFC占位符；随后滚动又会反复清除、恢复并重建可见行属性。只要恢复范围与重绘范围出现一次错位，单`$`公式就会永久缺少第一个`$`，图片会缺少`!`，静态文字也可能显示成乱码。
- 删除公式和图片的“替换源码字符为附件”架构。`NSTextStorage.string`现在始终保存完整真实Markdown；公式和图片只在原字符范围上附加不可持久化的绘制载荷，源码字符透明参与排版，再由文本视图覆盖绘制结果。
- 删除旧附件源码令牌、附件锚点恢复、进入源码前恢复、提交后重新安装及遗留附件类。渲染过程增加Debug不变量检查，任何样式或绘制代码只要改变正文就立即触发断言。
- 滚动不再是渲染事务。删除滚动观察器、滚动防抖样式刷新、可见区重渲染入口和滚动触发类型；文档载入或外部替换时建立完整静态渲染快照，普通编辑只更新受影响行，滚动完全交给AppKit读取并绘制既有结果。
- 公式和图片绘制先从可见片段反查完整载荷范围，再按完整范围去重绘制，避免视野边界切进单`$...$`公式时只画后半段。单`$...$`、双`$$...$$`、`\\(...\\)`和`\\[...\\]`统一使用同一套不改源码的绘制流程。
- 撤回后的显示改为重建完整静态渲染快照，避免只恢复当前视野导致滚动到屏外时露出半成品样式。
- 保留旧数据文件边界的U+FFFC修复，只负责读取历史损坏数据；新渲染管线不再产生新的占位字符。
- Mac Debug和iPhone Simulator Debug构建通过。

# 835 NodeMarkdown旧管线滚动崩溃修复

- 版本号更新为`V1.2607182324`。
- 查明第833步把附件恢复和重建扩大为外层`NSTextStorage`事务后，滚动刷新会在`beginEditing`与`endEditing`之间调用`NSLayoutManager`查询可见字形；AppKit在文本存储尚未结束编辑时不允许强制布局，因此触发`EXC_BREAKPOINT`。
- 删除跨越布局查询的外层事务，恢复为每批样式属性独立、完整闭合的文本存储事务；可见行计算和字形失效均只在事务外执行。
- 保留第833步的关键数据修复：最终重绘行、附件源码恢复行和显示失效行仍使用同一集合，公式首个`$`与图片源码不会因相邻行刷新再次丢失。
- Mac Debug构建通过。

# 833 NodeMarkdown旧管线附件源码与滚动渲染事务收口

- 版本号更新为`V1.2607182307`。
- 查明可见区增量渲染会把目标行扩展到相邻行，但附件源码恢复仍只处理原目标行；相邻公式和图片随后被清样式，源码令牌丢失，只剩TextKit占位字符，造成公式首个`$`丢失、图片不再渲染和滚动时文字错位。
- 增量渲染现在先生成唯一的最终行集合；附件源码恢复、旧样式清理、公式图片重建和显示失效全部严格使用同一集合，不再分别扩展行。
- 附件锚点恢复与重新安装合并进同一个`NSTextStorage`编辑事务，布局系统看不到源码与附件之间的中间状态，避免滚动和局部刷新时行高、文字位置短暂跳动。
- 增加相邻行扩展回归检查，分别验证普通渲染会准确包含前后行、编辑期间只处理当前行。
- 根据实际旧数据补充占位字符修复用例，覆盖公式内部多个残留、公式首边界丢失、普通符号前残留和图片首字符丢失。
- 将历史占位字符修复前移到NodeMarkdown CSV读入边界，并在CSV写出边界再次兜底；新旧编辑管线、PDF/HTML导出和同步从文档模型拿到的都是真实公式与图片源码。
- 增加文件边界回归检查，验证损坏CSV读入后恢复公式首个`$`，再次写出时不再包含TextKit占位字符。
- Mac Debug和iPhone Simulator Debug构建通过。

# 832 NodeMarkdown导出与样式、统计机构视觉和编辑一致性完善

- 版本号更新为`V1.2607182254`。
- NodeMarkdown导出菜单增加黑白PDF；黑白模式保留字体、字号、间距、粗体、下划线和背景条结构，强制白底黑字并去除彩色语义。
- NodeMarkdown文档设置取消12级下拉菜单，改成可直接浏览的12级样式列表；正文1至正文5分别设置、分别落盘，并继续兼容旧设置中共用正文样式的数据。
- 统一12级编号与正文的水平排版契约：每一级使用相同缩进步长，编号宽度和编号到正文的间距不再随该级字号变化。
- 文档样式变化和外部整篇替换改为一次完整一致刷新；滚动实时渲染当前视野并预渲染前后24行，关闭时隐藏即将拆除的文本视图，避免快速滚动露源码及关闭瞬间混合渲染。
- 课程开始时间快捷调整补齐`-60`至`-5`和`+5`至`+60`。
- 机构新增时从高区分度色板自动推荐与现有机构差异最大的颜色，并支持选择课程图标；图标写入机构设置和课程快照，以80%课块尺寸透明叠放。
- Debug功能按钮增加“颜色整理”，可重新分配全部当前机构颜色，但不改历史成课快照和其他机构信息。
- 排课课块和文字清单右键增加“打开信息”“打开课反”；学生信息复用统一资料表，课反按课程日期查找，不存在时按学生统一设置建立后打开。
- 单列表格图片导出先提交活动文本框，再在下一轮主线程生成图片，首次导出即可包含最后一行刚输入的内容。
- 学生改名完成后发送统一学生列表变化通知，授课首页和独立课程窗口立即重载新姓名。
- Mac Debug和iPhone Simulator Debug构建通过。

# 831 NodeMarkdown旧管线公式与图片附件增量重绘修复

- 查明第829步启用可见区增量渲染后，目标行直接执行`setAttributes`，会先清除公式和图片附件保存的源码令牌；附件首字符仍是TextKit占位符，后续解析因此看不到公式开头的`$`和图片开头的`!`。
- 所有增量渲染统一执行“等长恢复目标行附件源码、清理旧样式、重新渲染”的顺序；公式、图片和普通文字重新使用同一份真实源码，禁止样式刷新销毁源码记录。
- 恢复只处理本次目标行，不扫描或替换全文；字符长度不变，因此不会移动后续行坐标和焦点。
- 增加历史附件占位符修复：U+FFFC只允许存在于TextKit内存，进入编辑缓冲区前根据成对公式边界和完整图片语法恢复；不在后台直接批量改写用户文件，修复内容随正常保存流程落盘。
- Mac Debug和iPhone Simulator Debug构建通过。

# 830 NodeMarkdown旧管线首次进入空白修复

- 查明第829步改用显式外部同步令牌后，异步磁盘载入仍直接更新正文和行身份，却没有推进同步令牌；编辑器先以空草稿创建后，会把真正载入误判为“没有外部变化”，因此进入时完全空白，退出或其他状态变化时才可能偶然补显示。
- 磁盘载入现在把正文、Node行身份和外部同步版本放在同一个主线程事务中更新。TextKit收到新版本后执行一次外部正文替换、重建行坐标并只渲染可见区。
- 保留第829步禁止日常全文比较的规则，没有恢复每次SwiftUI更新都提取全文的旧做法。
- Mac Debug和iPhone Simulator Debug构建通过。

# 829 NodeMarkdown旧管线分级渲染与过期任务隔离

- 完成旧管线鲁棒性整改第4步。首次打开之外彻底删除延迟全文渲染入口；普通输入、层级和段落变化、结构修改、外部覆盖以及滚动分别使用当前行、布局影响行、结构影响行或可见区刷新。
- SwiftUI更新不再提取NSTextStorage整篇正文判断是否变化。父页外部覆盖使用显式同步令牌，编辑器内部输入使用正文版本；没有新令牌就不扫描、不覆盖。
- 正文、文档样式和搜索分别维护独立版本。所有延迟渲染任务携带三元版本，执行前逐项核对；任何一项已经变化，旧任务直接作废，不能把旧层级或旧样式重新写回界面。
- 搜索词、当前匹配和活动行变化只恢复并重画搜索底色，不再重新执行字体、段落、公式、图片、编号和背景条渲染；原有Markdown高亮底色会在搜索结束后准确恢复。
- 滚动过程中只重画自绘装饰，停止滚动后仅补渲染新进入视野的行；长文档滚到后部不再借滚动触发全文样式工作。
- 字体、段落、行内正则、公式附件和图片尺寸继续使用缓存；样式变化只失效对应样式版本，搜索变化不清除这些缓存。
- 将活动草稿与保存快照、一次性焦点事务、渲染版本与刷新策略拆成独立Swift文件，主编辑器只负责TextKit连接与事件编排，职责边界更明确。
- 新增旧管线自动回归检查，覆盖末尾空行、普通字符坐标平移、公式图片行边界、结构焦点按UUID恢复且只消费一次、统一Node快照、H3来源保护、撤销快照形状、旧渲染版本作废以及万行文档只取可见区等十类边界。
- Mac Debug和iPhone Simulator Debug构建通过。

# 828 NodeMarkdown旧管线焦点、视野与拼音输入事务统一

- 旧管线确立唯一焦点管理规则：普通打字、鼠标点击和拼音候选完全交给TextKit；程序只在回车、跨行Backspace、Tab、Shift+Tab、多行结构删除以及Node包操作后恢复焦点。
- 结构焦点不再保存易受公式、图片附件和行重排影响的全文字符位置，统一保存`Node UUID + 行内UTF-16偏移`；目标Node被删除时才使用目标行号作为明确回退。
- 结构焦点锚点改为一次性事务。正文和行身份稳定后消费一次，设置一次选择范围并立即销毁；样式刷新、SwiftUI状态回写以及后续主线程任务都不能再次使用旧锚点移动焦点。
- 删除`applyStyle`中的隐式焦点恢复。渲染层只负责字体、颜色、段落、公式和图片，不再拥有光标控制权，避免一次结构操作被多个样式阶段重复改焦点。
- 回车、跨行Backspace、单行Tab/Shift+Tab、H3保护产生相邻空行和多行结构删除全部接入同一个结构焦点事务；删除各入口内原有的绝对字符位置恢复。
- 图片插入、Node包删除、剪切、粘贴和母本H3删除在交给父视图前先建立结构焦点锚点；外部文档快照落回TextKit并完成样式后只恢复一次。
- 回车后的视野抬升只执行一次`scrollRangeToVisible`。删除下一轮主线程再次滚动的任务，也删除样式刷新后下一轮主线程恢复旧滚动位置的任务；用户滚动后不再可能被迟到任务拉回旧视野。
- 拼音候选期间冻结活动Node身份和稳定行坐标，只允许TextKit维护临时候选字符；候选选择变化不发布活动行、不切换源码行、不设置焦点。
- 中文确认后只按最终字符差量更新当前Node，取消原有的强制全文样式重建；后续行的level、编号、字体、颜色和背景条不参与拼音事务。
- ESC先提交当前活动草稿和Node身份，再退出源码状态；不会因先失焦再提交而丢失最后一次输入。
- Mac Debug和iPhone Simulator Debug构建通过。

# 827 NodeMarkdown旧管线正文与Node身份统一快照

- 新增`NodeMarkdownLegacyDocumentSnapshot`。每一行在同一个不可拆分的值中携带`UUID、level、content、SourceID、SourceFile`，整份快照带编辑器会话ID和递增版本号。
- 旧管线的回车、跨行删除、Tab、Shift+Tab、多行操作和撤销等结构修改只发布统一快照，不再分别回调正文与行元数据；普通字符输入继续只更新活动Node草稿，不进入整篇快照路径。
- 父页保存最新快照身份并取消旧解析；异步解析提交前再次核对会话ID和版本号，迟到的旧结果不得覆盖新修改。外部重载、父页结构修改和单Node直接提交都会使旧快照失效。
- 新增快照专用解析入口，按UUID直接重建Node，不再使用正文相似度、行位置或前后缀猜测Node身份。新行使用新UUID，剪切移动保留原UUID。
- 快照生成时校验UUID合法性与唯一性；同一快照里的非法或重复身份立即换成新UUID，并清空其母本来源，避免两个本地H3包错误认领同一身份。
- 已有关联母本的H3在快照或活动草稿短暂缺少来源字段时，强制从原Node恢复`SourceID`和`SourceFile`，不得因编辑提交而清空母本链接。
- 保存、同步和导出强制收口时，旧管线优先解析完整快照；正文与Node身份不再从两个可错位的状态临时拼接。
- Mac Debug和iPhone Simulator Debug构建通过。

# 826 NodeMarkdown旧管线活动草稿保存事务

- 新增旧管线专用`NodeMarkdownLegacyDraftCommitController`，父页可以在不让NSTextView失焦的情况下，同步要求编辑器提交当前Node草稿。
- 强制提交完整保留`UUID、level、content、SourceID、SourceFile`；提交后立即以已提交内容重建当前行基线，保存不改变焦点，用户可继续输入。
- 活动行草稿第一次发生变化时立即把文档标记为未保存，不再等失焦；保存中继续输入会开启新修订，旧保存不得将新修订标为已保存。
- `Command+S`、工具栏保存、自动保存、普通关闭、上课关闭、下课同步、教案包操作前落盘以及PDF/HTML/MD导出，全部在生成文档快照前先经过同一草稿提交入口。
- 存在未提交活动草稿时，外部文件变化不得自动重载并覆盖当前内容。
- 取消上课窗口关闭时“保存同步事务”和“普通关闭保存”同时启动的重复写入；上课关闭只保留一条串行落盘与同步链路。
- 保存失败时不清理文档变更和图片撤回暂存，仍可再次保存。
- Mac Debug和iPhone Simulator Debug构建通过。

# 824 NodeMarkdown旧管线终行坐标统一

- 完成旧管线第一轮鲁棒性筛查，确认活动草稿保存、终行坐标、外部更新事务、滚动恢复和全文扫描为后续需要收束的主要风险。
- 修复文末空Node被解释为倒数第二行的根因。终行的UUID、level、Content和样式现在统一通过`NodeMarkdownLegacyRowRangeIndex`解析，不再将文末位置强制减一。
- 终行的局部样式、草稿内容提取、输入属性、附件源码恢复、回车、Tab、Shift+Tab、上下移动和结构删除改用同一份有效行范围。
- 修正末行行首Backspace合并逻辑：合并后是否保留末尾换行由当前被合并行决定，不再错用上一行的换行符，避免“虚空末行”删不掉。
- 终行普通字符输入只扩展终行的临时范围，不再借用上一行的段落属性，也不得改动倒数第二行的层级。
- Mac Debug和iPhone Simulator Debug构建通过。

# 823 NodeMarkdown旧管线末行结构与背景条修复

- 将文本末尾换行后产生的零长度段落正式纳入旧管线行范围索引；最后空行不再借用倒数第二行的level、编号和样式，Tab与Shift+Tab能够命中真正的最后一行。
- 全量样式构建统一复用`NodeMarkdownLegacyRowRangeIndex`，删除编辑器内部另一套不包含末尾空行的遍历规则，避免两份行数定义再次分叉。
- 背景条高度计算剔除行尾换行符，末尾额外排版片段不再被算进上一行，因此最后一条背景固定按真实Node内容高度绘制。
- 对真正的末尾空行使用TextKit额外行片段绘制其编号和背景；刚回车时无需继续打字，也能立即看到新行所属层级。
- 背景条保持原色相与亮度，饱和度提高40%；左右圆角按背景条高度的一半计算，形成稳定的半圆端部。
- Mac Debug和iPhone Simulator Debug构建通过。

# 822 NodeMarkdown旧管线行坐标与层级渲染稳定

- 查明当前行输入后，局部渲染仍用修改前的字符起点寻找第n+1行；找到错误段落后又把错误范围反写进稳定行表，导致后续行的UUID顺序虽未改变，level、编号、字体、颜色和背景条却读取到别行数据。
- 拼音候选会在确认前多次改变当前行长度，因此比英文输入更容易连续污染后续行；本次按同一根因处理，不再为拼音另加刷新补丁。
- `NodeMarkdownLegacyRowRangeIndex`成为旧管线唯一文字坐标入口：稳定行范围保持不动，当前行长度按累计字符差修正，后续行起点只在查询时整体平移。
- 协调器的焦点定位、活动行内容读取、局部样式、附件源码恢复、滚动和局部重画统一读取有效范围；自绘编号、背景条和下划线也复用同一套二分行定位。
- 局部渲染禁止把临时解析出的范围写回稳定行表。普通字符输入期间只改变当前行的临时偏移，不再污染第n+1行及其后的坐标。
- 真正执行全文样式重建时，以当前文本一次性建立新的稳定行表，并同时清除协调器和文本视图两边的临时偏移，避免同一个字符增量被重复计算。
- Mac Debug和iPhone Simulator Debug构建通过。

# 821 NodeMarkdown扁平文件与内存树索引

- NodeMarkdown的CSV格式保持原样，不给正文行增加任何索引字段，不改变教案、随堂笔记、完成清单和同步协议的兼容性。
- 新增独立`NodeMarkdownDocumentIndex`内存结构层。打开文档时按UUID、level和行序一次扫描，建立`UUID -> 行位置`、重复UUID位置、父节点、直接子节点、完整子树范围、所属H3以及全部H3包范围。
- UUID定位、父子和所属H3查询为直接字典或数组访问；任意Node包与H3包的结束位置在建索引时一次算好，右键删除、剪切和粘贴不再临时向后扫描全文。
- 新旧NodeMarkdown管线共用编辑器持有的同一份结构索引。普通字符输入只更新当前Node正文和H3修时，UUID、level和行序未变时不重建索引。
- 回车、跨行删除、Tab、Shift+Tab、多行结构修改、清空节点、Node包增删、剪切粘贴、母本删除和外部文件覆盖后才校准或重建结构索引。
- 旧管线活动行定位修正为二分索引优先。原实现虽然已有二分函数，却仍先从文章开头逐字统计换行，导致文档后部输入位置查询为`O(n)`；现在全文扫描只保留为索引异常时的兜底。
- H3包签名、H3包范围和修时传播统一复用结构索引；多Nodes修改不再为每个Node重复向前寻找所属H3，也不再反复通过UUID扫描全文。
- 焦点切换的脏包追踪使用内存索引定位原Node及所属H3，避免每次进入或离开一行都进行线性查询。
- 索引只存在于内存，不参与Codable、CSV写入和跨设备同步；关闭文档后自然释放。
- Mac Debug和iPhone Simulator Debug构建通过。

# 821 PDF固定配色与洗教案图片尺寸残留清理

- NodeMarkdown PDF导出不再读取导出时系统当前的深浅外观。文档设置为固定浅色时，系统语义默认字色被解析为黑色，系统背景固定为白色；夜间导出也不会再把默认文字变成白色。
- 文档设置为固定深色时，PDF按深色外观解析系统语义颜色，系统背景固定为黑色；固定浅色、固定深色分别形成确定的静态导出颜色。
- 文档设置为“跟随系统”时仍按既有导出约定使用浅色，避免同一文档因导出时刻不同而得到不同颜色。
- TextKit主PDF导出、HTML/WebKit PDF回退导出和TeachingPrint回退导出统一使用同一份文档导出配色规则，公式、正文、编号和背景不再各自读取不同外观。
- “洗教案”在现有母本清洗阶段增加旧图片尺寸残留识别：去掉去除首尾空白后以`height`开头、以`}`结尾的整行；多行Node只删匹配行，整Node只有该残留时删除该Node。
- 洗教案报告新增“清理图片height残留行”计数，便于核对真实清理数量。
- Mac Debug和iPhone Simulator Debug构建通过。

# 820 H3脏包与新包状态机恢复

- 重构随堂笔记H3包追踪。旧实现只分类某次编辑推导出的候选H3；层级变化、包边界变化或活动行身份丢失后，旧H3可能无人注销，真正修改的包也可能无人重新分类。
- 每次文档结构或Node提交后统一建立当前H3事实表：删除或降级为非H3的UUID立即从脏包、新包和基线中移除；当前所有H3再按来源状态和开课基线统一分类。
- 已有完整`SourceID + SourceFile`的H3包始终用整包语义签名与开课基线比较。标题、正文、层级、子Node增删或包边界改变都会进入脏包；内容恢复到基线时自动退出脏包。
- 新包统一定义为“当前仍是H3、标题非空、SourceID和SourceFile同时为空”。空标题H3、非H3以及只有一半来源信息的损坏节点不再误入新包。
- 新包名单仍按文档顺序长期维护；H3改成其他层级、删除H3或新包入库补齐来源后立即移出。
- 插入教案等外部磁盘写入后的刷新不再重新建立整篇基线。已有脏包的开课基线被保留，新插入且已绑定母本的H3只为自身建立干净基线。
- 同步预览、界面名单、真正入库和关闭前最终磁盘复核统一使用同一个新包定义，避免界面没有新包但下课仍被空H3阻塞。
- 手动更新不再无条件清空全部本地队列。只推进真正同步成功包的基线并移除成功项；失败包和未选择的新包继续保留。
- Mac Debug和iPhone Simulator Debug构建通过。

# 819 NodeMarkdown旧管线拼音编号时序修复

- 修复#818局部行索引改造后，拼音候选期间下方编号再次跳动的问题。
- 查明AppKit会在`setMarkedText`改变文字后、协调器收到`textDidChange`之前立即绘制；这一帧已经是新文字长度，但自绘编号仍持有上一帧的临时位移。
- 将拼音组合行和字符位移下沉到`NodeMarkdownStyledTextView.setMarkedText`，在输入法改变文字的同一调用内立即更新，不再等待后续代理回调。
- 每次候选变化后只要求TextKit完成当前可见区域布局，再重画可见编号、背景条和下划线；不扫描全文、不重建全文行表，也不修改Node层级。
- 拼音确认时同样先完成可见区布局，消除旧编号Y坐标残留。
- Mac Debug和iPhone Simulator Debug构建通过。

# 818 长文本输入热路径局部化

- 重新核对NodeMarkdown旧管线和普通Markdown TextKit2在大文本后部输入更卡的现象，确认问题同时来自后续行坐标平移、整页重画、重复装饰绘制、同一活动行反复上报、普通Markdown块级解析和高频全文绑定快照。
- NodeMarkdown旧管线启用非连续布局，TextKit只保证可见区域和光标附近完成布局，不再为后部光标主动铺开整篇文档。
- 连续在同一Node内输入时，不再复制并平移后面所有行的绝对范围；改为保存当前编辑行和累计字符位移。只有离开该行或发生回车、跨行删除、Tab、Shift+Tab等结构操作时，才重建一次稳定行坐标。
- 行号和level查询统一改为二分查找，并支持临时字符位移，文尾查找不再从第一行逐条寻找。
- 拼音候选期间不再每一帧重建或平移全文行坐标；候选文字使用临时位移，确认中文后才按最终结果重建一次。
- 旧管线删除`setNeedsDisplay(textView.bounds)`整页重画，只让当前行布局失效并刷新可见区域；编号和背景装饰从两次绘制合并为一次。
- 连续普通字符输入不再给当前整行反复重设字体和段落属性，进入源码行时设置一次，后续字符使用`typingAttributes`；结构操作和失焦渲染仍执行完整行处理。
- 活动行没有变化时，不再向NodeMarkdown父页面发送回调，因此不会反复结束和开始H3跟踪、刷新脏包列表、写活动行状态或触发SwiftUI更新。
- 普通Markdown TextKit2新增单行输入事务。普通字符只重新解析和设置当前行，不再扫描相邻Markdown块；回车、跨行删除等结构操作才进入块级处理。
- 普通Markdown全文绑定快照改为输入停止0.35秒后合并执行，连续输入期间不断取消旧任务，不再每约0.08秒提取一次整篇文本；结构操作仍快速落盘。
- Mac Debug和iPhone Simulator Debug构建通过。

# 811 图片资源所有权与无损事务重构

- 完整审计NodeMarkdown图片的插入、正文引用、删除撤回、新包暂存、入库迁移、保存同步、改名和关闭流程，确认旧实现存在单份随堂笔记扫描整章`Pic`目录并自动删除的越权风险。
- 正式教案图片归章教案所有。随堂笔记和其他引用文档只有读取、引用权限，保存或同步任何单份文档都不再扫描、删除母本`Pic`中的所谓“未引用图片”。
- 删除目录级`garbageCollectManagedImages`入口。保存与同步现在只恢复正文仍引用的暂存图片，禁止根据单份文档推断全工作区图片无用。
- 图片链接从正文删除时，只有当前文档自己建立、且位于对应新H3包临时目录中的图片可以进入撤回暂存；正式母本图片和其他文档的临时图片均不能被当前文档移动。
- 撤回暂存从全局共享改为按NodeMarkdown文件路径生成稳定目录，不同学生、不同文档和不同窗口不再相互清空恢复资源。
- 暂存事务改为“复制图片到暂存 -> 原子写恢复清单 -> 删除原图”。任一步失败都保留原图；恢复清单写入失败不再被静默吞掉后仍报告删除成功。
- 打开编辑器不再直接清空图片暂存，而是读取磁盘文档并优先恢复仍被引用的图片。普通窗口关闭的异步保存未确认成功前不清空暂存；明确完成保存和同步的关闭流程才可整理本文件的暂存。
- 图片导入改为原子覆盖，不再先删除旧目标再移动新文件；写入后检查文件非空、大小合规且能够真实解码，损坏的同名文件不能被当成可用图片复用。
- Markdown图片解析从“遇到第一个右括号就结束”的正则改为括号平衡扫描，支持目录名或文件名中的括号、转义字符、尖括号路径和`{width=...}`。
- 只读扫描当前数据：正式`img-哈希`格式已有10个图片文件缺失，形成12条断链并影响4份文档；这些文件在当前工作区、回收站和图片暂存中均未找到。本次只修复继续误删的机制，没有伪造图片、删除引用或擅自改动历史数据。
- Mac Debug和iPhone Simulator Debug构建通过。
- 更新版本号为`V1.2607170744`。

# 810 Markdown编辑背景档案稳定与公式字母扩展

- 查明普通Markdown自动保存时，档案目录监听器会收到文件写入、原子替换和属性更新等多个连续事件。虽然档案页已经能用可见签名识别“文件列表没变”，每次事件仍会切换`isLoading`，进度条反复插入和消失，导致背后整张列表上下跳动闪烁。
- 档案目录监听增加0.18秒合并窗口，同一次原子保存产生的一批文件系统事件只触发一次目录核对。
- 档案加载分为“首次/进入目录”和“后台静默刷新”。只有首次和真正换目录时显示加载状态；Markdown写盘引发的后台核对不改`isLoading`、不显示进度条、不替换签名相同的列表。
- 后台刷新增加目录身份校验；如果读取期间用户已进入另一目录，迟到的旧结果不得把页面带回原目录。
- 扩展统一公式字符规范化表，覆盖24个现代希腊字母的大小写、终结sigma和常见变体；新增APL omega `⍵`、微符号`µ`、欧姆符号`Ω`、`ℏ`、`ℓ`、`℘`、`ℑ`、`ℜ`、`ℵ`及`ℕ/ℤ/ℚ/ℝ/ℂ`等常用字母符号。
- `⍵`明确转换为`\\omega`，不再依赖字体是否恰好含有APL字形。中文继续包装为`\\text{...}`，不改动原有中文公式支持。
- 普通Markdown TextKit2公式渲染改为与NodeMarkdown新旧管线共用`NodeMarkdownFormulaLatexNormalizer`，删除只替换少数全角标点的局部规则。
- 独立规范化测试覆盖全部现代希腊字母、`⍵`、`µ`、`Ω`、`ℕ`和中文；并核对SwiftMath内置测试确认新增LaTeX命令可被解析。
- Mac Debug和iPhone Simulator Debug构建通过。
- 更新版本号为`V1.2607170646`。

# 809 NodeMarkdown旧管线拼音期自绘行索引

- 对比正常渲染和拼音候选期截图后，确认正文字体、字号和颜色并未被改成其他level；真正错乱的是自绘编号、H1/H2/H3背景条和下划线。
- 查明时序：AppKit的`setMarkedText`先改变NSTextStorage长度并可能立即发起绘图，`textDidChange`稍后才到达协调器并重建`rowCharacterRanges`。在这个窗口内，自绘层使用旧行首位置去精确匹配已经后移的新行，匹配失败后把后续行当成第7级正文，因此背景条消失、编号变成圆点或留在上一行。
- 自绘层现在每次绘制前先校验缓存行索引是否仍覆盖当前NSTextStorage全长。长度一致时继续使用高速缓存；候选文字导致坐标变化时，从当前存储建立一份只读临时行索引，不等待代理回调。
- 编号、背景条和下划线在同一次绘制中共用这份行索引，level始终按当前行序号取自`nodeRowLevels`，不再用上一帧字符位置。
- `NodeMarkdownStyledTextView` 覆盖`setMarkedText`和`unmarkText`，每次候选替换前后都让整个可见自绘区失效，清除旧Y坐标上残留的编号和背景条。
- Debug模式校验临时行索引必须从0开始、连续且覆盖全文。
- Mac Debug和iPhone Simulator Debug构建通过。
- 更新版本号为`V1.2607162300`。

# 808 NodeMarkdown旧管线拼音组合排他边界

- 继续检查“英文正常，拼音候选时下方行仍异常，定字后恢复”，确认#806只暂停了协调器内部的样式队列，但SwiftUI `updateNSView`仍会在判断是否应延迟外部正文同步之前，先更新文档样式、搜索状态、行元数据和自绘层级。
- `updateNSView`现在以marked text为最高优先级边界：只要输入法组合未提交，整个SwiftUI外部更新入口立即返回，不得先改变任何渲染状态。
- `applyStyle`增加统一的marked text禁入规则。无论调用来自搜索、选区、外部回刷还是旧异步任务，候选期都不能执行字体、段落、公式、图片或全文样式处理。
- 拼音每次变化不再重复向父页发布“当前行”。父页不再因同一行的候选文字变化而反复结束/开始H3跟踪、刷新包列表和触发SwiftUI重算。
- 重构旧管线的底层构造：删除先建`NSTextContentStorage/NSTextLayoutManager`、随后又被`NSTextView`暗中丢弃的隐式流程；改为显式且唯一的`NSTextStorage -> NSLayoutManager -> NSTextContainer -> NSTextView` TextKit 1对象链。
- Debug构建新增存储和布局器对象身份断言，防止旧管线再次被无意接回TextKit 2或变成隐式对象替换。
- Mac Debug和iPhone Simulator Debug构建通过。
- 更新版本号为`V1.2607162250`。

# 807 NodeMarkdown旧管线拼音期自绘装饰坐标

- 根据候选拼音存在时的实际截图继续分析：文字本身及确认中文后的level均正常，错位集中在编号标志、背景条和其他`NodeMarkdownStyledTextView`自绘内容。
- 查明拼音marked text改变当前行宽度或换行高度后，TextKit会延迟重排下游文字；自绘编号和背景条却使用`withoutAdditionalLayout`读取旧几何坐标，因此出现n+6行的编号画到n+5行附近的现象。
- 不恢复候选期的正式样式渲染。新增候选文字专用装饰刷新：先要求`NSLayoutManager`仅完成当前可见区域的布局，再重画可见的编号、背景条、高亮和下划线。
- 刷新不修改文字属性、Node level、UUID、草稿或行元数据，也不扫描不可见的文档内容；英文和数字的正常增量路径不变。
- Mac Debug和iPhone Simulator Debug构建通过。
- 更新版本号为`V1.2607162107`。

# 806 NodeMarkdown旧管线拼音候选期隔离

- 继续根据“拼音待选时下方显示错乱，确认中文后恢复”的精确现象检查旧管线，确认最终中文和Node元数据没有损坏，问题只发生在AppKit marked text尚未提交的组合期。
- 旧实现在每次拼音变化时仍会重建正式编辑环境，切换源码行、更新草稿，并向只占行内一小段的候选拼音写入段落样式。段落属性被局部改写后，TextKit会重排后续段落，造成候选期下方层级、字号和编号短暂错乱。
- 新增独立的拼音组合事务。候选期只根据当前NSTextStorage维护UTF-16行坐标和组合所在行，不切换Node、不提交草稿、不写入段落样式、不触发下游行渲染。
- 拼音组合开始时立即取消已排队的增量和全文样式任务，并提升渲染版本；旧任务即使已进入主队列也会被判定过期，不能在候选窗口存在时重画全文。
- 拼音定字后才退出组合事务，按最终中文一次性重建行坐标、更新当前Node草稿和输入属性。
- 删除旧的marked text局部段落样式方法，避免后续代码再次误用。
- Mac Debug和iPhone Simulator Debug构建通过。
- 更新版本号为`V1.2607162057`。

# 805 NodeMarkdown旧管线拼音组合事务

- 英文输入稳定后继续单独检查拼音路径，确认输入法组合开始时第一次字母会留下普通按键`PendingEdit`；后续marked text由AppKit直接替换，确认中文时旧代码仍使用第一字母的长度和位置更新范围，导致n+1以下重新错位。
- marked text一旦开始立即丢弃普通按键事务及其字符差、行差和删除分类，拼音组合不再与英文按键事务混用。
- 拼音显示期间每次按当前NSTextStorage重建行范围，再确定活动行、输入层级和marked text属性。
- 拼音确认成中文时，无论AppKit是否提供普通替换回调，都无条件按确认后的全文重建行范围，之后才确定焦点和源码行。
- 修正无`PendingEdit`回调的影响范围：以前默认返回当前行及上下邻行，导致输入法确认天然触碰n-1和n+1；现在只返回当前行，不再伪装成多行或结构修改。
- Mac Debug和iPhone Simulator Debug构建通过。
- 更新版本号为`V1.2607162047`。

# 804 NodeMarkdown旧管线输入渲染稳定性重构

- 查明严重错乱不是Node level本身变化，而是普通字符输入后把“需要刷新段间距的样式脏行”误用于更新文字行范围。第n行变长后，n+1行的旧起点落入第n行，两个行号因而指向同一段文字；随后n+1的level属性覆盖第n行，并使后续编号和样式连续错位。
- 新增独立`NodeMarkdownLegacyRowRangeIndex`，把NSTextStorage的UTF-16文字范围与样式刷新范围彻底隔离。普通字符编辑只重算实际编辑行，并让后续行连续平移。
- 新行范围必须逐行连续、不得重叠或越界，并且必须与NSString真实行范围一致；校验失败时拒绝增量结果并从当前正文重建，不再带着坏索引继续渲染。
- 删除正文开头“前8个字符可能改变层级”的旧前缀探测。显示正文已不携带`### `或`- `，普通打字不能再触发层级与段间距判断。
- Tab、Shift+Tab及结构编辑需要连带刷新下一行段间距时，只扩大样式目标，不修改下一行UUID、level或文字范围。
- 调整输入事务顺序：先消费编辑、更新行范围，再判断当前行和设置输入属性。行尾输入不会因旧范围被误判为n+1行。
- 拼音marked text期间AppKit可能不调用普通替换代理，因此每次组合变化先从当前正文重建行范围，再确定活动行；拼音过程不再让后续编号和层级漂移。
- 独立范围索引测试覆盖行中输入、行尾输入、删除、中文UTF-16及100次跨行连续输入，所有增量结果均与全文重建一致。
- Mac Debug和iPhone Simulator Debug构建通过。
- 更新版本号为`V1.2607162039`。

# 803 NodeMarkdown末行编号与成课唯一来源

- 修正旧NodeMarkdown管线最后一行的编号标志垂直位置。旧算法把行尾换行符形成的额外排版片段计入文字矩形，导致编号落到下一行高度，看起来像一个不存在且不可进入的空行。
- 首行文字矩形现在先剔除首尾换行符，只使用第一个真实文字片段计算编号的Y轴中线；不删除真实Node，也不改变Enter和末行编辑行为。
- 开课流程不再向成课JSON自动添加两小时课程；开课只负责授课资料、完成清单、随堂笔记和当天上课信息。
- 删除已经没有调用者的自动成课方法及自动整点算法，避免以后从别的入口误用。成课唯一来源改为排课页面将当前时刻之前的排课转入历史。
- Mac Debug和iPhone Simulator Debug构建通过。
- 更新版本号为`V1.2607162017`。

# 802 NodeMarkdown旧管线完整Node编辑暂存

- 按用户回退后的代码重新检查旧TextKit管线，没有沿用新TextKit2管线改动。
- 确认去掉`### `、`- `等显示源码前缀后，Content字符串已经不再携带层级；旧管线仍在输入中频繁把整篇Content回送父层异步重建Nodes，元数据缺行时又用第7级`.empty`补齐，导致H1-H6、正文和注释一起退化为正文层级。
- 新增旧管线专属`NodeMarkdownLegacyEditingNodeDraft`，进入编辑行时一次性暂存Node UUID、level、Content、SourceID和SourceFile；UUID成为提交主键，层级与Source不再从Content反推。
- 普通打字、拼音组合和行内删除只更新当前编辑Node暂存，不再每个字符触发整篇Node解析。当前行及其他行的12级样式保持原有元数据。
- 切换到另一行或ESC失焦时，先读取当前行最终Content，再按UUID把暂存原位提交到`NodeMarkdownDocument`；提交完成后才通知活动行变化。
- 父层新增旧管线单Node提交入口，只修改目标Node的level、Content、SourceID和SourceFile，其他Nodes的UUID、层级、Source和顺序不变。
- Tab与Shift+Tab在普通行只修改当前暂存和本地行元数据，保持源码编辑态；失焦时统一提交。受保护H3仍保持原规则：Tab在后面建立H4空行，Shift+Tab在前面建立H2空行，不允许改变H3自身层级。
- Enter、跨行Backspace、多行选择和撤销等结构操作，在行数组变化前先提交当前暂存，再用完整正文与行元数据执行结构事务，避免旧行号在变化后指向另一Node。
- 单Node提交调用现有Node修时和包变化逻辑。H3自身或H3包内子Node变化会更新所属H3修时，并进入原有脏包/新包记录流程；H1、H2不伪造H3归属。
- 旧管线活动编辑期间拒绝缺UUID或重复UUID的第7级占位元数据覆盖完整行身份；SwiftUI回刷时使用协调器确认后的层级，不再直接使用可能迟到的外部数组。
- 元数据补行不再写入固定第7级`.empty`，而是为真正新增行生成新UUID并继承邻近层级，防止无源码前缀条件下永久丢层级。
- 使用实际包含1至12级的`系统/教案/测试教案/测试章节.csv`强制启动旧管线完成可视验收：H1、H2、H3背景条、正文多级缩进、编号标志、字体大小和颜色均按设置显示，证明旧管线层级已注入渲染器。
- 后续根据上课截图定位到具体数据：母本和学生-C的相同H3包均保留完整层级，学生-D的随堂笔记则有87个数据节点全部已经落盘为第7级正文，同时丢失H3来源。上课显示异常并非渲染器未使用level，而是该随堂文件本身已经被旧解析兜底改坏。
- 找到整篇退化的根因：编辑正文已不含层级前缀，但解析器在正文行数与行元数据短暂不一致时仍尝试从正文反推，识别不到后把所有行降为第7级。现在不同步时优先按UUID沿用原Node层级，其次保留原位置层级，禁止整篇降级。
- 使用学生-D的完成清单、同步基线和母本H3边界精确确认6个既有H3包；恢复87行的UUID、层级和H3来源关系，同时保留随堂笔记中已经修改过的Content。恢复前原文件备份到`系统/暂存/层级恢复`。
- Mac Debug和iPhone Simulator Debug构建通过。
- 更新版本号为`V1.2607162004`。

# 798 NodeMarkdown新管线Mac首次载入空白修复

- 查明Mac端TextKit2新管线空白并非文字颜色或排版问题，而是首次载入的同步边界错误。
- `NSTextView`挂入窗口后可能自动开始编辑。旧逻辑把“已经获得焦点”误判成“存在尚未确认的本地正文”，因此拒绝把随后从磁盘读出的正文写入空的原生存储。
- 同一误判还会拒绝载入行层级、H3保护和编辑行信息，即使正文偶然出现，也可能全部按正文层级显示。
- 新增统一的`hasUnacknowledgedLocalText`判定。只有原生编辑器确实发布过正文、且SwiftUI尚未回送确认时，才保护本地正文不被外部快照覆盖。
- 焦点和输入会话状态不再参与正文与行元数据的载入决策；它们只负责输入状态、焦点报告和自动保存时机。
- 保留原有并发保护：用户刚输入但尚未被绑定确认的正文仍然优先，迟到的旧快照不能覆盖。
- 核对iPhone实现后确认，目前iOS在选择TextKit2时仍使用系统纯文本`TextEditor`兜底，并未运行Mac的新TextKit2对象链；因此能显示正文，但不具备新管线完整渲染能力。
- Mac Debug与iPhone Simulator Debug最终构建通过。
- 更新版本号为`V1.2607160936`。

# 797 统计与排课的iPhone响应式布局

- 统计首页默认页面改为“排课”，Mac、iPhone和iPad使用同一默认规则。
- 统计页顶部在iPhone上改为两行：标题和操作按钮位于第一行，机构、成课、排课、周表位于第二行，避免分段按钮与刷新、添加、导出互相挤压。
- 授课首页顶部主导航在窄屏改为横向滚动，按钮保留正常点击尺寸，不再通过压缩造成文字和图标错位。
- 排课与周表顶部在iPhone上拆为月份导航行、导出与选学生行；Mac继续保持原有单行工具栏。
- iPhone的学生选择从窄小浮窗改为系统底部弹页，支持中等和全屏两种高度；Mac仍使用浮窗。
- 排课表和周表在iPhone上获得独立的可滚动视口，课程记录区也限制在手机可用高度内，防止超高表格把整个统计页面和底部导航顶出屏幕。
- 机构列表增加手机纵向行布局，名称、备注、课费和月度统计分层展示；Mac继续使用横向表格式布局。
- 课程编辑、机构编辑和机构数据弹页只在Mac保留固定最小宽高；iPhone使用屏幕可用宽度。课程时间快捷按钮可横向滚动，机构数据表可横向查看。
- 修正外部Markdown窗口文件把`AppKit`写在平台判断外的问题，使iOS target可以完整编译。
- Mac Debug和iPhone Simulator Debug均构建通过。CoreSimulator运行服务在启动设备时自行进入关闭状态，因此本轮未取得模拟器运行截图。
- 更新版本号为`V1.2607160920`。

# 796 NodeMarkdown TextKit2性能与完整回归第四阶段

- 完成TextKit2新管线四阶段重构的最后阶段：高频输入性能、局部状态刷新、文末移动和运行时一致性验收。
- 新增与唯一`NSTextStorage`等长的源码只读快照。它不是第二个编辑器，也不接受外部直接写入；原生字符事务按受影响范围投影快照，公式和图片附件的显示切换不改变快照。
- `documentString()`从每次遍历整篇富文本、枚举附件属性并重建源码，改为直接返回当前源码快照。只有系统未提供普通替换范围的异常路径才从唯一存储重建一次。
- 中文输入法的每次marked text变化继续由系统独占，但源码快照会按原生替换范围同步；拼音确认后沿用当前行字符增量布局，不再默认重建全文布局。
- 删除正文已无层级前缀后遗留的“每行前八个字符可能改变前缀”判断。普通字母和普通删除不刷新整行；只有换行或Markdown/HTML行内语法符号变化才刷新当前行。
- 搜索词本身改变时仍全篇重算搜索标记；只改变当前搜索项、活动行或编辑行时，仅刷新旧行和新行，不再重新给整篇文档挂载公式图片和样式。
- 上下方向键改为按Node布局移动，支持进入和离开文末零长度空Node，不再由`NSString.lineRange`忽略尾行。
- 文末空Node输入属性从该Node的真实层级取得，不再错误退回正文第七级样式。
- 新增独立`NodeMarkdownTextKit2Validation`。每次普通输入做常数级轻校验；全文载入、Enter、Backspace、撤销和恢复做深校验，覆盖唯一存储身份、源码长度、行范围连续性、文档覆盖、选择范围和附件源码还原一致性。
- 清理被拒绝编辑和输入法组合阶段的待处理状态，禁止下一次输入误用上一笔操作的删除分类、范围或行元数据。
- 四阶段重构至此全部完成。Mac Debug最终构建通过，只有工程既有的AppIntents无依赖提示，没有Swift编译警告或错误。
- 更新版本号为`V1.2607160745`。

# 795 NodeMarkdown TextKit2焦点事务与同步边界第三阶段

- 完成TextKit2新管线四阶段重构的第三阶段：点击焦点、结构命令、撤销恢复与SwiftUI外部同步边界。
- 新增独立`NodeMarkdownTextKit2FocusTransaction`。结构编辑后的焦点不再只保存易漂移的全文字符位置，而是保存Node UUID、Node内偏移和选择长度。
- Enter、行首Backspace、Tab、Shift+Tab、受保护H3旁新建行、撤销和恢复统一通过同一焦点事务落点；样式刷新和SwiftUI状态回写只能恢复该事务，不能另选文首、文末或邻行。
- 鼠标点击和普通方向键选择继续完全交给NSTextView/TextKit2原生命中；真实用户选择一旦发生，旧结构焦点事务立即结束，不会把用户拉回旧位置。
- 新管线增加本地编辑会话和单调递增的本地修订序号。本地正文尚未被SwiftUI确认时，迟到的旧正文、旧行元数据和旧编辑行状态不得整篇反写TextKit2。
- SwiftUI只有回送与当前最新本地正文一致的快照时，才算确认该修订；较旧修订的回送不能确认或覆盖较新的输入。
- 修正尾部空Node缺失：正文以换行结尾时，布局表现在会建立正式的零长度尾行，并携带自己的层级、UUID、段间距和H3保护状态。
- Enter和行首Backspace不再用`NSString.lineRange`猜文末空行，而是直接按Node布局的`contentRange`拆分、合并，消除文末空行被误认成上一行的根因。
- Mac Debug构建通过。后续还剩最后一阶段：性能收口、焦点与中英文输入完整回归验收。
- 更新版本号为`V1.2607160734`。

# 794 NodeMarkdown TextKit2原位渲染与中文输入第二阶段

- 完成TextKit2新管线四阶段重构的第二阶段：原位样式、公式图片附件切换和中文输入法保护。
- 全量样式刷新不再创建一份新富文本后覆盖正文；现在先在唯一`NSTextStorage`中等长恢复附件锚点，再原位重置属性并逐行应用样式，字符总数和换行位置保持不变。
- 单行进入或退出源码状态只处理目标行：恢复该行公式图片首字符、清理该行旧属性、重新应用该行源码或渲染样式，不再以“单行刷新”的名义复制和回写整篇文档。
- 公式和图片继续使用源码全长范围承载附件，挂载时仅把首字符换成U+FFFC，恢复时仅把该字符换回源码首字符，始终是`1 -> 1`原位操作。
- 快捷输入只替换命中的目标范围，不再复制并写回整篇富文本。
- 拼音marked text存在期间，`textDidChange`不运行快捷输入、不重建行、不刷新样式、不挂载附件，也不向SwiftUI发布未确认的拼音；系统确认中文后再按一次普通提交处理。
- 输入法组合期间外部同步和全量样式刷新主动避让，marked text只通过`typingAttributes`取得字体颜色，不再直接修改组合文本属性。
- 删除已经无调用者的任意属性修改入口，防止后续重新从该入口重写输入法管理的文本。
- 后续还剩两阶段：焦点结构命令与同步边界、性能及完整回归验收。
- 更新版本号为`V1.2607152314`。

# 793 NodeMarkdown TextKit2唯一正文存储第一阶段

- 开始按四阶段重构NodeMarkdown新管线，本阶段只解决唯一正文存储和基本输入，不提前混入公式图片原位渲染、输入法保护及同步版本边界。
- 确认焦点锁在文首的根因是新管线先写`NSTextView.textStorage`，随后又给`NSTextContentStorage.attributedString`赋值；后一次赋值会解除TextView正在编辑的存储，使排版层仍有全文而原生编辑层变成空文本。
- 新管线现在显式建立唯一`NSTextStorage`，并按`NSTextStorage -> NSTextContentStorage -> NSTextLayoutManager -> NSTextContainer -> NSTextView`连接。显示、点击、选择、英文输入和中文输入共同使用这一实例。
- `documentString`、富文本读取、全文载入、范围替换和属性修改全部改为操作唯一存储，删除`NSTextContentStorage.attributedString`双写入口。
- 自定义范围替换不再复制并回写整篇富文本，而是在TextKit2编辑事务中只替换目标范围；外部载入文档仍允许明确的全文替换。
- Debug构建增加存储、内容存储、布局管理器和TextView身份断言，一旦对象链再次分裂立即停止，不允许以“能显示但不能编辑”的状态继续运行。
- 给TextKit2文本容器和TextView提供稳定的初始尺寸，排除零宽初始排版对首次点击命中的干扰。
- 后续还剩三阶段：原位样式与中文组合输入、焦点结构命令与同步边界、性能及完整回归验收。
- 更新版本号为`V1.2607152239`。

# 791 旧管线公式图片附近结构编辑焦点事务

- 修复旧管线在公式或图片附近回车后跳过新行、行首退格合并后跳到文尾的问题。
- 根因是公式和图片以附件占位符显示，而行身份回写触发的全量样式刷新会为恢复附件源码替换整篇NSTextStorage；AppKit随后把插入点推到替换范围末尾。
- 删除全量恢复时的整篇文本替换，也删除逐行恢复时的整行文本替换。附件恢复现在只把U+FFFC占位符原位换回源码首字符，始终是单字符等长替换，不改变任何行范围。
- 样式应用和附件挂载全过程标记为程序内部更新，渲染产生的选择变化不能进入活动行和焦点状态。
- Enter以新Node的UUID和Node内第0位建立一次性焦点事务；行首Backspace以上一Node的UUID和原内容末尾建立事务。事务跨过本地编辑、SwiftUI正文与行身份回写及样式重排，下一次真实用户操作时释放。
- 撤销和恢复快照统一保存附件还原后的真实源码，不再把附件占位符写入撤销历史。
- 更新版本号为 `V1.2607152212`。

# 790 NodeMarkdown双管线焦点事务重构

- 新管线删除手算点击坐标和字符位置的焦点入口，点击先完整交给NSTextView/TextKit2原生命中，再根据原生选择结果进入对应Node的源码状态。
- 新管线把Enter、Backspace、Delete、Tab、Shift+Tab和Esc正式接入NSTextView响应链，修复命令处理器存在但从未被调用、焦点长期锁在文首的问题。
- 删除新管线Enter后的异步二次焦点修正；回车命令本身一次性决定新行行首，后续滚动和渲染不得再改光标。
- 新旧管线的正文与行元数据改为一次发布，禁止SwiftUI中间出现“新正文+旧UUID/层级”的无效快照并触发整篇外部刷新。
- 解析器现在接受编辑器为新行生成的UUID；Enter产生的新Node从出生、解析到落盘保持同一身份，不再解析一次更换一次UUID。
- 旧管线行内Enter和行首Backspace完成后，不再被后续身份变更和整篇样式刷新推到文末；H3行首保护和Esc失焦规则保持不变。
- 更新版本号为 `V1.2607152043`。

# 789 NodeMarkdown打开时恢复真实层级

- 修复NodeMarkdown进入新旧管线后所有行暂时按正文层级显示、只有关闭瞬间才恢复真实层级的问题。
- 根因是#788拆分正文与层级后，文件加载只初始化了正文草稿，没有同步初始化每行的层级、UUID和来源元数据。
- 文件读取完成后，正文与行元数据现在作为同一份编辑快照同时交给编辑器，不再允许正文先进入、层级稍后补入。
- 旧管线在正文不变但行元数据改变时立即执行完整样式刷新；新管线把完整行元数据纳入排版快照，层级、身份或H3保护状态变化都会触发重新排版。
- 更新版本号为 `V1.2607152024`。

# 788 NodeMarkdown双管线去除编辑前缀

- NodeMarkdown新旧管线的编辑缓冲区改为只保存 Node `Content`，不再把 `### `、`- ` 等 `Prefix` 拼进 `NSTextStorage` 后设为透明。
- Node 层级、UUID、SourceID、SourceFile 和 H3 保护状态由行元数据独立承载；行样式、缩进和十二级编号标志只从元数据绘制。
- 点击与选择的可编辑起点现在就是正文行首，不再存在透明前缀禁入区；正文真的以 `# ` 或 `- ` 开头时也不会被当成层级。
- 回车按正文位置分割 Node，新行生成新 UUID 并继承层级，焦点停在带下来内容之前。
- 行首 Backspace 合并前后 Node，删除后一 Node 身份，焦点停在原前一行内容末尾；已关联母本的 H3 继续禁止这项操作。
- Tab/Shift+Tab 只改行元数据的 level，不替换文字也不重算焦点；受保护 H3 上的 Tab/Shift+Tab 仍分别新建 H4/H2 空 Node。
- 新旧管线的撤销快照同时保存正文和行元数据，避免撤销后层级、UUID 与行错位。
- CSV 的 `Prefix` 列和旧数据读写协议保持不变，只在文件边界按 level 生成，不影响已有数据。
- 更新版本号为 `V1.2607152010`。

# 787 学生UUID改名事务

- 学生改名统一以学生 UUID 作为身份，姓名只作显示值；成课 `LessonRecords.json`、排课 `排课.json`、周表 `周表.json` 都只更新同 UUID 的学生名，缺少 UUID 的旧记录不猜测。
- 改名同步迁移学生档案文件夹，并精确改名随堂笔记、学生信息、上课信息和完成情况文件；同步刷新 CSV 的 `[META_TITLE]`。
- 学生信息和上课信息的姓名内容优先按设置中的行 UUID 修改，旧数据只允许“姓名/学生姓名/name/studentname”精确回退，不再模糊匹配家长姓名等字段。
- 只改学生个人设置中实际使用的 `编号-学生名` 同步目录，并递归改名精确的问题 PDF、试卷 PDF、随堂笔记 PDF/HTML；全局默认同步路径和用户自建文件不动。
- 改名先检查空名、非法路径字符、忽略大小写的学生重名、档案缺失和所有目标冲突；不再覆盖目标，也不再在旧档案丢失时创建空学生目录。
- 文件内容、文件移动、学生设置和三份课单纳入同一改名事务；任意步骤失败会逆序恢复。上课、笔记或后台同步任务运行时拒绝改名。
- 更新版本号为 `V1.2607151944`。

# 786 授课教案右键管理

- 正确入口限定为“授课-教案”的教案管理页面，不修改“档案”页面的右键菜单。
- 根层的教案文件夹和进入教案后的章教案文件统一增加右键菜单，菜单只有“改名、删除”。
- 改名先填写新名称，再弹出影响确认；教案文件夹和章教案分别说明会影响的随堂笔记、完成清单、学生备份、模板、同步记录、图片目录和图片链接。
- 删除弹出专属确认，明确提醒移入回收站后，已有随堂笔记、完成清单和同步记录中的母本引用会失效。
- 存储层只允许对教案根层文件夹和其内部章文件执行这两个操作；章文件改名自动保留 `.csv` 扩展名，改名继续使用#785的完整关联迁移事务。
- 正在上课或笔记时禁止改名和删除教案。
- 更新版本号为 `V1.2607151733`。

# 785 教案与章教案改名事务

- 新增独立的 `TeachingLessonRenameService`；档案中对教案文件夹或章教案改名时，不再只移动一个文件，而是执行完整关联迁移。
- 章教案改名同步修改所有学生随堂笔记、完成清单、学生备份和教案模板中的 `SourceFile`，并同步移动同名图片目录、更新正文Markdown/HTML图片链接。
- 教案文件夹改名按路径前缀迁移全部章引用，同时改名学生完成清单、备份完成清单和中央教案模板文件；被改名CSV内部的 `[META_TITLE]` 同步更新。
- `系统/course-sync-baselines` 的 `SourceFile#SourceID` 键和 `系统/course-sync-conflicts` 中仍可操作的冲突记录同步迁移；历史日志、洗教案报告和回收站保持历史原貌。
- 教案改名后清除旧的插包撤回快照，避免撤回把旧 `SourceFile` 写回；正在上课或笔记时拒绝改名。
- 改名先扫描并检查全部目标重名，写盘、清撤回记录和文件移动组成同一事务；任一步失败时恢复原始文件字节、被清记录和已经完成的移动。
- Mac 构建通过；iOS 模拟器构建仍被工程既有的 `ExternalMarkdownTabWindowView.swift` 无条件导入 `AppKit` 阻挡，与本次改名代码无关。
- 更新版本号为 `V1.2607151601`。

# 786 洗教案人工待办表格化

- 洗教案的待人工处理项目改为结构化数据，逐条记录类型、学生、文件、H3标题、当前UUID、SourceID、SourceFile和未处理原因。
- 清洗报告中的“待人工处理”改为Markdown表格，不再只输出难以核对的项目符号；“保持原样的文件”也单独形成文件与原因表。
- Debug-功能按钮-洗教案完成后，页面直接展开全部人工待办和保持原样文件，不再只显示数量和报告路径。
- 已将本次 `20260715-153825` 的既有报告原地整理为表格，不重新执行清洗，不改动教案和随堂笔记。
- 更新版本号为 `V1.2607151541`。

# 785 功能按钮窗口尺寸恢复

- 修正 Mac 主窗口只设置最小外框、却不恢复当前异常小窗口的问题；现在按内容区统一要求至少 `720 x 520`。
- 如果系统记住了过小的旧窗口尺寸，进入功能按钮页时会立即扩展到可用尺寸，不再只显示标题栏和空白区域。
- Debug 功能按钮页、洗教案页和 SSC 整理页统一声明相同的最小内容尺寸，导航切换时不会重新收缩。
- 更新版本号为 `V1.2607151537`。

# 784 洗教案与全库H3身份重建

- 新增独立的 `TeachingLessonWashService`，不复用会删除上课收集包的旧SSC迁移器；Debug-功能按钮中新增“洗教案”入口和二次确认。
- 正式教案先全量扫描再统一处理；`上课收集/上课收集.CSV`只参与识别和报告，清洗过程绝不修改它。
- 重复H3 UUID按“有效随堂引用数量、教案路径、行号”稳定选择保留项，其余生成避开全库已有身份的新UUID。
- 每组重复H3的全部旧根节点信息和完整包JSON写入UUID暂存表，最后一列记录目标新UUID；暂存表不污染教案Content。
- 正式教案中H3以外的Node也执行全库唯一性整理；H3身份优先保留，和H3碰撞或彼此重复的普通Node获得新UUID。
- 旧图片乱码按真实Markdown/HTML图片引用和三行AI图片残留清洗：真实文件存在则保留，确认无文件的引用替换为单行`🌼{图片}🌼`。
- 公式后遗留的`LP_SOURCEFILE`旧公式图片关联注释统一清除，不改公式源码。
- 随堂H3按`SourceFile+SourceID`、旧映射、节点UUID、标题唯一、完整包唯一逐级认回正式母本；成功后同时统一根UUID、SourceID和SourceFile，并用清洗后的母本完整覆盖H3包。
- 同一随堂笔记中明确认回同一母本的重复H3只保留第一包；无法唯一判断或仍指向上课收集的项目保持原样并进入人工报告。
- 所有现有学生完成清单从新母本重新生成；能认回的旧完成项保留Status，清洗后随堂笔记中实际存在的H3强制标记完成，H1/H2按层级和标题继承状态；找不到正式教案文件夹时不写空清单。
- 写盘前验证正式教案全部Node UUID全局唯一，并对每份随堂笔记执行#783身份检查；无法通过的随堂文件保持原样。
- 清洗结果落盘到`系统/暂存/洗教案/`，包括H3 UUID暂存表和Markdown人工处理报告。
- 更新版本号为 `V1.2607151530`。

# 783 Node身份唯一性与编辑事务收口

- 新增独立的 `NodeMarkdownIdentityPolicy`，统一规定：编辑保留 UUID，新增生成 UUID，删除使 UUID 退出文档，剪切整体移动原 UUID，复制为全部节点生成新 UUID。
- 纯文本转 Node 时先预留编辑器明确传回的行身份；每个旧 UUID 在一次解析中最多消费一次，位置后备匹配不得抢用已预留或已消费身份。
- 行元信息数量与新文本行数不一致时判定为结构编辑后的旧快照，不再盲目按新行号套用，改由前后相同区和改变区的一对一规则恢复身份。
- 位置后备匹配禁止跨越 H3 边界；普通子节点不能再因行号变化成为 H3，H3 身份也不能再按位置流入普通正文。
- 带完整母本来源的受保护 H3 即使收到异常层级前缀，解析结果仍保持 H3，不允许 tab、删除或文本重组间接解除母本身份。
- 回车拆行时原行保留身份，新行获得全新 UUID；合并时保留主体行身份，被合并行身份退出；新 UUID 同时避开旧文档和本次解析已分配的全部 UUID。
- 剪切包继续携带包内全部 UUID 移动；粘贴和剪切回退前检查目标文档，发现任一 UUID 已存在则拒绝插入并保留待处理剪切包。
- 复制包的统一规则已收进身份策略：复制出来的每个 Node 使用新 UUID；复制 H3 清空来源并成为真正的新包，不伪装成同一个母本包。
- `NodeMarkdownFileManager.write` 成为统一写盘门卫：重复 UUID、非 H3 携带来源、完整 H3 链接的 UUID 与 `SourceID` 不一致时全部停止写盘；旧文件中只剩一半来源信息的 H3 允许暂存，由同步流程继续修复。
- 插包撤销不再把历史 Data 直接覆盖随堂笔记；快照先按 NodeMarkdown 读取并通过同一身份门卫，旧损坏快照不能绕过检查重新污染文件。
- 上课同步入口在读取随堂笔记后先做身份检查；重复 UUID 或完整 H3 链接身份不一致时停止同步，防止损坏从随堂笔记继续传播到母本。
- 旧损坏文件保持可读取，不自动猜测修复；在身份修复前禁止保存和同步，避免无依据地改写用户数据。
- 更新版本号为 `V1.2607151448`。

# 782 新包永久身份与可重入入库收口

- 新包名单继续长期保存在编辑会话中：建立无来源的 H3 时立即加入；删除 H3、改成非 H3 或补齐来源后立即移出。
- 名单维护增加文档事实校准；日常由编辑事件增删，打开文档、结构修改和刷新更新面板时再全文校准，避免漏报与残影。
- 新包 H3 的 UUID 从出生起就是最终母本 UUID；入库不再另造 UUID，也不再修改随堂 H3 的节点身份。
- 新包写入章教案或“上课收集”前先按原 UUID 查找；不存在才插入，已经存在则原位复用并更新，因此同一次入库可以安全重复执行，不会生成重复 H3 包。
- 母本已写入但随堂来源字段尚未写回时，再次执行会凭同一个 UUID 找到母本并补齐 `SourceID`、`SourceFile`，用于恢复中途失败。
- 重试时会先在中心教案全部 CSV 中按 UUID 找回上次实际入库的文件，并提前纳入该文件和目录的写入锁；即使关闭时已经没有原更新面板的目标章信息，也不会把同一个包再写进“上课收集”。
- 半截来源信息统一进入修复流程：保留 H3 UUID 和完整包内容，优先沿已有 `SourceFile` 修复，否则按新包收集，不再长期卡在不可处理状态。
- 新包入库写盘后重新读取随堂笔记和母本，复核 `随堂H3.id = SourceID = 母本H3.id`；任一侧没有真实落盘都会返回明确失败，不把内存成功当成完成。
- 关闭和下课前忽略界面名单，重新读取磁盘并扫描全部 H3；已入库但名单残留的项目不会阻止下课，磁盘上真实未入库的包才进入失败结果。
- 新包图片迁移沿用同一个出生 UUID，临时图片目录与正式母本包的身份不再因入库换 UUID 而断开。
- 更新版本号为 `V1.2607142329`。

# 781 H3修时自动同步与母本身份收口

- H3 包同步方向改为修时自动裁决，不再依赖“有无新式基准”决定是否强制覆盖。
- 上课中内容不同时：母本 H3 修时更晚则只提示母本待更新，不回灌当前随堂；随堂修时更晚或修时相同时，随堂自动回写母本。
- 开课准备时内容不同：母本修时更晚或相同时母本自动覆盖随堂；随堂修时更晚时自动补推母本，用于恢复上次关闭同步未完成的情况。
- 新包入库后把随堂 H3 根节点 `id` 改为母本新生成的 UUID，并同时写入 `SourceID`；从此满足“随堂 H3.id = 随堂 SourceID = 母本 H3.id”。
- 已有包回写母本时强制保留母本 H3 根 UUID，随堂的内部身份异常不会再把母本 UUID 换掉。
- 同步不再把“同步执行时间”写成 H3 修时；用户修改时形成的修时会原样随内容传入母本。
- 实时保存和最终保存在真正写盘前，都会将当前文档与磁盘上一版按 Node UUID 比较；如发现 H3 内容真实改变但某个编辑入口漏了修时，保存前统一补齐 H3 根修时。
- 空行清洗保留；关闭或下课清洗导致包内容变化时，保存前比较会给所属 H3 生成新修时。
- 新增母本文件路径与母本所在目录两级写入锁；不同学生同时更新同一章教案，以及母本正在 NodeMarkdown 编辑器中保存时，全部按同一队列串行。
- 拿到母本锁后会重新读取最新随堂和母本，只替换目标 H3 包，避免两个学生用各自旧的整篇章教案互相覆盖。
- 开课准备找不到母本或 `SourceID` 时的规则不变：保留整个 H3 包，清空来源字段后转为新包，绝不删除内容。
- 更新版本号为 `V1.2607142307`。

# 780 上课随堂笔记单写入与同步方向收口

- 新增授课文件单写入队列，同一学生目录的自动保存、手动保存、插包、同步、开课准备和下课整理严格串行，不再同时抢写随堂笔记。
- 编辑器每次有效修改递增文档版本号；保存任务只能确认自己截取的版本，旧保存等到队列头时如发现新版本，会放弃旧快照并改保存最新版。
- 旧保存完成时若用户已继续输入，保存按钮保持未完成状态，不再把新文字误标成“已保存”。
- 上课中的自动同步改为单向：只把确认变化的随堂 H3 回写母本；发现母本单向更新时只计入待更新状态，不修改当前随堂文字。
- 回写普通脏包时只写母本，不再为了更新缓存时间而重写随堂笔记；同步结果没有改变随堂文档时，不再整篇写回。
- 新包入库后必须填写 `SourceID` 和 `SourceFile`，这种确实改变随堂结构的操作依然允许写回，但必须经过同一写入队列。
- 开课准备保留母本较新时替换更旧随堂 H3 的功能；如随堂和母本相对上次同步基准都已改动，保留随堂并不自动覆盖。
- 开课准备找不到母本文件或母本 `SourceID` 时，保留整个 H3 包的文字、图片、公式和子节点，只清空根节点来源字段，将它转为新包，不再删除。
- 外部随堂文件通知到达时，只有编辑器没有未保存内容才允许重载；用户正在编辑时保留内存新版本，不自动改回磁盘旧版本。
- 显式插包和新包入库期间暂时锁定编辑面，操作完成后再重载，避免外部结构变化与用户同时输入无法安全合并。
- 上课事务失败时的“整个学生文件夹自动回滚”已挂起；失败只记录和报错，不把后续已保存内容恢复成旧快照。用户主动执行的系统一致性修复/撤销工具保留。
- 实时保存和 Command+S 不再清理空行；空行清洗只在下课或关闭 NodeMarkdown 的最终保存阶段执行。
- 更新版本号为 `V1.2607142231`。

# 779 H3脏包判定与双向同步收口

- 新增独立的 H3 包有效内容签名，一个包只按 Node 顺序、每行层级和文字判断是否变化。
- UUID、`SourceID`、`SourceFile`、缓存、修改时间等管理信息不再导致脏包，避免未修改的 H3 被误报。
- 签名前与同步入库共用同一套空行清洗，避免显示用空行与有效内容判定不一致。
- 打开文档时建立每个 H3 的本地基准；编辑后内容不同才加入脏包，Command+Z 恢复到基准内容后会自动移出脏包。
- 单 Node 修改和多 Nodes 修改统一为“收集受影响 H3 候选集合，去重后逐包比较”；被选中但实际没变的 H3 不会成为脏包。
- 群体删除会同时收集删除前的所属 H3 和删除后的所属 H3，可靠覆盖跨包操作；受保护的 H3 根节点仍不允许被群体删除。
- 预览和真正同步统一读取持久同步基准，区分本地脏包、新包、母本更新和双方冲突四种状态。
- 只有随堂笔记变化时才回写母本；只有母本变化时刷新随堂笔记；两边都变化时记录冲突并停止自动覆盖。
- 同步执行时再次计算实际包内容，不盲信界面内存里的脏包名单；相同内容会直接结算为无变化。
- 母本向随堂笔记刷新时保留随堂 H3 的 `SourceID` 和 `SourceFile`；随堂回写母本时保留母本 H3 自身的来源信息。
- 旧的同步基准 JSON 结构保持不变；首次新同步自动迁移为 `semantic-v1` 内容签名，不删除、不重建用户数据。
- 更新版本号为 `V1.2607142208`。

# 778 NodeMarkdown旧管线群体删除H3身份与保护收口

- 群体行删除不再按照删除后的行号套用删除前的行信息；旧管线在编辑发生前计算每个幸存 Node 的身份去向，删除后让 UUID、层级、`SourceID`、`SourceFile` 随对应 Node 一起移动。
- 同一个旧 Node 身份在一次拆行中只能继续交给一行；新拆出的行获得新身份，避免两个 Node 共用同一个 UUID。
- 撤销和恢复现在同时保存、恢复整组行身份信息，结构删除撤销后不会只恢复文字而丢失 H3 母本关系。
- 群体选择只要会删除受保护 H3 的层级标志、前置换行或后置换行，整次操作立即拒绝；H3 标题的普通文字修改仍然允许。
- 跨多个 H3 的群体删除会根据删除前的 Node UUID 找回每个被删 Node 原属的 H3，把所有实际受影响的 H3 一次性加入集合并去重。
- 新增子 Node 同样会按它插入后的所属 H3 记录改动；新建的 H3 根节点仍按新包处理，不误算成母本脏包。
- 焦点行修改跟踪由固定行号改为 Node UUID；前方增删行后不再拿同一个数字位置上的其他 H3 作比较。
- 旧管线解析时优先使用随编辑事务移动后的 Node UUID恢复节点信息，位置匹配只作为没有身份信息时的后备。
- 更新版本号为 `V1.2607142113`。

# 777 NodeMarkdown旧管线H3母本身份保护

- 修复旧管线上课编辑 H3 包后，H3 节点的 `SourceID` 和 `SourceFile` 可能被文本回写流程清空的问题。
- 旧管线不再只把可见文字传回文档解析器，同时传回每一行隐藏的行身份信息，包括节点 UUID、层级、`SourceID`、`SourceFile`。
- 旧管线回写源码时优先按隐藏 UUID 找回原节点；如果这是带母本来源的 H3 节点，标题或正文改了也保留原来的母本链接。
- 新增 H3 母本链接兜底恢复：解析文本和保存落盘前都会从上一版文档或磁盘文档里恢复丢失的 H3 `SourceID`、`SourceFile`。
- H3 保护规则明确为：第 3 层且 `SourceID`、`SourceFile` 都不为空才是受保护的母本 H3。
- 受保护 H3 行不允许通过 tab 或 shift+tab 改层级；tab 会在下方插入空 H4，shift+tab 会在上方插入空 H2。
- 受保护 H3 行首 backspace 不允许删除或合并该 H3 节点，避免把母本节点保护信息从结构上破坏掉。
- 更新版本号为 `V1.2607141959`。

# 775 NodeMarkdown旧管线回车焦点鲁棒化

- 修复旧管线从第 n 行回车新建 n+1 行后，焦点可能落到 n+3 行编号标志区的问题。
- 修复回车后立即输入英文时，第一个字母输入完成后光标向左跑到字母前面的问题。
- 旧管线焦点管理从“多处记录 pending structural caret anchor、多轮异步恢复”改为“操作当场计算一次真实落点，后续渲染不再抢焦点”。
- 删除旧管线 pending structural caret anchor 机制，避免异步刷新、结构删除、tab/shift+tab、用户点击之间互相争夺选区。
- `textDidChange` 和刷新调度不再恢复结构锚点，用户点击回 n+1 行时不会再被旧锚点弹回去。
- 当前行判断改为优先扫描当前文本真实换行数，不再优先信任可能滞后的 `rowCharacterRanges` 缓存。
- 回车拆行时目标行号同样按当前文本真实行号计算，再进入新行的可输入区，避免缓存错位导致落入禁入区。
- 普通字符输入后用本次 `PendingEdit` 统一确认插入后的选区，只处理单行普通输入，不干扰回车、删除、拼音标记文本。
- 更新版本号为 `V1.2607141915`。

# 772 NodeMarkdown TextKit2新管线点击焦点第二版

- 修复上一版把 TextKit2 显示存储改得过重后可能造成正文不显示、只剩编号标志的问题；这次保留新管线原有 `NSTextContentStorage/NSTextLayoutManager` 显示链路，不再替换文本存储主结构。
- 新增 `NodeMarkdownTextKit2HitTesting.swift`，单独负责把鼠标点击位置映射回 NodeMarkdown 源码行，避免系统默认命中渲染附件、隐藏前缀或不可编辑标志区后把选区拉回文首。
- TextKit2 TextView 新增左键点击回调，点击时由 coordinator 统一处理：先成为 firstResponder，再定位到被点击行的源码内容区，再进入该行源码编辑状态。
- 点击某行后会把光标压入该行可编辑内容范围，不能落在 `# `、`- ` 等编号标志禁入区，也不会继续停在文档左上角。
- 该改动只作用于 NodeMarkdown TextKit2 新管线，不改旧管线显示逻辑。
- 更新版本号为 `V1.2607132059`。

# 771 NodeMarkdown编辑行段前空间防跳动

- 修复焦点进入第 n 行后，第 n 行上方空间有时突然变小、有时不变的问题。
- 修复编辑状态打字过程中，第 n 行上方空间在“变小、恢复”之间反复跳动的问题。
- 旧 TextKit 管线新增编辑行 `paragraphStyle` 锁定：行进入源码编辑状态后，显示属性和后续输入的 `typingAttributes` 使用同一份段落样式。
- 旧管线在结构变化、换行、切换编辑行、提交渲染时清理段落样式锁，避免 tab/shift+tab 或层级变化沿用旧段前。
- TextKit2 新管线同步增加编辑行段落样式锁，输入瞬间不再先套默认段落样式再被刷新修正。
- 这次不调整段前段后常数，只统一编辑行显示和输入的段落样式来源，避免布局高度抖动。
- 更新版本号为 `V1.2607131923`。

# 770 普通Markdown模式记忆与档案刷新防闪烁

- 普通 Markdown 编辑器打开时不再固定默认“双区域”，而是读取上一次用户选择的模式。
- 模式菜单切换“双区域、TextKit2编辑、源码模式、Web渲染”后会写入本机设置，下次进入普通 Markdown 编辑器直接恢复该模式。
- 档案页监听到当前目录变化时，不再只要有文件写入就重绘列表。
- 新增档案可见列表签名比较：只有文件/文件夹路径、名称、类型、元类型发生变化时才更新 `entries`。
- 普通 Markdown 自动保存同一个文件时，背后的档案文件夹列表内容没有变化，因此不会反复刷新闪烁。
- 更新版本号为 `V1.2607131642`。

# 769 课程页随下课关闭与旧管线当前行可见性

- 点击学生进入课程页并开课后，在 NodeMarkdown 随堂笔记中点“下课”或“退出/关闭”时，课程页同步关闭。
- 课程页接到随堂笔记 session 退出回调后，除了结束 session 状态，也会执行 `dismiss()`。
- 下课流程成功完成后，课程页同样执行 `dismiss()`，避免随堂窗口关闭后课程页残留。
- 修复 NodeMarkdown 旧 TextKit 管线中“当前行打完字仍停留在该行时文字暂时消失，离开该行后才出现”的问题。
- 原因是当前编辑行为了避免跳动不做即时整行渲染，但输入点可能继承公式、图片或源码隐藏区的透明文字属性；现在每次输入后只对当前源码行做可见属性校正，不触发整篇重渲染。
- 更新版本号为 `V1.2607131620`。

# 768 随堂完成清单选中行立即写入完成状态

- 修正随堂按钮插入 H3 包时完成清单 `Status` 不变化的问题。
- 原因是清单弹窗提交后，插入服务虽然会尝试写 `Status=1`，但弹窗关闭时 `onDisappear` 会用本地旧 `rows` 自动保存，把刚写好的状态覆盖回 `0`。
- 现在在完成清单编辑器点击“插入”时，先把被选中的所有 H3 行本地改成 `Status=1` 并立即保存，再交给随堂插入流程提取 H3 包。
- 完成清单保存时保留原来的 `META_TYPE`，不再把课程完成清单强行写成普通 `checklist` 类型。
- 更新版本号为 `V1.2607131559`。

# 767 随堂H3完成清单防重复插入与保存后渲染保持

- 随堂 H3 包插入以学生该教案的完成清单 `Status` 为硬权限来源：只有 `Status=0`、L3、且带 `SourceFile/SourceID` 的行可以被选中和提交。
- 完成清单 UI 提交前再次过滤不可插入行，避免旧选择或拖选把已完成 H3 继续传给插入流程。
- 插入服务在写随堂笔记前重新读取磁盘上的完成清单，用最新 `Status` 拦截已完成项，防止同一 H3 包二次插入。
- 插入成功或发现随堂笔记已存在同 Source 包时，继续把对应完成清单行写为 `Status=1`。
- 修正 Command+S 保存后的渲染路径：保存成功后只同步 document 与草稿字符串，不再触发外部整篇文本替换，避免图片和公式附件从渲染状态退回源码状态。
- 更新说明书和白皮书，记录完成清单防重复规则与保存不重载附件渲染规则。
- 更新版本号为 `V1.2607131536`。

# 766 双区域跟随、公式兼容、随堂脏包回写与旧管线焦点稳定

- 普通 Markdown 双区域模式增加源行号同步桥：左侧 TextKit2 和右侧 Web 都以“当前视野中线所在源文本行”为锚点。
- 左侧滚动停下后发布中心源行；右侧 Web 滚动停下后通过消息通道回传中心源行；打字停止 3 秒后也会重新对齐。
- Web 渲染块增加 `startLine`，`markdown-renderer.js` 增加 `scrollToSourceLine` 和 `currentCenterSourceLine`，避免用滚动百分比造成左右不同内容错位。
- NodeMarkdown 公式规范化增加全角等号、全角大小于号、更多箭头和数学符号映射。
- NodeMarkdown 旧管线和 TextKit2 管线的 SwiftMath 公式渲染统一设置常规中文 fallback 字体，避免公式中的中文被渲染成粗体。
- NodeMarkdown 旧管线从公式行回车后强化结构光标锚点，多轮恢复到新行可输入区，减少进入 n+2 行前不可进入区的概率。
- 授课随堂笔记刷新母本包时，若随堂 H3 根节点时间不早于母本，不再用母本覆盖随堂，保证课堂改过的脏包先保留，再由同步流程回写母本。
- 更新说明书和白皮书，记录双区域跟随、公式规则、脏包同步方向和回车焦点约束。
- 更新版本号为 `V1.2607131255`。

# 765 档案移动、多选与Markdown渲染统一

- 档案页补齐文件和文件夹右键能力：改名、移入回收站、移动到其他档案目录。
- 档案页右上角按钮扩展为新建文件、新建文件夹、多选、刷新；多选后底部出现删除、移动、组成文件夹。
- 增加档案目录监听器，当前目录被访达改动后会自动请求刷新，减少手动刷新。
- 普通 Markdown 默认改为“双区域”：左侧 TextKit2 编辑，右侧 Web 预览；旧即时渲染代码保留为 Markdown 旧管道，但不再作为常用模式入口。
- 普通 Markdown TextKit2 行内样式补齐 `__bold__`、`<b>/<strong>`、`<i>/<em>`、`<code>`、`~~删除线~~`、`<s>/<del>`、`==高亮==`、`<mark>` 等源码视觉提示。
- 普通 Markdown 层级数字编号缩进时，按“同缩进 + 同编号前缀”查找同层编号，不再把不同层级编号混成一串。
- Web/PDF/HTML 导出前保护源码中显式写出的数字编号行，保留原数字和层级缩进，不交给 Web 列表自动重排。
- 更新版本号为 `V1.2607122206`。

# 763-1

## 意义

- 修复 NodeMarkdown 公式内部中文无法显示的问题，让公式中的普通中文也能进入可渲染的文本模式。

## 技术改动

- 在公共公式 LaTeX 正规化层 `NodeMarkdownFormulaLatexNormalizer` 增加普通中文连续片段包装。
- 中文片段统一转换为 `\text{...}`，旧 TextKit 管线和新 TextKit2 管线共享这个处理。
- 已存在的 `\text{...}` 内容会跳过，不做二次嵌套包装。
- 保留已有的脚标、上标、`\sqrt{}` 中文包装逻辑。

# 763-2

## 意义

- 修复从有公式或附件的第 n 行回车后，焦点仍可能进入第 n+2 行前不可进入区域的问题。

## 技术改动

- 旧 TextKit 管线回车后先同步设置新行可编辑起点，再在下一轮主线程重新按新行 prefix 夹紧光标。
- TextKit2 管线回车后也增加异步二次校正，按新行源码 prefix 重新设置 selection。
- 回车后立即滚动到校正后的光标位置，避免回车瞬间焦点跑到视野外。

# 763-3

## 意义

- 修复 Command+S 保存后，刚渲染好的图片或公式附件可能退回成未渲染状态的问题。

## 技术改动

- 保存成功后强制从当前 document 刷新 TextKit 草稿，并触发外部文本同步 token。
- 避免保存按钮或快捷键改变焦点后，编辑视图继续停留在旧的附件显示状态。

# 763-4

## 意义

- 降低 NodeMarkdown 打字时文字上下跳动的问题，尤其是拼音输入和确认中文时，不让当前输入行在源码态和渲染态之间反复切换。

## 技术改动

- 旧 TextKit 管线发生结构变化时，延迟刷新排除当前正在编辑的行，只刷新其它受影响行。
- TextKit2 管线中，单行输入不再立即刷新当前行样式；只有换行或 prefix 变化才触发当前行刷新。
- 当前行继续保持源码编辑态，避免段前空间在输入瞬间被渲染过程吃掉再恢复。

# 760

## 目标

- 更新版本号并记录本次改动。
- 机构页按左上角选中月份展示总时长、总收入，并让机构本月/上月统计跟随选中月份。
- 成课和排课支持课程复制、空白处粘贴、单击空白格添加课程、拖动冲突失败提示、撤回/恢复。
- 课程修改弹窗增加开始时间快捷调整，开始时间变动后结束时间自动按 +2 小时刷新。
- 学生新增初始化增加统计设置页，第一次建档即可写入机构和 2 小时课时价格。

## 改动

- 机构页顶部增加月份控制、绿色总时长高亮、黄色总收入高亮。
- 机构列表的本月/上月、复制文字、导出图片改为基于左上角选中月计算。
- 成课顶部在导出左侧增加添加按钮，添加课程复用原有学生统计设置和冲突检查。
- 排课/周表与成课的课块右键增加“复制”，空白格子右键增加“粘贴”；粘贴保留原课程时间段，只替换为右键所在日期。
- 图形课表空白格子支持左键单击直接插入课程。
- 排课/周表与成课增加本地撤回/恢复栈，并接入 Command+Z / Command+Shift+Z。
- 课程时间编辑弹窗在开始时间下方增加 +5、+10、+20、+30、+40、+50 快捷按钮，开始时间变化时结束时间自动更新为 +2 小时。
- 学生新增向导从 6 页扩展为 7 页，第 6 页为统计设置，第 7 页为同步文件夹；统计设置保存到学生 profile 的 `lessonStatistics`。
- 更新版本号为 `V1.2607111946`。

## 验证

- Mac 构建通过。

# 745

## 目标

- 修复统计页中课表、排课、周表区域最低高度过高，导致小窗口下顶掉上方主功能按钮、统计子页切换栏和底部三按钮的问题。
- 按版本规则更新版本号为 `V1.2607101805`。

## 改动

- 去掉 `TeachingLessonStatisticsSubpageView` 中课表图形区域外层 `minHeight: 620`。
- 去掉 `TeachingLessonPlanningBoardView` 中排课外层 `minHeight: 620` 和周表外层 `minHeight: 560`。
- 更新 Xcode 工程 Debug/Release 的 `CURRENT_PROJECT_VERSION` 和 `MARKETING_VERSION`。

## 验证

- Mac 构建通过。

# 759

## 目标

- 修复通过 Finder 打开 Markdown 文件时同时打开主窗口的问题。
- 修复连续打开多个 Markdown 文件时重复打开多个外部 Markdown 标签页窗口的问题。
- 保持后续打开的 Markdown 文件追加到已有标签页窗口，标签页仍可拖出为独立页。
- 更新版本号。

## 改动

- 主窗口启动行为改为 suppressed，由 AppDelegate 在普通启动时主动打开；如果本次启动来自 Markdown 文件打开，则不主动打开主窗口。
- 外部 Markdown 标签页窗口从 SwiftUI `WindowGroup` 改为单例 `NSWindow` 管理，保证 A 文档打开后再打开 B 文档只追加标签页，不再新建第二个 Markdown 标签窗口。
- 外部 Markdown 文件路由收到文件后立即交给单例标签页窗口，不再依赖主窗口存在。
- 拖出标签页成为独立 Markdown 窗口时，改由外部 Markdown 窗口管理器创建单文件窗口，保留原有拖出体验。
- 更新版本号为 `V1.2607111447`。

## 验证

- Mac 构建通过。

# 758

## 目标

- 继续完善外部 Markdown 标签页窗口。
- 支持把标签页通过拖拽分离为独立 Markdown 编辑窗口。
- 更新版本号。

## 改动

- 外部 Markdown 标签页增加拖拽手势，拖动距离超过 90pt 后松手，会打开成独立 `markdown-editor` 窗口，并从标签页窗口移除该标签。
- 拖动过程中标签页有轻微位移、缩放和阴影反馈，明确处于拖动状态。
- 保留右键和双击“在新窗口打开”作为备用入口。
- 更新版本号为 `V1.2607111437`。

## 验证

- Mac 构建通过。

# 757

## 目标

- 继续完善外部 Markdown 默认编辑器能力。
- 增加外部修改检测，避免自动保存覆盖其他软件刚改过的文件。
- 更新版本号。

## 改动

- 普通 Markdown 编辑器读入文件后记录文件修改时间。
- 自动保存、手动保存、关闭前保存都会在写入前检查磁盘文件是否被外部修改。
- 自动保存遇到外部修改时停止写入并把保存状态置为失败，不静默覆盖磁盘文件。
- 手动保存遇到外部修改时弹窗提示，可选择“重新载入”、“保留当前内容并覆盖”或“取消”。
- 覆盖保存后刷新最后已知文件修改时间，后续自动保存继续正常工作。
- 更新版本号为 `V1.2607111435`。

## 验证

- Mac 构建通过。

# 756

## 目标

- 继续完善外部 Markdown 标签页窗口。
- 支持把标签页打开为独立 Markdown 编辑窗口。
- 让外部 Markdown 标签页窗口标题跟随当前文件。
- 更新版本号。

## 改动

- 外部 Markdown 标签页支持右键“在新窗口打开”，会用原有单文件 `markdown-editor` 窗口打开该文件，并从标签页窗口移除该标签。
- 双击标签页也会执行“在新窗口打开”，作为拖出独立页的第一版快捷入口。
- 外部 Markdown 标签页窗口标题同步为 `Markdown-<当前文件名>`，没有文件时显示 `Markdown`。
- 将 Markdown 窗口 ID 从文件私有改为模块内可见，供外部标签页窗口复用已有单文件编辑窗口。
- 更新版本号为 `V1.2607111433`。

## 验证

- Mac 构建通过。

# 755

## 目标

- Markdown 外部打开功能第3步：完善普通 Markdown 编辑器作为外部文件编辑器时的自动保存和文件权限处理。
- 更新版本号。

## 改动

- 普通 Markdown 编辑器从 60 秒轮询自动保存改为输入后 1.2 秒防抖自动保存。
- 手动保存会取消等待中的自动保存任务，避免重复写入。
- 关闭或切换离开编辑器时取消等待中的自动保存任务，并立即保存未保存内容。
- 读取和写入文件时统一使用 security scoped access，增强 Finder / “打开方式”传入外部文件时的沙盒读写稳定性。
- 保留现有保存状态：未保存为蓝色，保存中、失败、已同步继续沿用原图标状态。
- 更新版本号为 `V1.2607111430`。

## 验证

- Mac 构建通过。

# 754

## 目标

- Markdown 外部打开功能第2步：Finder 打开的多个 Markdown 文件进入同一个外部 Markdown 标签页窗口。
- 保留 APP 内部原有 Markdown 打开方式不变。
- 更新版本号。

## 改动

- 新增 `ExternalMarkdownTabWindowView.swift`，包含外部 Markdown 标签页模型、标签页状态中心和标签页窗口外壳。
- 新增 `external-markdown-tabs` 窗口组，专门承载外部 Markdown 文件。
- 外部 `.md` / `.markdown` 打开路由改为加入标签页状态中心，并打开外部 Markdown 标签页窗口。
- 原 `markdown-editor` 窗口组继续保留，供档案、授课等 APP 内部调用。
- 更新版本号为 `V1.2607111426`。

## 验证

- Mac 构建通过。

# 753

## 目标

- Markdown 外部打开功能第1步：让系统能把 `.md` / `.markdown` 文件交给 ATeaching，并进入普通 Markdown 编辑器。
- 更新版本号。

## 改动

- 在 `Info.plist` 中声明 Markdown 文档类型，支持 `.md` 和 `.markdown`，角色为 `Editor`。
- 增加 `net.daringfireball.markdown` 的导入类型声明，兼容常见 Markdown UTI。
- 增加外部 Markdown 打开路由：macOS 通过 Finder 或“打开方式”传入文件时，进入现有 `markdown-editor` 窗口。
- 主窗口和备用主窗口统一接收外部 Markdown 打开通知，使用现有 `MarkdownEditorView` 打开文件。
- 更新版本号为 `V1.2607111423`。

## 验证

- Mac 构建通过。

# 752

## 目标

- 更新版本号。
- 调整统计页“成课、排课、周表”下面的文字列表显示方式。

## 改动

- 成课下面的机构汇总改为机构、小时、金额三列显示，保留“机构;小时;金额；”的视觉结构，并用机构颜色透明背景。
- 成课详单改为日期分组，课程行按“时间、机构、学生”三列显示，并使用机构颜色透明背景。
- 排课和周表下面的记录列表增加首行预计汇总，周表也显示“月预计课时量、月预计收入”。
- 排课记录行改为“圆形机构色、日期+时间、机构、学生”四段布局。
- 周表记录行改为“圆形机构色、星期 | 时间、机构、学生”四段布局。
- 更新版本号为 `V1.2607102159`。

## 验证

- Mac 构建通过。

# 751

## 目标

- 更新版本号。
- 统计页四个子页名称应为“机构、成课、排课、周表”，第二项不能只显示“课”。

## 改动

- 将统计页 `SectionMode.schedule` 的显示名从“课”改为“成课”。
- 更新版本号为 `V1.2607102139`。

## 验证

- Mac 构建通过。

# 749

## 目标

- 排课页面下面的文字第一条增加月预计课时量和月预计收入。

## 改动

- 在排课记录列表顶部增加“月预计课时量：...；月预计收入：...”摘要行。
- 摘要按当前展示月份内的排课/历史显示记录计算，跨月课程按当前月份裁切小时。
- 预计收入按课程快照中的 2 小时价格折算：`priceForTwoHours * 小时 / 2`。
- 更新版本号为 `V1.2607102111`。

## 验证

- Mac 构建通过。

# 750

## 目标

- 更新版本号。
- 修正课表、排课、周表导出图片中课程列宽和课块高度小于 APP 内显示，导致学生名字显示不全的问题。
- 导出渐变圆角图片时，圆角外侧保持透明，不再填白色。

## 改动

- 导出课表网格的每小时高度改为和 APP 内一致的 `36.7`，每列宽度继续按高度 `1.68` 倍计算。
- 导出网格的时间轴宽度、列间距、内边距、标题高度改为和 APP 内排课图一致。
- 导出课块里的学生名、机构、时间、时长字体改为和 APP 内课块一致。
- 课表、排课、周表相关图片导出的 `ImageRenderer` 统一设置为透明背景，并去掉一个导出入口的外层白色背景。
- 更新版本号为 `V1.2607102131`。

## 验证

- Mac 构建通过。

# 748

## 目标

- 更新版本号。
- 统计页中“课表”改名为“课”。
- 成课和排课右键菜单统一为复制文本、导出图片、可用导出、修改、删除 8 项。
- 成课、排课、机构数据的复制文本改为带星期列，并用空格进行紧凑对齐。

## 改动

- `MARKETING_VERSION` 更新为 `1.2607102046`，`CURRENT_PROJECT_VERSION` 更新为 `2607102046`。
- 统计子页切换项从“课表”改为“课”。
- 成课/排课课块右键菜单改为“复制文本·学生·当月/机构·当月/导出图片...”等完整名称。
- 新增按学生/机构范围生成当月文本的统一方法，输出“机构/学生、年月、总课时、日期 | 星期 | 时间 | 学生 | 小时”。
- 机构列表右键菜单改为“复制文字·上月课/当月课、导出图片·上月课/当月课”。
- 机构数据弹窗和导出增加星期列，并统一空格对齐。

## 验证

- Mac 构建通过。
# 2-40 APP内投屏

## 目标

- 投屏页以名字作为全部功能的使用前提。
- 在同一Wi-Fi内使用四位数字自动匹配投屏端和接收端。
- 支持同时投送上课NodeMarkdown和普通Markdown，接收端同步渲染文档、视野与鼠标。
- 接收状态使用专用观看界面，并提供批注、橡皮、清除和退出。

## 改动

- 将原固定IP、固定端口、单次读取的TCP原型重构为Bonjour发现、四位码握手和长度分帧协议，正确处理TCP拆包与粘包。
- 新增投屏会话、成员、频道、文档快照、视野、鼠标和批注数据模型；接收成员按设备稳定分配不同颜色。
- 投屏按钮确认四位数字后立即返回投屏页，状态区显示四位数字、上课/Markdown多选、修改四位数字和成员颜色列表。
- 接收端记忆上次四位数字，断开后保留最后画面并要求再次确认四位数字；连接后主窗口进入专用观看页，Mac暂时隐藏其他编辑窗口。
- NodeMarkdown上课编辑器和普通Markdown编辑器通过独立内容中心发布延迟快照，不介入保存、焦点和输入事务。
- 文档以完整渲染HTML传送；本地图片转换为Data URI，接收设备不依赖主控文件路径。
- 主控只读采样当前真实滚动容器和鼠标位置，不改变主控视野；缩小或离开编辑器时发送暂停状态。
- 接收WebView按文档版本持续更新，并按标准化滚动比例同步视野；批注使用跨Mac/iOS的矢量画布和双向消息。
- Info.plist增加本地网络用途说明和`_ateaching-cast._tcp` Bonjour服务声明。
- 版本更新为`V1.2608120742`。

## 验证

- Mac Debug构建通过。
- iPhone 17（iOS 26.5）Simulator Debug构建通过。

# 2-41 投屏启动后立即复位

## 问题

- Mac确认四位数字后，投屏状态短暂出现，随后立即恢复为未连接。
- `【诊断·40】`确认同一个`ScreenCastService`没有被SwiftUI重建；真正原因是沙盒拒绝`NWListener`入站监听，返回`Operation not permitted`。
- Listener失败后统一清理又把错误状态覆盖成“未连接”，因此界面只表现为闪一下。

## 改动

- Mac Debug和Release均启用App Sandbox的Incoming Connections，保留原有Outgoing Connections。
- Listener创建或运行失败时，清理连接资源但保留明确的失败信息，不再被“未连接”覆盖。
- 增加仅Debug、必须使用`--screen-cast-listener-probe`显式触发的签名App自检入口。
- 保留`【诊断·38】`和`【诊断·40】`，其他专项诊断不再输出。

## 验证

- 重新生成的签名Mac App实际携带`com.apple.security.network.server=true`和`com.apple.security.network.client=true`。
- 在签名沙盒App内运行自动自检，`NWListener`真实进入`ready`，状态变为“投屏中，等待接收端”。
- Mac Debug与iPhone Simulator Debug构建通过。

# 2-46 NodeMarkdown ProseMirror式核心架构

## 目标

- 将NodeMarkdown新管线从“整篇连续TextStorage加行元数据”改为真正的Node文档架构。
- 复制ProseMirror的Node State、Transaction、Step、Position Mapping、History、局部视口和活动编辑器思路，保留Swift/AppKit原生实现。
- 让普通Node输入的主路径与文档大小基本无关，并从结构上消除相邻Node串内容、串附件和串样式。

## 改动

- 新增`NodeMarkdownProseMirrorCore.swift`，实现Node位置与选区、八类事务Step、位置映射、基础版本验证、反向Step历史、UUID/父Node/子树/H3包结构索引。
- `NodeMarkdownTextKit2DocumentState`成为事务的唯一执行者。单Node文字变更不复制全文；结构事务具有原子回滚、撤销、重做和保护H3历史恢复。
- 新增`NodeMarkdownProseMirrorEditor.swift`，使用`NSTableView`虚拟化Node块。只创建可见Node的TextKit2视图，每个Node拥有独立`NSTextStorage`/`NSTextContentStorage`/`NSTextLayoutManager`对象链。
- Node静态显示继续使用原管线的公式、图片、编号、高亮和背景条渲染；进入编辑时只还原该Node附件源码。
- 普通英文和中文输入只更新当前Node值和当前视图，不构造全文快照、不平移后续行范围、不回写SwiftUI；离行或保存才按UUID提交草稿并触发脏包/新包判定。
- 回车、带选区回车、Tab/Shift+Tab、行首Backspace、行末Delete、多行粘贴、包操作和图片插入接入Node事务或显式外部快照。多行粘贴生成多个Node，禁止Node内隐藏换行。
- 上下方向键在Node边界按UUID切换到相邻Node。搜索/目录定位使用目标行真实矩形；单击进入编辑不调用追焦滚动。
- 可见Node高度由TextKit2的真实`NSTextLayoutFragment`测量。渲染缓存按UUID、内容版本、样式和宽度定位；Fenwick索引以`O(log N)`更新高度并保持滚动锚点。
- 保留旧的整篇TextKit2实现用于回归对照，但Mac新管线默认入口已切换到块式编辑器。
- 增加事务位置映射、相邻Node隔离、结构索引、过期事务、拆分撤销/重做、受保护H3回车撤销和5万行高度索引回归检查。
- 版本更新为`V1.2608132226`。

## 验证

- Mac Debug构建通过，启动8秒无断言、无SwiftMath资源崩溃。
- iPhone Simulator Debug通用目标构建通过。
- Debug回归套件覆盖Node事务、结构索引、高度索引、输入法属性、L9/L10样式、公式基线和H3包收集。
